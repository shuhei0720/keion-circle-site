# 第19章：投稿ページの実装

この章では、投稿一覧ページと投稿作成ページを実装していきます。検索、フィルター、投稿フォームなどの機能を含みます。

## 19.1 投稿一覧ページ

### app/posts/page.tsx

```typescript
import Link from 'next/link';
import { prisma } from '@/lib/db';
import { auth } from '@/lib/auth';
import PostCard from '@/components/PostCard';
import SearchBox from '@/components/SearchBox';
import { Plus } from 'lucide-react';

interface PageProps {
  searchParams: Promise<{
    q?: string;
    page?: string;
  }>;
}

async function getPosts(searchQuery?: string, page: number = 1) {
  const limit = 12;
  const skip = (page - 1) * limit;
  
  const where: any = {};
  
  if (searchQuery) {
    where.OR = [
      { title: { contains: searchQuery, mode: 'insensitive' } },
      { content: { contains: searchQuery, mode: 'insensitive' } },
    ];
  }
  
  const [posts, total] = await Promise.all([
    prisma.post.findMany({
      where,
      include: {
        author: {
          select: {
            id: true,
            name: true,
            image: true,
          },
        },
        _count: {
          select: {
            likes: true,
            comments: true,
          },
        },
      },
      orderBy: { createdAt: 'desc' },
      skip,
      take: limit,
    }),
    prisma.post.count({ where }),
  ]);
  
  return {
    posts,
    pagination: {
      total,
      page,
      limit,
      pages: Math.ceil(total / limit),
    },
  };
}

export default async function PostsPage({ searchParams }: PageProps) {
  const params = await searchParams;
  const session = await auth();
  
  const searchQuery = params.q || '';
  const page = parseInt(params.page || '1');
  
  const { posts, pagination } = await getPosts(searchQuery, page);
  
  return (
    <div className="max-w-6xl mx-auto px-4 py-8">
      {/* ヘッダー */}
      <div className="flex justify-between items-center mb-8">
        <h1 className="text-3xl font-bold">📝 活動報告</h1>
        
        {/* 管理者のみ投稿作成ボタン表示 */}
        {session?.user?.role === 'admin' && (
          <Link
            href="/posts/new"
            className="flex items-center gap-2 px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors"
          >
            <Plus className="w-5 h-5" />
            新規投稿
          </Link>
        )}
      </div>
      
      {/* 検索ボックス */}
      <div className="mb-8">
        <SearchBox
          placeholder="投稿を検索..."
          defaultValue={searchQuery}
        />
      </div>
      
      {/* 検索結果の表示 */}
      {searchQuery && (
        <div className="mb-6">
          <p className="text-gray-600">
            「{searchQuery}」の検索結果: {pagination.total}件
          </p>
        </div>
      )}
      
      {/* 投稿一覧 */}
      {posts.length === 0 ? (
        <div className="text-center py-12">
          <p className="text-gray-600 mb-4">
            {searchQuery ? '検索結果が見つかりませんでした' : 'まだ投稿がありません'}
          </p>
          
          {!searchQuery && session?.user?.role === 'admin' && (
            <Link
              href="/posts/new"
              className="inline-flex items-center gap-2 px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors"
            >
              <Plus className="w-5 h-5" />
              最初の投稿を作成
            </Link>
          )}
        </div>
      ) : (
        <>
          <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8">
            {posts.map(post => (
              <PostCard key={post.id} post={post} />
            ))}
          </div>
          
          {/* ページネーション */}
          {pagination.pages > 1 && (
            <Pagination pagination={pagination} />
          )}
        </>
      )}
    </div>
  );
}
```

---

## 19.2 投稿作成ページ

### app/posts/new/page.tsx

```typescript
import { redirect } from 'next/navigation';
import { auth } from '@/lib/auth';
import PostForm from '@/components/PostForm';

export default async function NewPostPage() {
  const session = await auth();
  
  // 認証チェック
  if (!session) {
    redirect('/login');
  }
  
  // 管理者チェック
  if (session.user.role !== 'admin') {
    redirect('/posts');
  }
  
  return (
    <div className="max-w-4xl mx-auto px-4 py-8">
      <h1 className="text-3xl font-bold mb-8">📝 新規投稿</h1>
      
      <PostForm />
    </div>
  );
}
```

---

## 19.3 投稿フォームコンポーネント

### components/PostForm.tsx

```typescript
'use client';

import { useState, FormEvent } from 'react';
import { useRouter } from 'next/navigation';
import { Plus, X, Upload, Youtube } from 'lucide-react';
import ImageUpload from './ImageUpload';

interface PostFormProps {
  initialData?: {
    id: string;
    title: string;
    content: string;
    youtubeUrls: string[];
    images: string[];
  };
}

export default function PostForm({ initialData }: PostFormProps) {
  const router = useRouter();
  const [loading, setLoading] = useState(false);
  
  // フォームの状態
  const [formData, setFormData] = useState({
    title: initialData?.title || '',
    content: initialData?.content || '',
    youtubeUrls: initialData?.youtubeUrls || [''],
    images: initialData?.images || [],
  });
  
  // YouTube URLを追加
  const addYoutubeUrl = () => {
    setFormData({
      ...formData,
      youtubeUrls: [...formData.youtubeUrls, ''],
    });
  };
  
  // YouTube URLを削除
  const removeYoutubeUrl = (index: number) => {
    setFormData({
      ...formData,
      youtubeUrls: formData.youtubeUrls.filter((_, i) => i !== index),
    });
  };
  
  // YouTube URLを更新
  const updateYoutubeUrl = (index: number, value: string) => {
    const newUrls = [...formData.youtubeUrls];
    newUrls[index] = value;
    setFormData({ ...formData, youtubeUrls: newUrls });
  };
  
  // 画像アップロード完了
  const handleImagesUpload = (urls: string[]) => {
    setFormData({ ...formData, images: urls });
  };
  
  // フォーム送信
  const handleSubmit = async (e: FormEvent) => {
    e.preventDefault();
    
    if (!formData.title || !formData.content) {
      alert('タイトルと内容を入力してください');
      return;
    }
    
    setLoading(true);
    
    try {
      const url = initialData
        ? `/api/posts/${initialData.id}`
        : '/api/posts';
      
      const method = initialData ? 'PUT' : 'POST';
      
      const response = await fetch(url, {
        method,
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          ...formData,
          youtubeUrls: formData.youtubeUrls.filter(url => url.trim() !== ''),
        }),
      });
      
      if (!response.ok) {
        const data = await response.json();
        throw new Error(data.error);
      }
      
      const post = await response.json();
      router.push(`/posts/${post.id}`);
      router.refresh();
    } catch (error) {
      alert(error instanceof Error ? error.message : '投稿に失敗しました');
    } finally {
      setLoading(false);
    }
  };
  
  return (
    <form onSubmit={handleSubmit} className="space-y-6">
      {/* タイトル */}
      <div>
        <label className="block text-sm font-medium mb-2">
          タイトル <span className="text-red-600">*</span>
        </label>
        <input
          type="text"
          value={formData.title}
          onChange={(e) => setFormData({ ...formData, title: e.target.value })}
          placeholder="例: 新歓ライブを開催しました！"
          className="w-full px-4 py-3 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
          required
        />
      </div>
      
      {/* 内容 */}
      <div>
        <label className="block text-sm font-medium mb-2">
          内容 <span className="text-red-600">*</span>
        </label>
        <textarea
          value={formData.content}
          onChange={(e) => setFormData({ ...formData, content: e.target.value })}
          placeholder="活動の内容を詳しく書いてください..."
          rows={12}
          className="w-full px-4 py-3 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
          required
        />
        <p className="text-sm text-gray-600 mt-1">
          {formData.content.length} 文字
        </p>
      </div>
      
      {/* YouTube URL */}
      <div>
        <label className="block text-sm font-medium mb-2">
          <Youtube className="w-4 h-4 inline mr-1" />
          YouTube動画
        </label>
        
        <div className="space-y-3">
          {formData.youtubeUrls.map((url, index) => (
            <div key={index} className="flex gap-2">
              <input
                type="url"
                value={url}
                onChange={(e) => updateYoutubeUrl(index, e.target.value)}
                placeholder="https://www.youtube.com/watch?v=..."
                className="flex-1 px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
              />
              
              {formData.youtubeUrls.length > 1 && (
                <button
                  type="button"
                  onClick={() => removeYoutubeUrl(index)}
                  className="p-2 text-red-600 hover:bg-red-50 rounded-lg transition-colors"
                >
                  <X className="w-5 h-5" />
                </button>
              )}
            </div>
          ))}
        </div>
        
        <button
          type="button"
          onClick={addYoutubeUrl}
          className="mt-3 flex items-center gap-2 text-blue-600 hover:text-blue-700 text-sm font-medium"
        >
          <Plus className="w-4 h-4" />
          YouTube URLを追加
        </button>
      </div>
      
      {/* 画像アップロード */}
      <div>
        <label className="block text-sm font-medium mb-2">
          <Upload className="w-4 h-4 inline mr-1" />
          画像
        </label>
        
        <ImageUpload
          onUpload={handleImagesUpload}
          maxFiles={5}
          existingImages={formData.images}
        />
      </div>
      
      {/* 送信ボタン */}
      <div className="flex gap-4">
        <button
          type="submit"
          disabled={loading}
          className="flex-1 px-6 py-3 bg-blue-600 text-white rounded-lg font-medium hover:bg-blue-700 disabled:opacity-50 disabled:cursor-not-allowed transition-colors"
        >
          {loading ? '投稿中...' : initialData ? '更新する' : '投稿する'}
        </button>
        
        <button
          type="button"
          onClick={() => router.back()}
          className="px-6 py-3 border rounded-lg hover:bg-gray-100 transition-colors"
        >
          キャンセル
        </button>
      </div>
    </form>
  );
}
```

---

## 19.4 YouTube埋め込みコンポーネント

### components/YouTubeEmbed.tsx

```typescript
'use client';

import YouTube from 'react-youtube';

interface YouTubeEmbedProps {
  url: string;
}

/**
 * YouTube URLからVideo IDを抽出
 */
function extractVideoId(url: string): string | null {
  // 正規表現パターン
  const patterns = [
    /(?:youtube\.com\/watch\?v=|youtu\.be\/|youtube\.com\/embed\/)([^&\n?#]+)/,
    /youtube\.com\/shorts\/([^&\n?#]+)/,
    /youtube\.com\/live\/([^&\n?#]+)/,
  ];
  
  for (const pattern of patterns) {
    const match = url.match(pattern);
    if (match && match[1]) {
      return match[1];
    }
  }
  
  return null;
}

export default function YouTubeEmbed({ url }: YouTubeEmbedProps) {
  const videoId = extractVideoId(url);
  
  if (!videoId) {
    return (
      <div className="aspect-video bg-gray-200 rounded-lg flex items-center justify-center">
        <p className="text-gray-600">無効なYouTube URLです</p>
      </div>
    );
  }
  
  return (
    <div className="aspect-video rounded-lg overflow-hidden">
      <YouTube
        videoId={videoId}
        opts={{
          width: '100%',
          height: '100%',
          playerVars: {
            autoplay: 0,
          },
        }}
        className="w-full h-full"
      />
    </div>
  );
}
```

---

## 19.5 投稿プレビュー

### components/PostPreview.tsx

```typescript
'use client';

import { useState } from 'react';
import { Eye, EyeOff } from 'lucide-react';
import YouTubeEmbed from './YouTubeEmbed';
import Image from 'next/image';

interface PostPreviewProps {
  title: string;
  content: string;
  youtubeUrls: string[];
  images: string[];
}

export default function PostPreview({
  title,
  content,
  youtubeUrls,
  images,
}: PostPreviewProps) {
  const [showPreview, setShowPreview] = useState(false);
  
  if (!showPreview) {
    return (
      <button
        type="button"
        onClick={() => setShowPreview(true)}
        className="flex items-center gap-2 text-blue-600 hover:text-blue-700 text-sm font-medium"
      >
        <Eye className="w-4 h-4" />
        プレビューを表示
      </button>
    );
  }
  
  return (
    <div className="border rounded-lg p-6 bg-gray-50">
      {/* ヘッダー */}
      <div className="flex justify-between items-center mb-4">
        <h3 className="font-bold text-lg">プレビュー</h3>
        
        <button
          type="button"
          onClick={() => setShowPreview(false)}
          className="flex items-center gap-2 text-gray-600 hover:text-gray-700 text-sm"
        >
          <EyeOff className="w-4 h-4" />
          非表示
        </button>
      </div>
      
      {/* プレビュー内容 */}
      <div className="bg-white rounded-lg p-6">
        {/* タイトル */}
        <h1 className="text-3xl font-bold mb-6">
          {title || 'タイトル未入力'}
        </h1>
        
        {/* 本文 */}
        <div className="prose max-w-none mb-6">
          {content ? (
            <p className="whitespace-pre-wrap">{content}</p>
          ) : (
            <p className="text-gray-400">本文未入力</p>
          )}
        </div>
        
        {/* YouTube動画 */}
        {youtubeUrls.filter(url => url.trim()).length > 0 && (
          <div className="space-y-4 mb-6">
            {youtubeUrls
              .filter(url => url.trim())
              .map((url, index) => (
                <YouTubeEmbed key={index} url={url} />
              ))}
          </div>
        )}
        
        {/* 画像 */}
        {images.length > 0 && (
          <div className="grid md:grid-cols-2 gap-4">
            {images.map((image, index) => (
              <div key={index} className="relative aspect-video">
                <Image
                  src={image}
                  alt={`Image ${index + 1}`}
                  fill
                  className="object-cover rounded-lg"
                />
              </div>
            ))}
          </div>
        )}
      </div>
    </div>
  );
}
```

---

## 19.6 投稿作成フォーム（プレビュー付き）

### components/PostFormWithPreview.tsx

```typescript
'use client';

import { useState } from 'react';
import PostForm from './PostForm';
import PostPreview from './PostPreview';

export default function PostFormWithPreview() {
  const [formData, setFormData] = useState({
    title: '',
    content: '',
    youtubeUrls: [''],
    images: [],
  });
  
  return (
    <div className="grid lg:grid-cols-2 gap-8">
      {/* フォーム */}
      <div>
        <h2 className="text-xl font-bold mb-4">投稿内容</h2>
        <PostForm
          onChange={(data) => setFormData(data)}
        />
      </div>
      
      {/* プレビュー */}
      <div className="lg:sticky lg:top-24 h-fit">
        <h2 className="text-xl font-bold mb-4">プレビュー</h2>
        <PostPreview {...formData} />
      </div>
    </div>
  );
}
```

---

## 19.7 投稿編集ページ

### app/posts/[id]/edit/page.tsx

```typescript
import { notFound, redirect } from 'next/navigation';
import { auth } from '@/lib/auth';
import { prisma } from '@/lib/db';
import PostForm from '@/components/PostForm';

interface PageProps {
  params: Promise<{
    id: string;
  }>;
}

async function getPost(id: string) {
  return prisma.post.findUnique({
    where: { id },
    select: {
      id: true,
      title: true,
      content: true,
      youtubeUrls: true,
      images: true,
      authorId: true,
    },
  });
}

export default async function EditPostPage({ params }: PageProps) {
  const { id } = await params;
  const session = await auth();
  
  // 認証チェック
  if (!session) {
    redirect('/login');
  }
  
  // 投稿を取得
  const post = await getPost(id);
  
  if (!post) {
    notFound();
  }
  
  // 管理者チェック
  if (session.user.role !== 'admin') {
    redirect(`/posts/${id}`);
  }
  
  return (
    <div className="max-w-4xl mx-auto px-4 py-8">
      <h1 className="text-3xl font-bold mb-8">✏️ 投稿を編集</h1>
      
      <PostForm initialData={post} />
    </div>
  );
}
```

---

## 19.8 下書き保存機能

### hooks/useDraftPost.ts

```typescript
'use client';

import { useEffect } from 'react';

const DRAFT_KEY = 'post_draft';

export interface DraftPost {
  title: string;
  content: string;
  youtubeUrls: string[];
  images: string[];
  savedAt: string;
}

/**
 * 下書きを保存・読み込みするカスタムフック
 */
export function useDraftPost() {
  // 下書きを保存
  const saveDraft = (data: Omit<DraftPost, 'savedAt'>) => {
    const draft: DraftPost = {
      ...data,
      savedAt: new Date().toISOString(),
    };
    
    localStorage.setItem(DRAFT_KEY, JSON.stringify(draft));
  };
  
  // 下書きを読み込み
  const loadDraft = (): DraftPost | null => {
    if (typeof window === 'undefined') return null;
    
    const stored = localStorage.getItem(DRAFT_KEY);
    if (!stored) return null;
    
    try {
      return JSON.parse(stored);
    } catch {
      return null;
    }
  };
  
  // 下書きを削除
  const clearDraft = () => {
    localStorage.removeItem(DRAFT_KEY);
  };
  
  return {
    saveDraft,
    loadDraft,
    clearDraft,
  };
}
```

### 使用例

```typescript
'use client';

import { useEffect } from 'react';
import { useDraftPost } from '@/hooks/useDraftPost';

export default function PostForm() {
  const { saveDraft, loadDraft, clearDraft } = useDraftPost();
  const [formData, setFormData] = useState({
    title: '',
    content: '',
    youtubeUrls: [''],
    images: [],
  });
  
  // マウント時に下書きを読み込み
  useEffect(() => {
    const draft = loadDraft();
    if (draft) {
      const shouldRestore = confirm(
        `下書きが見つかりました（${new Date(draft.savedAt).toLocaleString()}）。復元しますか？`
      );
      
      if (shouldRestore) {
        setFormData(draft);
      } else {
        clearDraft();
      }
    }
  }, []);
  
  // フォームデータが変更されたら下書きを保存
  useEffect(() => {
    const timer = setTimeout(() => {
      if (formData.title || formData.content) {
        saveDraft(formData);
      }
    }, 1000); // 1秒後に保存
    
    return () => clearTimeout(timer);
  }, [formData]);
  
  // 投稿成功時に下書きを削除
  const handleSubmit = async () => {
    // ... 投稿処理
    clearDraft();
  };
  
  return (
    // ... フォーム
  );
}
```

---

## まとめ

この章では、投稿ページの実装について学びました：

### 投稿一覧ページ
- ✅ **検索機能**: キーワード検索
- ✅ **ページネーション**: 12件ずつ表示
- ✅ **投稿カード**: サムネイル、タイトル、統計情報
- ✅ **管理者専用**: 新規投稿ボタン

### 投稿作成ページ
- ✅ **PostForm**: タイトル、内容、YouTube URL、画像
- ✅ **YouTube埋め込み**: 複数のYouTube動画
- ✅ **画像アップロード**: 最大5枚まで
- ✅ **プレビュー機能**: リアルタイムプレビュー

### 高度な機能
- ✅ **下書き保存**: localStorageに自動保存
- ✅ **投稿編集**: 既存の投稿を編集
- ✅ **バリデーション**: 必須項目のチェック

次の章では、**投稿詳細ページの実装**について詳しく見ていきます。

---

[← 前の章：第18章 トップページの実装](18-トップページの実装.md) | [目次に戻る](00-目次.md) | [次の章へ：第20章 投稿詳細ページの実装 →](20-投稿詳細ページの実装.md)

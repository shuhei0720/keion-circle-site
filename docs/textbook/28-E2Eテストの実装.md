# 第28章：E2Eテストの実装

この章では、**Playwright**を使った**E2Eテスト（End-to-End Test）**の実装を学びます。実際のブラウザで動作確認を行い、ユーザーの操作フローを自動テストします。

## 28.1 E2Eテストとは

### E2Eテストの位置づけ

```
テストの範囲:
単体テスト    → 関数・コンポーネント（第26章）
結合テスト    → API・データベース（第27章）
E2Eテスト     → アプリケーション全体（この章）← ユーザー視点

E2Eテストが検証するもの:
✅ ログイン → 投稿作成 → いいね → コメント
✅ 実際のブラウザでの動作
✅ JavaScript の実行
✅ CSS の表示
✅ ユーザー体験全体
```

### Playwrightの特徴

```
Playwright = Microsoft開発のE2Eテストツール

主な特徴:
✅ 複数ブラウザ対応（Chromium、Firefox、WebKit）
✅ 高速で信頼性が高い
✅ 自動待機機能
✅ スクリーンショット・動画録画
✅ モバイルエミュレーション
✅ TypeScript完全サポート
```

---

## 28.2 Playwrightのセットアップ

### インストール

```bash
# Playwright をインストール
npm install -D @playwright/test

# ブラウザをインストール
npx playwright install

# または、すべてを一度に
npm init playwright@latest
```

### 設定ファイルの作成

**playwright.config.ts を作成:**

```typescript
// playwright.config.ts
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  // テストディレクトリ
  testDir: './tests/e2e',

  // タイムアウト設定
  timeout: 30000,
  expect: {
    timeout: 5000,
  },

  // 並列実行
  fullyParallel: true,
  workers: process.env.CI ? 1 : undefined,

  // 失敗時のリトライ
  retries: process.env.CI ? 2 : 0,

  // レポート設定
  reporter: [
    ['html'],
    ['list'],
    ['json', { outputFile: 'test-results/results.json' }],
  ],

  // 共通設定
  use: {
    // ベースURL
    baseURL: 'http://localhost:3000',

    // トレース（デバッグ用）
    trace: 'on-first-retry',

    // スクリーンショット
    screenshot: 'only-on-failure',

    // 動画
    video: 'retain-on-failure',
  },

  // プロジェクト設定（ブラウザ別）
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
    {
      name: 'firefox',
      use: { ...devices['Desktop Firefox'] },
    },
    {
      name: 'webkit',
      use: { ...devices['Desktop Safari'] },
    },
    // モバイル
    {
      name: 'Mobile Chrome',
      use: { ...devices['Pixel 5'] },
    },
    {
      name: 'Mobile Safari',
      use: { ...devices['iPhone 12'] },
    },
  ],

  // ローカルサーバーの起動
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
    timeout: 120000,
  },
});
```

### package.json にスクリプト追加

```json
{
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:debug": "playwright test --debug",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:report": "playwright show-report"
  }
}
```

---

## 28.3 基本的なテストの作成

### ホームページのテスト

**tests/e2e/home.spec.ts を作成:**

```typescript
// tests/e2e/home.spec.ts
import { test, expect } from '@playwright/test';

test.describe('ホームページ', () => {
  test('タイトルが表示される', async ({ page }) => {
    await page.goto('/');
    
    await expect(page).toHaveTitle(/BOLD 軽音/);
  });

  test('ナビゲーションバーが表示される', async ({ page }) => {
    await page.goto('/');
    
    // ロゴが表示される
    await expect(page.getByText('🎸 BOLD 軽音')).toBeVisible();
    
    // メニュー項目が表示される
    await expect(page.getByRole('link', { name: 'ホーム' })).toBeVisible();
    await expect(page.getByRole('link', { name: '投稿' })).toBeVisible();
    await expect(page.getByRole('link', { name: 'イベント' })).toBeVisible();
  });

  test('フッターが表示される', async ({ page }) => {
    await page.goto('/');
    
    await expect(page.getByText('© 2025 BOLD 軽音')).toBeVisible();
  });

  test('レスポンシブメニューが動作する', async ({ page }) => {
    await page.setViewportSize({ width: 375, height: 667 }); // iPhone SE
    await page.goto('/');
    
    // ハンバーガーメニューをクリック
    await page.getByRole('button', { name: 'メニュー' }).click();
    
    // モバイルメニューが表示される
    await expect(page.getByRole('link', { name: 'ホーム' })).toBeVisible();
  });
});
```

### 投稿一覧ページのテスト

**tests/e2e/posts.spec.ts を作成:**

```typescript
// tests/e2e/posts.spec.ts
import { test, expect } from '@playwright/test';

test.describe('投稿一覧ページ', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/posts');
  });

  test('投稿一覧が表示される', async ({ page }) => {
    // 投稿タイトルが表示される
    await expect(page.getByRole('heading', { level: 2 })).toBeVisible();
    
    // 投稿カードが表示される
    const postCards = page.locator('[data-testid="post-card"]');
    await expect(postCards).toHaveCount(await postCards.count());
  });

  test('投稿をクリックすると詳細ページに遷移', async ({ page }) => {
    // 最初の投稿をクリック
    await page.locator('[data-testid="post-card"]').first().click();
    
    // 詳細ページに遷移
    await expect(page).toHaveURL(/\/posts\/.+/);
    
    // 詳細ページの要素が表示される
    await expect(page.getByRole('heading', { level: 1 })).toBeVisible();
  });

  test('検索機能が動作する', async ({ page }) => {
    // 検索ボックスに入力
    await page.getByPlaceholder('投稿を検索').fill('テスト');
    
    // 検索結果が絞り込まれる
    const results = page.locator('[data-testid="post-card"]');
    const count = await results.count();
    
    // 検索語を含む投稿のみ表示される
    for (let i = 0; i < count; i++) {
      const text = await results.nth(i).textContent();
      expect(text?.toLowerCase()).toContain('テスト');
    }
  });
});
```

---

## 28.4 認証フローのテスト

### ログインテスト

**tests/e2e/auth.spec.ts を作成:**

```typescript
// tests/e2e/auth.spec.ts
import { test, expect } from '@playwright/test';

test.describe('認証', () => {
  test('ログインページが表示される', async ({ page }) => {
    await page.goto('/login');
    
    await expect(page.getByRole('heading', { name: 'ログイン' })).toBeVisible();
    await expect(page.getByLabel('メールアドレス')).toBeVisible();
    await expect(page.getByLabel('パスワード')).toBeVisible();
    await expect(page.getByRole('button', { name: 'ログイン' })).toBeVisible();
  });

  test('空のフォームで送信するとエラーが表示される', async ({ page }) => {
    await page.goto('/login');
    
    await page.getByRole('button', { name: 'ログイン' }).click();
    
    await expect(page.getByText('メールアドレスとパスワードを入力してください')).toBeVisible();
  });

  test('正しい認証情報でログインできる', async ({ page }) => {
    await page.goto('/login');
    
    // 認証情報を入力
    await page.getByLabel('メールアドレス').fill('admin@test.com');
    await page.getByLabel('パスワード').fill('admin123');
    
    // ログインボタンをクリック
    await page.getByRole('button', { name: 'ログイン' }).click();
    
    // ホームページにリダイレクト
    await expect(page).toHaveURL('/');
    
    // ログイン状態の確認（ユーザー名が表示される）
    await expect(page.getByText('Admin User')).toBeVisible();
  });

  test('間違った認証情報でログインできない', async ({ page }) => {
    await page.goto('/login');
    
    await page.getByLabel('メールアドレス').fill('admin@test.com');
    await page.getByLabel('パスワード').fill('wrongpassword');
    
    await page.getByRole('button', { name: 'ログイン' }).click();
    
    // エラーメッセージが表示される
    await expect(page.getByText('ログインに失敗しました')).toBeVisible();
  });

  test('ログアウトできる', async ({ page }) => {
    // 先にログイン
    await page.goto('/login');
    await page.getByLabel('メールアドレス').fill('admin@test.com');
    await page.getByLabel('パスワード').fill('admin123');
    await page.getByRole('button', { name: 'ログイン' }).click();
    await page.waitForURL('/');
    
    // ログアウト
    await page.getByRole('button', { name: 'ログアウト' }).click();
    
    // ログインページにリダイレクト
    await expect(page).toHaveURL('/login');
  });
});
```

### 認証状態の保持

**tests/helpers/auth.ts を作成:**

```typescript
// tests/helpers/auth.ts
import { Page } from '@playwright/test';

export async function login(
  page: Page,
  email: string = 'admin@test.com',
  password: string = 'admin123'
) {
  await page.goto('/login');
  await page.getByLabel('メールアドレス').fill(email);
  await page.getByLabel('パスワード').fill(password);
  await page.getByRole('button', { name: 'ログイン' }).click();
  await page.waitForURL('/');
}

export async function loginAsAdmin(page: Page) {
  await login(page, 'admin@test.com', 'admin123');
}

export async function loginAsMember(page: Page) {
  await login(page, 'member@test.com', 'member123');
}
```

**認証状態を使ったテスト:**

```typescript
// tests/e2e/posts-create.spec.ts
import { test, expect } from '@playwright/test';
import { loginAsAdmin } from '../helpers/auth';

test.describe('投稿作成（管理者）', () => {
  test.beforeEach(async ({ page }) => {
    await loginAsAdmin(page);
    await page.goto('/posts/new');
  });

  test('投稿作成フォームが表示される', async ({ page }) => {
    await expect(page.getByRole('heading', { name: '投稿を作成' })).toBeVisible();
    await expect(page.getByLabel('タイトル')).toBeVisible();
    await expect(page.getByLabel('内容')).toBeVisible();
  });

  test('投稿を作成できる', async ({ page }) => {
    // フォームに入力
    await page.getByLabel('タイトル').fill('テスト投稿');
    await page.getByLabel('内容').fill('これはテストです');
    
    // YouTube URL を追加
    await page.getByPlaceholder('YouTube URL').fill('https://youtube.com/watch?v=abc123');
    
    // 送信
    await page.getByRole('button', { name: '投稿' }).click();
    
    // 投稿一覧にリダイレクト
    await expect(page).toHaveURL('/posts');
    
    // 作成した投稿が表示される
    await expect(page.getByText('テスト投稿')).toBeVisible();
  });

  test('タイトルが空の場合はエラー', async ({ page }) => {
    await page.getByLabel('内容').fill('内容のみ');
    await page.getByRole('button', { name: '投稿' }).click();
    
    await expect(page.getByText('タイトルを入力してください')).toBeVisible();
  });
});
```

---

## 28.5 いいね機能のテスト

**tests/e2e/likes.spec.ts を作成:**

```typescript
// tests/e2e/likes.spec.ts
import { test, expect } from '@playwright/test';
import { loginAsMember } from '../helpers/auth';

test.describe('いいね機能', () => {
  test.beforeEach(async ({ page }) => {
    await loginAsMember(page);
    await page.goto('/posts');
  });

  test('いいねボタンをクリックするといいね数が増える', async ({ page }) => {
    // 最初の投稿を開く
    await page.locator('[data-testid="post-card"]').first().click();
    
    // 現在のいいね数を取得
    const likeCountBefore = await page.getByTestId('like-count').textContent();
    const countBefore = parseInt(likeCountBefore || '0', 10);
    
    // いいねボタンをクリック
    await page.getByRole('button', { name: /いいね/ }).click();
    
    // いいね数が増える（楽観的UI）
    await expect(page.getByTestId('like-count')).toHaveText(
      (countBefore + 1).toString()
    );
    
    // ボタンの表示が変わる
    await expect(page.getByRole('button', { name: /いいね済み/ })).toBeVisible();
  });

  test('いいね済みの投稿をもう一度クリックするといいねが解除される', async ({ page }) => {
    await page.locator('[data-testid="post-card"]').first().click();
    
    // いいねする
    await page.getByRole('button', { name: /いいね/ }).click();
    await page.waitForTimeout(500);
    
    // いいね数を確認
    const likeCountAfterLike = await page.getByTestId('like-count').textContent();
    const countAfterLike = parseInt(likeCountAfterLike || '0', 10);
    
    // もう一度クリック（いいね解除）
    await page.getByRole('button', { name: /いいね済み/ }).click();
    
    // いいね数が減る
    await expect(page.getByTestId('like-count')).toHaveText(
      (countAfterLike - 1).toString()
    );
  });

  test('未ログインではいいねできない', async ({ page }) => {
    // ログアウト
    await page.getByRole('button', { name: 'ログアウト' }).click();
    await page.waitForURL('/login');
    
    // 投稿ページに直接アクセス
    await page.goto('/posts');
    await page.locator('[data-testid="post-card"]').first().click();
    
    // いいねボタンをクリック
    await page.getByRole('button', { name: /いいね/ }).click();
    
    // ログインページにリダイレクト
    await expect(page).toHaveURL('/login');
  });
});
```

---

## 28.6 コメント機能のテスト

**tests/e2e/comments.spec.ts を作成:**

```typescript
// tests/e2e/comments.spec.ts
import { test, expect } from '@playwright/test';
import { loginAsMember } from '../helpers/auth';

test.describe('コメント機能', () => {
  test.beforeEach(async ({ page }) => {
    await loginAsMember(page);
    await page.goto('/posts');
    await page.locator('[data-testid="post-card"]').first().click();
  });

  test('コメントフォームが表示される', async ({ page }) => {
    await expect(page.getByPlaceholder('コメントを入力')).toBeVisible();
    await expect(page.getByRole('button', { name: 'コメント' })).toBeVisible();
  });

  test('コメントを投稿できる', async ({ page }) => {
    const commentText = 'これはテストコメントです';
    
    // コメントを入力
    await page.getByPlaceholder('コメントを入力').fill(commentText);
    
    // 送信
    await page.getByRole('button', { name: 'コメント' }).click();
    
    // コメントが表示される
    await expect(page.getByText(commentText)).toBeVisible();
    
    // フォームがクリアされる
    await expect(page.getByPlaceholder('コメントを入力')).toHaveValue('');
  });

  test('空のコメントは送信できない', async ({ page }) => {
    await page.getByRole('button', { name: 'コメント' }).click();
    
    // エラーメッセージが表示される
    await expect(page.getByText('コメントを入力してください')).toBeVisible();
  });

  test('コメント数が表示される', async ({ page }) => {
    // コメント数を取得
    const commentCountBefore = await page.getByTestId('comment-count').textContent();
    const countBefore = parseInt(commentCountBefore || '0', 10);
    
    // コメントを投稿
    await page.getByPlaceholder('コメントを入力').fill('新しいコメント');
    await page.getByRole('button', { name: 'コメント' }).click();
    
    // コメント数が増える
    await expect(page.getByTestId('comment-count')).toHaveText(
      (countBefore + 1).toString()
    );
  });

  test('自分のコメントは削除できる', async ({ page }) => {
    const commentText = '削除するコメント';
    
    // コメントを投稿
    await page.getByPlaceholder('コメントを入力').fill(commentText);
    await page.getByRole('button', { name: 'コメント' }).click();
    await page.waitForTimeout(500);
    
    // コメントが表示される
    await expect(page.getByText(commentText)).toBeVisible();
    
    // 削除ボタンをクリック
    const comment = page.locator(`text=${commentText}`).locator('..');
    await comment.getByRole('button', { name: '削除' }).click();
    
    // 確認ダイアログが表示される
    page.on('dialog', dialog => dialog.accept());
    
    // コメントが削除される
    await expect(page.getByText(commentText)).not.toBeVisible();
  });
});
```

---

## 28.7 イベント機能のテスト

**tests/e2e/events.spec.ts を作成:**

```typescript
// tests/e2e/events.spec.ts
import { test, expect } from '@playwright/test';
import { loginAsAdmin, loginAsMember } from '../helpers/auth';

test.describe('イベント管理（管理者）', () => {
  test.beforeEach(async ({ page }) => {
    await loginAsAdmin(page);
  });

  test('イベントを作成できる', async ({ page }) => {
    await page.goto('/events/new');
    
    // フォームに入力
    await page.getByLabel('イベント名').fill('新年ライブ');
    await page.getByLabel('開催日時').fill('2025-01-15T18:00');
    await page.getByLabel('場所').fill('市民ホール');
    await page.getByLabel('内容').fill('新年最初のライブイベント');
    
    // 課題曲を追加
    await page.getByRole('button', { name: '課題曲を追加' }).click();
    await page.getByLabel('曲名').fill('春よ来い');
    await page.getByLabel('YouTube URL').fill('https://youtube.com/watch?v=abc');
    
    // 送信
    await page.getByRole('button', { name: '作成' }).click();
    
    // イベント一覧にリダイレクト
    await expect(page).toHaveURL('/events');
    
    // 作成したイベントが表示される
    await expect(page.getByText('新年ライブ')).toBeVisible();
  });

  test('イベントを編集できる', async ({ page }) => {
    await page.goto('/events');
    
    // 最初のイベントを選択
    await page.locator('[data-testid="event-card"]').first().click();
    
    // 編集ボタンをクリック
    await page.getByRole('button', { name: '編集' }).click();
    
    // イベント名を変更
    await page.getByLabel('イベント名').clear();
    await page.getByLabel('イベント名').fill('更新されたイベント');
    
    // 保存
    await page.getByRole('button', { name: '保存' }).click();
    
    // 変更が反映される
    await expect(page.getByText('更新されたイベント')).toBeVisible();
  });

  test('イベントを削除できる', async ({ page }) => {
    await page.goto('/events');
    
    // 最初のイベントを選択
    await page.locator('[data-testid="event-card"]').first().click();
    
    // イベント名を記録
    const eventName = await page.getByRole('heading', { level: 1 }).textContent();
    
    // 削除ボタンをクリック
    await page.getByRole('button', { name: '削除' }).click();
    
    // 確認ダイアログ
    page.on('dialog', dialog => {
      expect(dialog.message()).toContain('削除しますか');
      dialog.accept();
    });
    
    // イベント一覧にリダイレクト
    await expect(page).toHaveURL('/events');
    
    // 削除されたイベントが表示されない
    await expect(page.getByText(eventName!)).not.toBeVisible();
  });
});

test.describe('イベント参加（一般ユーザー）', () => {
  test.beforeEach(async ({ page }) => {
    await loginAsMember(page);
  });

  test('イベントに参加登録できる', async ({ page }) => {
    await page.goto('/events');
    await page.locator('[data-testid="event-card"]').first().click();
    
    // 参加ボタンをクリック
    await page.getByRole('button', { name: '参加する' }).click();
    
    // ボタンの表示が変わる
    await expect(page.getByRole('button', { name: '参加中' })).toBeVisible();
    
    // 参加者リストに自分の名前が表示される
    await expect(page.getByText('Member User')).toBeVisible();
  });

  test('参加をキャンセルできる', async ({ page }) => {
    await page.goto('/events');
    await page.locator('[data-testid="event-card"]').first().click();
    
    // 参加する
    await page.getByRole('button', { name: '参加する' }).click();
    await page.waitForTimeout(500);
    
    // キャンセル
    await page.getByRole('button', { name: '参加中' }).click();
    
    // ボタンが元に戻る
    await expect(page.getByRole('button', { name: '参加する' })).toBeVisible();
  });
});
```

---

## 28.8 スクリーンショットとビデオ

### 自動スクリーンショット

```typescript
test('失敗時にスクリーンショットを撮る', async ({ page }) => {
  await page.goto('/');
  
  // わざと失敗させる
  await expect(page.getByText('存在しない要素')).toBeVisible();
  
  // → 失敗時に自動でスクリーンショット撮影
});
```

**手動でスクリーンショット:**

```typescript
test('投稿作成画面のスクリーンショット', async ({ page }) => {
  await loginAsAdmin(page);
  await page.goto('/posts/new');
  
  // スクリーンショットを撮影
  await page.screenshot({ 
    path: 'screenshots/post-create.png',
    fullPage: true 
  });
});
```

### ビデオ録画

```typescript
// playwright.config.ts で設定
use: {
  video: 'on', // すべてのテストで録画
  // または
  video: 'retain-on-failure', // 失敗時のみ保存
}
```

**ビデオはtest-results/ディレクトリに保存されます。**

---

## 28.9 モバイルエミュレーション

### モバイルデバイスでのテスト

```typescript
// tests/e2e/mobile.spec.ts
import { test, expect, devices } from '@playwright/test';

test.use(devices['iPhone 12']);

test.describe('モバイル表示', () => {
  test('レスポンシブメニューが表示される', async ({ page }) => {
    await page.goto('/');
    
    // デスクトップメニューは非表示
    await expect(page.getByTestId('desktop-menu')).not.toBeVisible();
    
    // ハンバーガーメニューが表示される
    await expect(page.getByTestId('mobile-menu-button')).toBeVisible();
    
    // メニューを開く
    await page.getByTestId('mobile-menu-button').click();
    
    // モバイルメニューが表示される
    await expect(page.getByTestId('mobile-menu')).toBeVisible();
  });

  test('投稿カードがモバイル最適化されている', async ({ page }) => {
    await page.goto('/posts');
    
    // カードが1カラム表示
    const postCards = page.locator('[data-testid="post-card"]');
    const firstCard = postCards.first();
    
    // カードの幅がビューポート幅に近い
    const boundingBox = await firstCard.boundingBox();
    const viewportSize = page.viewportSize();
    
    expect(boundingBox!.width).toBeGreaterThan(viewportSize!.width * 0.8);
  });
});
```

---

## 28.10 パフォーマンステスト

### ページ読み込み時間の測定

```typescript
// tests/e2e/performance.spec.ts
import { test, expect } from '@playwright/test';

test.describe('パフォーマンス', () => {
  test('ホームページが2秒以内に読み込まれる', async ({ page }) => {
    const startTime = Date.now();
    
    await page.goto('/');
    await page.waitForLoadState('networkidle');
    
    const loadTime = Date.now() - startTime;
    
    expect(loadTime).toBeLessThan(2000);
  });

  test('投稿一覧が3秒以内に読み込まれる', async ({ page }) => {
    const startTime = Date.now();
    
    await page.goto('/posts');
    await page.waitForSelector('[data-testid="post-card"]');
    
    const loadTime = Date.now() - startTime;
    
    expect(loadTime).toBeLessThan(3000);
  });

  test('画像の遅延読み込みが機能する', async ({ page }) => {
    await page.goto('/posts');
    
    // 最初の画像が読み込まれる
    const firstImage = page.locator('img').first();
    await expect(firstImage).toHaveAttribute('loading', 'lazy');
    
    // スクロールして画像を表示
    await firstImage.scrollIntoViewIfNeeded();
    
    // 画像が読み込まれる
    await expect(firstImage).toHaveAttribute('src', /.+/);
  });
});
```

---

## 28.11 CI/CDでのE2Eテスト

### GitHub Actions でテスト実行

**.github/workflows/e2e.yml を作成:**

```yaml
name: E2E Tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Setup database
        run: |
          export DATABASE_URL="file:./test.db"
          npx prisma generate
          npx prisma db push

      - name: Create test admin user
        run: |
          export DATABASE_URL="file:./test.db"
          node scripts/create-admin.js admin@test.com admin123 "Admin User"

      - name: Run E2E tests
        env:
          DATABASE_URL: "file:./test.db"
          AUTH_URL: "http://localhost:3000"
          AUTH_SECRET: "test-secret-key-for-ci"
          NEXT_PUBLIC_SUPABASE_URL: "https://test.supabase.co"
          NEXT_PUBLIC_SUPABASE_ANON_KEY: "test-anon-key"
        run: npm run test:e2e

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/

      - name: Upload videos
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-videos
          path: test-results/
```

---

## まとめ

この章で学んだこと：

✅ **Playwright のセットアップ** - 設定、ブラウザインストール
✅ **基本的なテスト** - ページ表示、ナビゲーション
✅ **認証フローのテスト** - ログイン、ログアウト、権限
✅ **いいね・コメント機能** - 楽観的UI、リアルタイム更新
✅ **イベント管理** - CRUD操作、参加登録
✅ **スクリーンショット・ビデオ** - デバッグ、証拠保存
✅ **モバイルエミュレーション** - レスポンシブテスト
✅ **パフォーマンステスト** - 読み込み時間測定
✅ **CI/CD統合** - GitHub Actions で自動実行

**次の章では、CI/CD の詳細な実装を学びます。**

---

[← 前の章：第27章 結合テストの実装](27-結合テストの実装.md) | [目次に戻る](00-目次.md) | [次の章へ：第29章 CI/CDの実装 →](29-CI-CDの実装.md)

# 第18章：トップページの実装

この章では、サイトのトップページを実装していきます。最新の投稿、今後のイベント、メンバー紹介などを表示する魅力的なランディングページを作成します。

## 18.1 ページ構成

### トップページの要素

```
┌─────────────────────────────────────┐
│         ヘッダー（ナビゲーション）        │
├─────────────────────────────────────┤
│         ヒーローセクション              │
│    （サイトタイトル、キャッチコピー）      │
├─────────────────────────────────────┤
│         最新の活動報告                 │
│    （投稿カード × 3件）                │
├─────────────────────────────────────┤
│         今後のイベント                 │
│    （イベントカード × 3件）             │
├─────────────────────────────────────┤
│         メンバー紹介                   │
│    （メンバーアバター一覧）             │
├─────────────────────────────────────┤
│         フッター                       │
└─────────────────────────────────────┘
```

---

## 18.2 トップページの実装

### app/page.tsx

```typescript
import Link from 'next/link';
import { prisma } from '@/lib/db';
import PostCard from '@/components/PostCard';
import EventCard from '@/components/EventCard';
import MemberGrid from '@/components/MemberGrid';
import { Music, Calendar, Users } from 'lucide-react';

async function getRecentPosts() {
  return prisma.post.findMany({
    take: 3,
    orderBy: { createdAt: 'desc' },
    include: {
      author: {
        select: {
          id: true,
          name: true,
          image: true,
        },
      },
      _count: {
        select: {
          likes: true,
          comments: true,
        },
      },
    },
  });
}

async function getUpcomingEvents() {
  return prisma.event.findMany({
    where: {
      date: {
        gte: new Date(),
      },
    },
    take: 3,
    orderBy: { date: 'asc' },
    include: {
      _count: {
        select: {
          participation: true,
        },
      },
    },
  });
}

async function getMembers() {
  return prisma.user.findMany({
    take: 12,
    orderBy: { createdAt: 'desc' },
    select: {
      id: true,
      name: true,
      image: true,
      instrument: true,
      role: true,
    },
  });
}

export default async function HomePage() {
  // データを並行して取得
  const [posts, events, members] = await Promise.all([
    getRecentPosts(),
    getUpcomingEvents(),
    getMembers(),
  ]);
  
  return (
    <div>
      {/* ヒーローセクション */}
      <section className="relative bg-gradient-to-br from-blue-600 to-purple-700 text-white py-20 px-4">
        <div className="max-w-6xl mx-auto text-center">
          <h1 className="text-5xl md:text-6xl font-bold mb-6">
            🎸 BOLD 軽音
          </h1>
          
          <p className="text-xl md:text-2xl mb-8 opacity-90">
            音楽を通じて繋がる、学生軽音楽サークル
          </p>
          
          <div className="flex flex-wrap justify-center gap-4">
            <Link
              href="/posts"
              className="inline-flex items-center gap-2 px-6 py-3 bg-white text-blue-600 rounded-lg font-medium hover:bg-gray-100 transition-colors"
            >
              <Music className="w-5 h-5" />
              活動報告を見る
            </Link>
            
            <Link
              href="/events"
              className="inline-flex items-center gap-2 px-6 py-3 bg-transparent border-2 border-white text-white rounded-lg font-medium hover:bg-white hover:text-blue-600 transition-colors"
            >
              <Calendar className="w-5 h-5" />
              イベント一覧
            </Link>
          </div>
        </div>
        
        {/* 装飾的な波形 */}
        <div className="absolute bottom-0 left-0 right-0">
          <svg
            className="w-full h-16"
            viewBox="0 0 1200 120"
            preserveAspectRatio="none"
          >
            <path
              d="M321.39,56.44c58-10.79,114.16-30.13,172-41.86,82.39-16.72,168.19-17.73,250.45-.39C823.78,31,906.67,72,985.66,92.83c70.05,18.48,146.53,26.09,214.34,3V0H0V27.35A600.21,600.21,0,0,0,321.39,56.44Z"
              fill="#ffffff"
            />
          </svg>
        </div>
      </section>
      
      {/* 最新の活動報告 */}
      <section className="max-w-6xl mx-auto px-4 py-16">
        <div className="flex justify-between items-center mb-8">
          <h2 className="text-3xl font-bold">📝 最新の活動報告</h2>
          <Link
            href="/posts"
            className="text-blue-600 hover:underline font-medium"
          >
            すべて見る →
          </Link>
        </div>
        
        {posts.length === 0 ? (
          <p className="text-gray-600 text-center py-12">
            まだ投稿がありません
          </p>
        ) : (
          <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
            {posts.map(post => (
              <PostCard key={post.id} post={post} />
            ))}
          </div>
        )}
      </section>
      
      {/* 今後のイベント */}
      <section className="bg-gray-50 py-16">
        <div className="max-w-6xl mx-auto px-4">
          <div className="flex justify-between items-center mb-8">
            <h2 className="text-3xl font-bold">🎉 今後のイベント</h2>
            <Link
              href="/events"
              className="text-blue-600 hover:underline font-medium"
            >
              すべて見る →
            </Link>
          </div>
          
          {events.length === 0 ? (
            <p className="text-gray-600 text-center py-12">
              予定されているイベントはありません
            </p>
          ) : (
            <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
              {events.map(event => (
                <EventCard key={event.id} event={event} />
              ))}
            </div>
          )}
        </div>
      </section>
      
      {/* メンバー紹介 */}
      <section className="max-w-6xl mx-auto px-4 py-16">
        <div className="flex justify-between items-center mb-8">
          <h2 className="text-3xl font-bold">👥 メンバー</h2>
          <Link
            href="/users"
            className="text-blue-600 hover:underline font-medium"
          >
            すべて見る →
          </Link>
        </div>
        
        <MemberGrid members={members} />
      </section>
      
      {/* CTA（Call to Action） */}
      <section className="bg-gradient-to-r from-blue-600 to-purple-700 text-white py-16">
        <div className="max-w-4xl mx-auto px-4 text-center">
          <h2 className="text-3xl font-bold mb-4">
            一緒に音楽を楽しみませんか？
          </h2>
          
          <p className="text-lg mb-8 opacity-90">
            BOLD 軽音では新メンバーを募集中！<br />
            楽器初心者も大歓迎です。
          </p>
          
          <Link
            href="/register"
            className="inline-flex items-center gap-2 px-8 py-4 bg-white text-blue-600 rounded-lg font-medium hover:bg-gray-100 transition-colors"
          >
            <Users className="w-5 h-5" />
            メンバー登録
          </Link>
        </div>
      </section>
    </div>
  );
}
```

---

## 18.3 投稿カードコンポーネント

### components/PostCard.tsx

```typescript
import Link from 'next/link';
import { formatDistanceToNow } from 'date-fns';
import { ja } from 'date-fns/locale';
import { Heart, MessageCircle, Calendar } from 'lucide-react';
import Image from 'next/image';

interface PostCardProps {
  post: {
    id: string;
    title: string;
    content: string;
    images: string[];
    createdAt: Date;
    author: {
      id: string;
      name: string | null;
      image: string | null;
    };
    _count: {
      likes: number;
      comments: number;
    };
  };
}

export default function PostCard({ post }: PostCardProps) {
  return (
    <Link
      href={`/posts/${post.id}`}
      className="block border rounded-lg overflow-hidden hover:shadow-lg transition-shadow bg-white"
    >
      {/* サムネイル画像 */}
      {post.images.length > 0 && (
        <div className="relative w-full h-48 bg-gray-200">
          <Image
            src={post.images[0]}
            alt={post.title}
            fill
            className="object-cover"
          />
        </div>
      )}
      
      {/* コンテンツ */}
      <div className="p-4">
        {/* タイトル */}
        <h3 className="text-xl font-bold mb-2 line-clamp-2">
          {post.title}
        </h3>
        
        {/* 本文プレビュー */}
        <p className="text-gray-600 text-sm mb-4 line-clamp-3">
          {post.content}
        </p>
        
        {/* 著者情報 */}
        <div className="flex items-center gap-2 mb-3">
          <div className="relative w-8 h-8 rounded-full overflow-hidden bg-gray-200">
            {post.author.image ? (
              <Image
                src={post.author.image}
                alt={post.author.name || ''}
                fill
                className="object-cover"
              />
            ) : (
              <div className="w-full h-full flex items-center justify-center text-gray-400 text-xs">
                {post.author.name?.[0] || '?'}
              </div>
            )}
          </div>
          
          <div className="flex-1 min-w-0">
            <p className="text-sm font-medium truncate">
              {post.author.name}
            </p>
            <p className="text-xs text-gray-500">
              {formatDistanceToNow(new Date(post.createdAt), {
                addSuffix: true,
                locale: ja,
              })}
            </p>
          </div>
        </div>
        
        {/* 統計情報 */}
        <div className="flex items-center gap-4 text-sm text-gray-600">
          <span className="flex items-center gap-1">
            <Heart className="w-4 h-4" />
            {post._count.likes}
          </span>
          
          <span className="flex items-center gap-1">
            <MessageCircle className="w-4 h-4" />
            {post._count.comments}
          </span>
        </div>
      </div>
    </Link>
  );
}
```

---

## 18.4 イベントカードコンポーネント

### components/EventCard.tsx

```typescript
import Link from 'next/link';
import { format } from 'date-fns';
import { ja } from 'date-fns/locale';
import { Calendar, MapPin, Users } from 'lucide-react';

interface EventCardProps {
  event: {
    id: string;
    title: string;
    description: string | null;
    date: Date;
    location: string | null;
    _count: {
      participation: number;
    };
  };
}

export default function EventCard({ event }: EventCardProps) {
  const isPast = new Date(event.date) < new Date();
  
  return (
    <Link
      href={`/events/${event.id}`}
      className="block border rounded-lg p-6 hover:shadow-lg transition-shadow bg-white"
    >
      {/* ステータスバッジ */}
      <div className="flex justify-between items-start mb-3">
        <h3 className="text-xl font-bold flex-1 mr-2">
          {event.title}
        </h3>
        
        {isPast && (
          <span className="px-2 py-1 bg-gray-200 text-gray-600 text-xs rounded">
            終了
          </span>
        )}
      </div>
      
      {/* 日時 */}
      <div className="flex items-start gap-2 text-gray-600 mb-2">
        <Calendar className="w-4 h-4 mt-0.5 flex-shrink-0" />
        <span className="text-sm">
          {format(new Date(event.date), 'yyyy年M月d日(E) HH:mm', { locale: ja })}
        </span>
      </div>
      
      {/* 場所 */}
      {event.location && (
        <div className="flex items-start gap-2 text-gray-600 mb-3">
          <MapPin className="w-4 h-4 mt-0.5 flex-shrink-0" />
          <span className="text-sm">{event.location}</span>
        </div>
      )}
      
      {/* 説明 */}
      {event.description && (
        <p className="text-gray-700 text-sm mb-4 line-clamp-2">
          {event.description}
        </p>
      )}
      
      {/* 参加者数 */}
      <div className="flex items-center gap-2 text-sm text-gray-600">
        <Users className="w-4 h-4" />
        <span>{event._count.participation}人が参加予定</span>
      </div>
    </Link>
  );
}
```

---

## 18.5 メンバーグリッドコンポーネント

### components/MemberGrid.tsx

```typescript
import Link from 'next/link';
import Image from 'next/image';
import { Crown } from 'lucide-react';

interface Member {
  id: string;
  name: string | null;
  image: string | null;
  instrument: string | null;
  role: string;
}

interface MemberGridProps {
  members: Member[];
}

export default function MemberGrid({ members }: MemberGridProps) {
  return (
    <div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6 gap-4">
      {members.map(member => (
        <Link
          key={member.id}
          href={`/users/${member.id}`}
          className="group"
        >
          <div className="relative">
            {/* アバター */}
            <div className="relative w-full aspect-square rounded-full overflow-hidden bg-gray-200 mb-2 group-hover:ring-4 group-hover:ring-blue-200 transition-all">
              {member.image ? (
                <Image
                  src={member.image}
                  alt={member.name || ''}
                  fill
                  className="object-cover"
                />
              ) : (
                <div className="w-full h-full flex items-center justify-center text-gray-400 text-2xl font-bold">
                  {member.name?.[0] || '?'}
                </div>
              )}
            </div>
            
            {/* 管理者バッジ */}
            {member.role === 'admin' && (
              <div className="absolute top-0 right-0 w-6 h-6 bg-yellow-400 rounded-full flex items-center justify-center">
                <Crown className="w-4 h-4 text-yellow-800" />
              </div>
            )}
          </div>
          
          {/* 名前 */}
          <p className="text-center font-medium text-sm truncate">
            {member.name}
          </p>
          
          {/* 楽器 */}
          {member.instrument && (
            <p className="text-center text-xs text-gray-600 truncate">
              {member.instrument}
            </p>
          )}
        </Link>
      ))}
    </div>
  );
}
```

---

## 18.6 ヘッダーコンポーネント

### components/Header.tsx

```typescript
'use client';

import { useState } from 'react';
import Link from 'next/link';
import { useSession, signOut } from 'next-auth/react';
import { Menu, X, Music, LogOut, User, Settings } from 'lucide-react';

export default function Header() {
  const { data: session } = useSession();
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);
  
  return (
    <header className="bg-white border-b sticky top-0 z-50">
      <nav className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-16">
          {/* ロゴ */}
          <Link href="/" className="flex items-center gap-2 font-bold text-xl">
            <Music className="w-6 h-6 text-blue-600" />
            <span>BOLD 軽音</span>
          </Link>
          
          {/* デスクトップメニュー */}
          <div className="hidden md:flex items-center gap-6">
            <Link href="/posts" className="hover:text-blue-600 transition-colors">
              活動報告
            </Link>
            <Link href="/events" className="hover:text-blue-600 transition-colors">
              イベント
            </Link>
            <Link href="/schedules" className="hover:text-blue-600 transition-colors">
              スケジュール
            </Link>
            <Link href="/users" className="hover:text-blue-600 transition-colors">
              メンバー
            </Link>
            
            {session ? (
              <div className="flex items-center gap-4">
                <Link
                  href={`/users/${session.user.id}`}
                  className="flex items-center gap-2 hover:text-blue-600 transition-colors"
                >
                  <User className="w-4 h-4" />
                  マイページ
                </Link>
                
                {session.user.role === 'admin' && (
                  <Link
                    href="/admin"
                    className="flex items-center gap-2 text-yellow-600 hover:text-yellow-700 transition-colors"
                  >
                    <Settings className="w-4 h-4" />
                    管理
                  </Link>
                )}
                
                <button
                  onClick={() => signOut()}
                  className="flex items-center gap-2 text-red-600 hover:text-red-700 transition-colors"
                >
                  <LogOut className="w-4 h-4" />
                  ログアウト
                </button>
              </div>
            ) : (
              <Link
                href="/login"
                className="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors"
              >
                ログイン
              </Link>
            )}
          </div>
          
          {/* モバイルメニューボタン */}
          <button
            onClick={() => setMobileMenuOpen(!mobileMenuOpen)}
            className="md:hidden p-2"
          >
            {mobileMenuOpen ? (
              <X className="w-6 h-6" />
            ) : (
              <Menu className="w-6 h-6" />
            )}
          </button>
        </div>
        
        {/* モバイルメニュー */}
        {mobileMenuOpen && (
          <div className="md:hidden py-4 border-t">
            <div className="flex flex-col gap-4">
              <Link
                href="/posts"
                className="hover:text-blue-600 transition-colors"
                onClick={() => setMobileMenuOpen(false)}
              >
                活動報告
              </Link>
              <Link
                href="/events"
                className="hover:text-blue-600 transition-colors"
                onClick={() => setMobileMenuOpen(false)}
              >
                イベント
              </Link>
              <Link
                href="/schedules"
                className="hover:text-blue-600 transition-colors"
                onClick={() => setMobileMenuOpen(false)}
              >
                スケジュール
              </Link>
              <Link
                href="/users"
                className="hover:text-blue-600 transition-colors"
                onClick={() => setMobileMenuOpen(false)}
              >
                メンバー
              </Link>
              
              {session ? (
                <>
                  <Link
                    href={`/users/${session.user.id}`}
                    className="hover:text-blue-600 transition-colors"
                    onClick={() => setMobileMenuOpen(false)}
                  >
                    マイページ
                  </Link>
                  
                  {session.user.role === 'admin' && (
                    <Link
                      href="/admin"
                      className="text-yellow-600 hover:text-yellow-700 transition-colors"
                      onClick={() => setMobileMenuOpen(false)}
                    >
                      管理
                    </Link>
                  )}
                  
                  <button
                    onClick={() => {
                      signOut();
                      setMobileMenuOpen(false);
                    }}
                    className="text-left text-red-600 hover:text-red-700 transition-colors"
                  >
                    ログアウト
                  </button>
                </>
              ) : (
                <Link
                  href="/login"
                  className="text-blue-600 hover:text-blue-700 transition-colors"
                  onClick={() => setMobileMenuOpen(false)}
                >
                  ログイン
                </Link>
              )}
            </div>
          </div>
        )}
      </nav>
    </header>
  );
}
```

---

## 18.7 フッターコンポーネント

### components/Footer.tsx

```typescript
import Link from 'next/link';
import { Music, Mail, Github } from 'lucide-react';

export default function Footer() {
  return (
    <footer className="bg-gray-900 text-white py-12">
      <div className="max-w-6xl mx-auto px-4">
        <div className="grid md:grid-cols-3 gap-8 mb-8">
          {/* サークル情報 */}
          <div>
            <div className="flex items-center gap-2 font-bold text-xl mb-4">
              <Music className="w-6 h-6" />
              <span>BOLD 軽音</span>
            </div>
            
            <p className="text-gray-400 text-sm">
              音楽を通じて繋がる、学生軽音楽サークルです。
              楽器初心者も大歓迎！
            </p>
          </div>
          
          {/* リンク */}
          <div>
            <h3 className="font-bold mb-4">リンク</h3>
            <ul className="space-y-2 text-sm">
              <li>
                <Link href="/posts" className="text-gray-400 hover:text-white transition-colors">
                  活動報告
                </Link>
              </li>
              <li>
                <Link href="/events" className="text-gray-400 hover:text-white transition-colors">
                  イベント
                </Link>
              </li>
              <li>
                <Link href="/schedules" className="text-gray-400 hover:text-white transition-colors">
                  スケジュール
                </Link>
              </li>
              <li>
                <Link href="/users" className="text-gray-400 hover:text-white transition-colors">
                  メンバー
                </Link>
              </li>
            </ul>
          </div>
          
          {/* お問い合わせ */}
          <div>
            <h3 className="font-bold mb-4">お問い合わせ</h3>
            <ul className="space-y-2 text-sm">
              <li className="flex items-center gap-2 text-gray-400">
                <Mail className="w-4 h-4" />
                <a href="mailto:info@bold-music.com" className="hover:text-white transition-colors">
                  info@bold-music.com
                </a>
              </li>
              <li className="flex items-center gap-2 text-gray-400">
                <Github className="w-4 h-4" />
                <a href="https://github.com" className="hover:text-white transition-colors" target="_blank" rel="noopener noreferrer">
                  GitHub
                </a>
              </li>
            </ul>
          </div>
        </div>
        
        {/* コピーライト */}
        <div className="border-t border-gray-800 pt-8 text-center text-sm text-gray-400">
          <p>&copy; 2025 BOLD 軽音. All rights reserved.</p>
        </div>
      </div>
    </footer>
  );
}
```

---

## 18.8 レイアウトの統合

### app/layout.tsx

```typescript
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import Header from '@/components/Header';
import Footer from '@/components/Footer';
import { SessionProvider } from '@/components/SessionProvider';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'BOLD 軽音 - メンバーサイト',
  description: '音楽を通じて繋がる、学生軽音楽サークル',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="ja">
      <body className={inter.className}>
        <SessionProvider>
          <div className="flex flex-col min-h-screen">
            <Header />
            
            <main className="flex-1">
              {children}
            </main>
            
            <Footer />
          </div>
        </SessionProvider>
      </body>
    </html>
  );
}
```

### components/SessionProvider.tsx

```typescript
'use client';

import { SessionProvider as NextAuthSessionProvider } from 'next-auth/react';

export function SessionProvider({ children }: { children: React.ReactNode }) {
  return <NextAuthSessionProvider>{children}</NextAuthSessionProvider>;
}
```

---

## まとめ

この章では、トップページの実装について学びました：

### ページ構成
- ✅ **ヒーローセクション**: サイトタイトルとキャッチコピー
- ✅ **最新の活動報告**: 投稿カード × 3件
- ✅ **今後のイベント**: イベントカード × 3件
- ✅ **メンバー紹介**: メンバーアバター一覧
- ✅ **CTA**: メンバー登録への誘導

### コンポーネント
- ✅ **PostCard**: 投稿カード（サムネイル、タイトル、統計）
- ✅ **EventCard**: イベントカード（日時、場所、参加者数）
- ✅ **MemberGrid**: メンバーグリッド（アバター、名前、楽器）
- ✅ **Header**: ヘッダー（ナビゲーション、ログイン状態）
- ✅ **Footer**: フッター（リンク、お問い合わせ）

### データ取得
- ✅ **並行取得**: Promise.allで複数のクエリを同時実行
- ✅ **Server Components**: サーバーサイドでデータを取得
- ✅ **Prisma include**: リレーションデータを効率的に取得

次の章では、**投稿ページの実装**について詳しく見ていきます。

---

[← 前の章：第17章 検索・フィルタリングAPIの実装](17-検索・フィルタリングAPIの実装.md) | [目次に戻る](00-目次.md) | [次の章へ：第19章 投稿ページの実装 →](19-投稿ページの実装.md)

# 第33章：ライブラリとユーティリティの詳細解説

> **この章では、プロジェクトの基盤となるライブラリとユーティリティの実装を詳細に解説します**

## 📚 この章の目的

この章は**リファレンス**として、プロジェクト全体で使われる共通ライブラリとユーティリティの仕組みを深く理解するためのものです。

- ✅ 認証システムの設定（NextAuth.js）
- ✅ データベース接続の管理（Prisma）
- ✅ ストレージ連携（Supabase）
- ✅ 権限管理の実装
- ✅ 型定義の設計

## 💡 lib ディレクトリの役割

```
┌──────────────────────────────────────────────────┐
│     src/lib/ ディレクトリの構成                   │
└──────────────────────────────────────────────────┘

src/lib/
├─ auth.ts           # NextAuth.js 認証設定
├─ prisma.ts         # Prisma Client インスタンス
├─ supabase.ts       # Supabase クライアント
└─ permissions.ts    # 権限チェック関数


【libディレクトリの目的】

  1. 再利用可能な機能をまとめる
     → プロジェクト全体で共通して使う機能

  2. 設定を一元管理
     → 変更が必要な時、1箇所だけ修正すればOK

  3. 依存関係を隠蔽
     → 外部ライブラリの使い方を隠す
     → インターフェースを統一


【使用例】

  // どこからでもインポートして使える
  import { auth } from '@/lib/auth'
  import { prisma } from '@/lib/prisma'
  import { isAdmin } from '@/lib/permissions'

  const session = await auth()
  const users = await prisma.user.findMany()
  const admin = await isAdmin()
```

---

## 33.1 認証設定（src/lib/auth.ts）

NextAuth.js v5 の設定ファイルです。プロジェクトの認証システムの心臓部です。

**実装コード:**

```typescript
import NextAuth from "next-auth"
import GoogleProvider from "next-auth/providers/google"
import CredentialsProvider from "next-auth/providers/credentials"
import { prisma } from "@/lib/prisma"
import bcrypt from "bcryptjs"

/**
 * NextAuth v5 認証設定
 * - Google OAuth: Google アカウントでログイン
 * - Credentials: メールアドレスとパスワードでログイン
 * - JWT Strategy: IDとroleのみ保存（Cookieサイズ最小化）
 */
export const { handlers, signIn, signOut, auth } = NextAuth({
  providers: [
    GoogleProvider({
      clientId: process.env.GOOGLE_CLIENT_ID!,
      clientSecret: process.env.GOOGLE_CLIENT_SECRET!,
      authorization: {
        params: {
          prompt: "consent",
          access_type: "offline",
          response_type: "code"
        }
      }
    }),
    CredentialsProvider({
      name: "Credentials",
      credentials: {
        email: { label: "Email", type: "email" },
        password: { label: "Password", type: "password" },
      },
      async authorize(credentials) {
        console.log("[NextAuth Credentials] Authorize called");
        console.log("[NextAuth Credentials] Email:", credentials?.email);

        try {
          // 入力チェック
          if (!credentials?.email || !credentials?.password) {
            console.error("[NextAuth Credentials] Missing credentials");
            throw new Error("メールアドレスとパスワードを入力してください");
          }

          // データベースからユーザーを取得
          const user = await prisma.user.findUnique({
            where: { email: credentials.email as string },
            select: { id: true, email: true, password: true, role: true },
          });
          console.log("[NextAuth Credentials] User found:", !!user);

          // ユーザーが存在しない、またはパスワードが設定されていない
          if (!user || !user.password) {
            console.error(
              "[NextAuth Credentials] User not found or no password"
            );
            throw new Error("ユーザーが見つかりません");
          }

          // パスワード検証（bcryptでハッシュを比較）
          const isPasswordValid = await bcrypt.compare(
            credentials.password as string,
            user.password
          );
          console.log(
            "[NextAuth Credentials] Password valid:",
            isPasswordValid
          );

          if (!isPasswordValid) {
            console.error("[NextAuth Credentials] Invalid password");
            throw new Error("パスワードが正しくありません");
          }

          // 認証成功: ユーザー情報を返す
          const result = {
            id: user.id,
            email: user.email,
          };
          console.log(
            "[NextAuth Credentials] Success, returning:",
            JSON.stringify(result, null, 2)
          );
          return result;
        } catch (error) {
          console.error("[NextAuth Credentials] Error:", error);
          return null; // 認証失敗
        }
      },
    }),
  ],

  // セッション設定
  session: {
    strategy: "jwt", // JWT を使用（データベースセッションは使わない）
    maxAge: 30 * 24 * 60 * 60, // 30日間有効
  },

  // Cookie 設定
  cookies: {
    sessionToken: {
      name: `__Secure-next-auth.session-token`,
      options: {
        httpOnly: true, // JavaScript からアクセス不可（XSS対策）
        sameSite: "lax", // CSRF対策
        path: "/", // サイト全体で有効
        secure: true, // HTTPS のみ（本番環境）
      },
    },
  },

  // Vercelでの動作に必要
  trustHost: true,

  // カスタムページ
  pages: {
    signIn: "/auth/signin", // ログインページ
    signOut: "/", // ログアウト後の遷移先
  },

  // コールバック関数
  callbacks: {
    // リダイレクト先を制御
    async redirect({ url, baseUrl }) {
      console.log("[NextAuth Redirect] URL:", url);
      console.log("[NextAuth Redirect] Base URL:", baseUrl);

      // ログイン後はHome画面(/)に遷移
      if (url === baseUrl || url === `${baseUrl}/`) {
        return baseUrl;
      }
      // サインイン後もHomeに
      if (url.startsWith(baseUrl)) {
        return url;
      }
      return baseUrl;
    },

    // サインイン時の処理
    async signIn({ user, account, profile }) {
      console.log("[NextAuth SignIn] Provider:", account?.provider);
      console.log("[NextAuth SignIn] User email:", user.email);
      console.log(
        "[NextAuth SignIn] User object:",
        JSON.stringify(user, null, 2)
      );

      // Google OAuth の場合、ユーザーをデータベースに登録
      if (account?.provider === "google" && user.email) {
        try {
          // データベースにユーザーが存在するかチェック
          let dbUser = await prisma.user.findUnique({
            where: { email: user.email },
          });
          console.log("[NextAuth SignIn] DB User found:", !!dbUser);

          // ユーザーが存在しない場合は新規登録
          if (!dbUser) {
            console.log("[NextAuth SignIn] Creating new user...");
            dbUser = await prisma.user.create({
              data: {
                email: user.email,
                name: user.name || user.email.split("@")[0],
                role: "member", // デフォルトはメンバー
              },
            });
            console.log("[NextAuth SignIn] New user created:", dbUser.id);
          }
        } catch (error) {
          console.error("[NextAuth SignIn] Error:", error);
          return false; // サインイン失敗
        }
      }
      console.log("[NextAuth SignIn] Success");
      return true; // サインイン成功
    },

    // JWT トークンの生成・更新時の処理
    async jwt({ token, user, trigger, account }) {
      console.log("[NextAuth JWT] Trigger:", trigger);
      console.log("[NextAuth JWT] User present:", !!user);
      console.log("[NextAuth JWT] User email:", user?.email);
      console.log(
        "[NextAuth JWT] Token before:",
        JSON.stringify(token, null, 2)
      );

      // サインイン時: データベースからユーザー情報を取得してトークンに追加
      if (user?.email) {
        const dbUser = await prisma.user.findUnique({
          where: { email: user.email },
          select: { id: true, role: true, name: true, avatarUrl: true },
        });
        console.log("[NextAuth JWT] DB User:", JSON.stringify(dbUser, null, 2));

        if (dbUser) {
          token.id = dbUser.id;
          token.role = dbUser.role;
          token.name = dbUser.name;
          token.avatarUrl = dbUser.avatarUrl;
        }
      }

      console.log(
        "[NextAuth JWT] Token after:",
        JSON.stringify(token, null, 2)
      );
      return token;
    },

    // セッション情報を生成
    async session({ session, token }) {
      console.log(
        "[NextAuth Session] Session before:",
        JSON.stringify(session, null, 2)
      );
      console.log("[NextAuth Session] Token:", JSON.stringify(token, null, 2));

      // トークンの情報をセッションに追加
      if (token && session.user) {
        session.user.id = token.id as string;
        session.user.role = token.role as string;
        session.user.name = token.name as string;
        session.user.image = token.avatarUrl as string | null;
      }

      console.log(
        "[NextAuth Session] Session after:",
        JSON.stringify(session, null, 2)
      );
      return session;
    },
  },
});
```

**認証フローの詳細:**

```
┌──────────────────────────────────────────────────┐
│     Google OAuth 認証フロー                       │
└──────────────────────────────────────────────────┘

1. ユーザーが「Googleでログイン」をクリック
   ↓
2. Google の認証画面にリダイレクト
   ↓
3. ユーザーがGoogle アカウントで認証
   ↓
4. Google から認証コードが返ってくる
   ↓
5. NextAuth.js が認証コードをアクセストークンに交換
   ↓
6. signIn コールバックが実行
   - データベースにユーザーが存在するかチェック
   - 存在しない場合は新規登録
   ↓
7. jwt コールバックが実行
   - データベースからユーザー情報を取得
   - JWT トークンに情報を追加（id, role, name, avatarUrl）
   ↓
8. session コールバックが実行
   - JWT トークンの情報をセッションに追加
   ↓
9. ログイン完了、Home画面にリダイレクト


┌──────────────────────────────────────────────────┐
│     メールアドレス + パスワード 認証フロー        │
└──────────────────────────────────────────────────┘

1. ユーザーがメールアドレスとパスワードを入力
   ↓
2. authorize 関数が実行
   - 入力値のバリデーション
   - データベースからユーザーを検索
   - パスワードのハッシュ検証（bcrypt.compare）
   ↓
3. 認証成功の場合、ユーザー情報を返す
   ↓
4. jwt コールバックが実行
   - データベースからユーザー情報を取得
   - JWT トークンに情報を追加
   ↓
5. session コールバックが実行
   - JWT トークンの情報をセッションに追加
   ↓
6. ログイン完了、Home画面にリダイレクト


┌──────────────────────────────────────────────────┐
│     JWT トークンの内容                            │
└──────────────────────────────────────────────────┘

{
  "id": "cuid123...",       // ユーザーID
  "role": "admin",          // 役割（admin or member）
  "name": "山田太郎",       // 名前
  "avatarUrl": "https://...", // アバター画像URL
  "email": "user@example.com", // メールアドレス
  "iat": 1640000000,        // 発行時刻
  "exp": 1642592000         // 有効期限
}


【JWT vs Database Session】

  JWT Session（このプロジェクトで採用）:
  ✅ サーバーレス環境で動作が安定
  ✅ データベースへのアクセスが不要
  ✅ スケーラビリティが高い
  ❌ トークンのサイズに制限（Cookie は 4KB まで）
  ❌ 即座にログアウトさせるのが難しい

  Database Session:
  ✅ 即座にセッションを無効化できる
  ✅ セッション情報を詳細に管理できる
  ❌ データベースへのアクセスが増える
  ❌ サーバーレス環境で不安定な場合がある
```

**重要な設定ポイント:**

```
┌──────────────────────────────────────────────────┐
│     セキュリティ設定                              │
└──────────────────────────────────────────────────┘

【1. Cookie の httpOnly】
  httpOnly: true
  → JavaScript から Cookie にアクセスできない
  → XSS 攻撃からセッショントークンを守る

【2. Cookie の sameSite】
  sameSite: 'lax'
  → CSRF 攻撃を防ぐ
  → 他のサイトからの POST リクエストで Cookie が送られない

【3. Cookie の secure】
  secure: true
  → HTTPS のみで Cookie を送信
  → 中間者攻撃（MITM）を防ぐ

【4. パスワードのハッシュ化】
  await bcrypt.compare(inputPassword, hashedPassword)
  → 平文パスワードを保存しない
  → ハッシュ化されたパスワードと比較

【5. 環境変数の使用】
  process.env.GOOGLE_CLIENT_ID
  → 秘密情報をコードに含めない
  → .env.local や Vercel の環境変数で管理


┌──────────────────────────────────────────────────┐
│     Google OAuth の設定                           │
└──────────────────────────────────────────────────┘

【必要な環境変数】
  GOOGLE_CLIENT_ID=<Google Cloud Console で取得>
  GOOGLE_CLIENT_SECRET=<Google Cloud Console で取得>

【Google Cloud Console での設定】
  1. プロジェクトを作成
  2. OAuth 2.0 クライアント ID を作成
  3. 承認済みのリダイレクト URI に追加:
     - http://localhost:3000/api/auth/callback/google (開発)
     - https://your-domain.vercel.app/api/auth/callback/google (本番)
  4. OAuth 同意画面を設定
     - アプリ名
     - サポートメール
     - スコープ: email, profile


【authorization.params の意味】
  prompt: "consent"
    → 毎回同意画面を表示
    → ユーザーが権限を確認できる

  access_type: "offline"
    → リフレッシュトークンを取得
    → 長期間アクセスできる

  response_type: "code"
    → 認証コードフローを使用
    → セキュアな認証方法
```

---

## 33.2 データベース接続（src/lib/prisma.ts）

Prisma Client のインスタンスを管理するファイルです。

### 実装の詳細

```typescript
import { PrismaClient } from "@prisma/client";

// グローバルオブジェクトに Prisma の型を追加
const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined;
};

/**
 * Prisma Client インスタンス
 *
 * 開発環境: ホットリロード時に複数のインスタンスが作られるのを防ぐ
 * 本番環境: 新しいインスタンスを作成
 */
export const prisma =
  globalForPrisma.prisma ?? // 既存のインスタンスがあればそれを使う
  new PrismaClient({
    log:
      process.env.NODE_ENV === "development"
        ? ["error", "warn"] // 開発環境: エラーと警告のみログ出力
        : [], // 本番環境: ログ出力なし
    datasources: {
      db: {
        url: process.env.DATABASE_URL, // 環境変数から接続URLを取得
      },
    },
  });

// 開発環境では、グローバルオブジェクトにインスタンスを保存
// → ホットリロード時に同じインスタンスを再利用
if (process.env.NODE_ENV !== "production") {
  globalForPrisma.prisma = prisma;
}

// 本番環境でのクリーンアップ
if (process.env.NODE_ENV === "production") {
  // Serverless環境でのコネクションプールの適切な管理
  prisma.$connect(); // 明示的に接続
}

// デフォルトエクスポートも追加（import prisma from '@/lib/prisma' でも使える）
export default prisma;
```

**Prisma Client の仕組み:**

```
┌──────────────────────────────────────────────────┐
│     Prisma Client の役割                          │
└──────────────────────────────────────────────────┘

【Prisma Client とは】
  TypeScript で型安全にデータベースを操作できるORM

  ORM (Object-Relational Mapping):
  オブジェクト ←→ データベースのテーブル
  の対応関係を自動的に管理


【使い方の例】

  import { prisma } from '@/lib/prisma'

  // ユーザーを取得
  const user = await prisma.user.findUnique({
    where: { id: '123' }
  })

  // 投稿を作成
  const post = await prisma.post.create({
    data: {
      title: 'タイトル',
      content: '内容',
      userId: user.id
    }
  })

  // 複雑なクエリ
  const posts = await prisma.post.findMany({
    where: {
      userId: user.id,
      createdAt: {
        gte: new Date('2025-01-01')
      }
    },
    include: {
      user: true,
      comments: true
    },
    orderBy: {
      createdAt: 'desc'
    }
  })


【型安全性】

  // ❌ 存在しないフィールドはエラー
  const user = await prisma.user.findUnique({
    where: { username: 'test' }  // username は存在しない
  })
  // → TypeScript エラー: Property 'username' does not exist

  // ✅ 存在するフィールドのみ使える
  const user = await prisma.user.findUnique({
    where: { email: 'test@example.com' }
  })


┌──────────────────────────────────────────────────┐
│     開発環境でのホットリロード対策                │
└──────────────────────────────────────────────────┘

【問題】
  Next.js の開発サーバーは、コードを変更すると
  自動的に再読み込み（ホットリロード）される
  → 毎回 new PrismaClient() が実行される
  → 大量の Prisma Client インスタンスが作られる
  → データベース接続数の上限に達する

【解決策】
  グローバルオブジェクトにインスタンスを保存

  const globalForPrisma = globalThis as unknown as {
    prisma: PrismaClient | undefined
  }

  export const prisma =
    globalForPrisma.prisma ??      // 既存のインスタンスがあればそれを使う
    new PrismaClient({ ... })      // なければ新しく作る

  if (process.env.NODE_ENV !== 'production') {
    globalForPrisma.prisma = prisma  // グローバルに保存
  }

  → ホットリロード時も同じインスタンスを再利用


┌──────────────────────────────────────────────────┐
│     本番環境での接続管理                          │
└──────────────────────────────────────────────────┘

【Serverless 環境の特性】
  - 関数が実行されるたびに新しいコンテナが起動
  - コンテナは一定時間アイドル状態が続くと破棄される
  - 次のリクエストで再び新しいコンテナが起動

【接続管理の重要性】
  prisma.$connect()
  → 明示的にデータベースに接続
  → コネクションプールを初期化

【コネクションプール】
  データベース接続を使い回す仕組み

  ❌ 毎回接続・切断:
    リクエスト → 接続 → クエリ → 切断
    → 遅い、リソースの無駄

  ✅ コネクションプール:
    リクエスト → プールから接続を取得 → クエリ → プールに返却
    → 速い、効率的


【DATABASE_URL の形式】

  PostgreSQL（Supabase）:
  DATABASE_URL="postgresql://user:password@host:port/database?schema=public"

  Supabase（PostgreSQL + PgBouncer）:
  DATABASE_URL="postgresql://postgres.[project]:[password]@...?pgbouncer=true&connection_limit=1"

  pgbouncer=true: コネクションプーリング有効化
  connection_limit=1: Serverless 環境での推奨設定
```

**ログ設定の詳細:**

```
┌──────────────────────────────────────────────────┐
│     ログレベルの設定                              │
└──────────────────────────────────────────────────┘

【使用可能なログレベル】
  log: ['query', 'info', 'warn', 'error']

  'query': 実行されるSQL文を出力
  'info': 一般的な情報
  'warn': 警告
  'error': エラー

【開発環境での設定】
  log: ['error', 'warn']
  → エラーと警告のみ出力
  → クエリは出力しない（大量のログを防ぐ）

【本番環境での設定】
  log: []
  → ログを出力しない
  → パフォーマンス向上

【デバッグ時の設定】
  log: ['query', 'error', 'warn']
  → 全てのクエリを確認できる
  → パフォーマンス問題の調査に有用

【ログの例】
  prisma:query SELECT * FROM "User" WHERE "email" = $1
  prisma:info Starting a postgresql pool with 2 connections
  prisma:warn 10 connections are already active
  prisma:error Invalid `prisma.user.create()` invocation
```

---

## 33.3 Supabase クライアント（src/lib/supabase.ts）

Supabase Storage クライアントの設定です。

### 実装の詳細

```typescript
import { createClient } from "@supabase/supabase-js";

// 環境変数から設定を取得
const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL;
const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;

// 環境変数が設定されていない場合の警告
if (!supabaseUrl || !supabaseAnonKey) {
  console.warn(
    "Supabase URL and Anon Key are not configured. Avatar upload will not work."
  );
}

/**
 * Supabase クライアント
 *
 * 用途: 画像のアップロードとStorage管理
 * - avatars バケット: ユーザーのアバター画像
 * - posts バケット: 投稿の画像
 *
 * 環境変数が設定されていない場合は null を返す
 * → アプリは動作するが、画像アップロードは無効
 */
export const supabase =
  supabaseUrl && supabaseAnonKey
    ? createClient(supabaseUrl, supabaseAnonKey)
    : null;
```

**Supabase Storage の使い方:**

```
┌──────────────────────────────────────────────────┐
│     Supabase Storage の基本操作                   │
└──────────────────────────────────────────────────┘

【1. 画像のアップロード】

  import { supabase } from '@/lib/supabase'

  // ファイルをアップロード
  const { data, error } = await supabase?.storage
    .from('avatars')  // バケット名
    .upload(`${userId}/avatar.jpg`, file, {
      cacheControl: '3600',  // 1時間キャッシュ
      upsert: true           // 既存ファイルを上書き
    })

  if (error) {
    console.error('Upload error:', error)
    return
  }

  // 公開URLを取得
  const { data: { publicUrl } } = supabase.storage
    .from('avatars')
    .getPublicUrl(`${userId}/avatar.jpg`)

  console.log('Uploaded to:', publicUrl)


【2. 画像の削除】

  const { error } = await supabase?.storage
    .from('avatars')
    .remove([`${userId}/avatar.jpg`])

  if (error) {
    console.error('Delete error:', error)
  }


【3. 画像の一覧取得】

  const { data, error } = await supabase?.storage
    .from('avatars')
    .list(userId, {
      limit: 100,
      offset: 0,
      sortBy: { column: 'name', order: 'asc' }
    })

  console.log('Files:', data)


【4. 画像のダウンロード】

  const { data, error } = await supabase?.storage
    .from('avatars')
    .download(`${userId}/avatar.jpg`)

  if (data) {
    const url = URL.createObjectURL(data)
    // url を img src に設定
  }


┌──────────────────────────────────────────────────┐
│     Storage バケットの設定                        │
└──────────────────────────────────────────────────┘

【Supabase Dashboard での設定】

  1. Storage → Create bucket
  2. バケット名: avatars
  3. Public bucket: ON
     → 誰でもアクセス可能
  4. File size limit: 2MB
     → アバター画像のサイズ制限
  5. Allowed MIME types: image/*
     → 画像ファイルのみ許可

【Storage Policies（アクセス制御）】

  Supabase では RLS (Row Level Security) ポリシーを設定可能

  例: 自分のフォルダにのみアップロード可能

  CREATE POLICY "Users can upload to own folder"
  ON storage.objects
  FOR INSERT
  TO authenticated
  WITH CHECK (
    bucket_id = 'avatars' AND
    (storage.foldername(name))[1] = auth.uid()::text
  );


┌──────────────────────────────────────────────────┐
│     環境変数の設定                                │
└──────────────────────────────────────────────────┘

【必要な環境変数】
  NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
  NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

【NEXT_PUBLIC_ プレフィックス】
  → クライアント側（ブラウザ）でも使える
  → 公開情報なので秘密情報は含まない

【Supabase Dashboard で取得】
  1. Project Settings
  2. API
  3. URL: NEXT_PUBLIC_SUPABASE_URL
  4. anon public key: NEXT_PUBLIC_SUPABASE_ANON_KEY


┌──────────────────────────────────────────────────┐
│     実際の使用例（プロフィール画像アップロード）   │
└──────────────────────────────────────────────────┘

// components/ProfileImageUpload.tsx

const handleImageUpload = async (file: File) => {
  if (!supabase) {
    alert('画像アップロード機能が無効です')
    return
  }

  // ファイル名を生成（ユニーク）
  const fileExt = file.name.split('.').pop()
  const fileName = `${session.user.id}/avatar.${fileExt}`

  try {
    // アップロード
    const { error: uploadError } = await supabase.storage
      .from('avatars')
      .upload(fileName, file, {
        cacheControl: '3600',
        upsert: true  // 既存ファイルを上書き
      })

    if (uploadError) {
      throw uploadError
    }

    // 公開URLを取得
    const { data: { publicUrl } } = supabase.storage
      .from('avatars')
      .getPublicUrl(fileName)

    // データベースを更新
    await fetch('/api/profile', {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ avatarUrl: publicUrl })
    })

    alert('プロフィール画像を更新しました')
  } catch (error) {
    console.error('Upload error:', error)
    alert('画像のアップロードに失敗しました')
  }
}
```

---

## 33.4 権限管理（src/lib/permissions.ts）

権限チェック用のヘルパー関数です。このプロジェクトでは3つの役割（role）をサポートしています。

### 実装の詳細

```typescript
import { auth } from '@/lib/auth'
import { prisma } from '@/lib/prisma'

/**
 * 管理者権限をチェック（adminまたはsite_admin）
 *
 * @returns {Promise<boolean>} 管理者の場合 true
 */
export async function isAdmin(): Promise<boolean> {
  const session = await auth()
  if (!session?.user?.id) return false

  const user = await prisma.user.findUnique({
    where: { id: session.user.id },
    select: { role: true }
  })

  // adminまたはsite_adminの場合はtrue
  return user?.role === 'admin' || user?.role === 'site_admin'
}

/**
 * サイト管理者権限をチェック（site_adminのみ）
 *
 * @returns {Promise<boolean>} サイト管理者の場合 true
 */
export async function isSiteAdmin(): Promise<boolean> {
  const session = await auth()
  if (!session?.user?.id) return false

  const user = await prisma.user.findUnique({
    where: { id: session.user.id },
    select: { role: true }
  })

  // site_adminのみtrue
  return user?.role === 'site_admin'
}

/**
 * 管理者権限を要求（adminまたはsite_admin）
 * 管理者でない場合はエラーをスロー
 *
 * @throws {Error} 管理者でない場合
 */
export async function requireAdmin() {
  const admin = await isAdmin()
  if (!admin) {
    throw new Error('管理者権限が必要です')
  }
}

/**
 * サイト管理者権限を要求（site_adminのみ）
 * サイト管理者でない場合はエラーをスロー
 *
 * @throws {Error} サイト管理者でない場合
 */
export async function requireSiteAdmin() {
  const siteAdmin = await isSiteAdmin()
  if (!siteAdmin) {
    throw new Error('サイト管理者権限が必要です')
  }
}
```

### 役割（Role）の種類と権限

```
┌──────────────────────────────────────────────────┐
│     役割の階層構造                                │
└──────────────────────────────────────────────────┘

  site_admin (サイト管理者)
      ↓ すべての権限を持つ
  admin (管理者)
      ↓ 投稿・イベント・スケジュールの管理
  member (一般メンバー)
      ↓ 閲覧・いいね・コメント


【各役割の権限】

┌─────────────┬─────────┬─────────┬─────────┐
│    操作     │ member  │  admin  │site_admin│
├─────────────┼─────────┼─────────┼─────────┤
│ 投稿の閲覧  │    ✅   │    ✅   │    ✅   │
│ いいね      │    ✅   │    ✅   │    ✅   │
│ コメント    │    ✅   │    ✅   │    ✅   │
├─────────────┼─────────┼─────────┼─────────┤
│ 投稿の作成  │    ❌   │    ✅   │    ✅   │
│ 投稿の編集  │    ❌   │    ✅   │    ✅   │
│ 投稿の削除  │    ❌   │    ✅   │    ✅   │
├─────────────┼─────────┼─────────┼─────────┤
│ イベント作成│    ❌   │    ✅   │    ✅   │
│ スケジュール│    ❌   │    ✅   │    ✅   │
│ 作成        │         │         │         │
├─────────────┼─────────┼─────────┼─────────┤
│ ユーザー管理│    ❌   │    ❌   │    ✅   │
│ 役割変更    │    ❌   │    ❌   │    ✅   │
│ ユーザー削除│    ❌   │    ❌   │    ✅   │
└─────────────┴─────────┴─────────┴─────────┘


【実装例】

// 投稿作成（adminまたはsite_admin）
export async function POST(request: NextRequest) {
  const admin = await isAdmin()
  if (!admin) {
    return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
  }
  // ...
}

// ユーザー管理（site_adminのみ）
export async function PATCH(request: NextRequest) {
  const siteAdmin = await isSiteAdmin()
  if (!siteAdmin) {
    return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
  }
  // ...
}
```

**権限チェックの実装パターン:**

```
┌──────────────────────────────────────────────────┐
│     権限チェックの使い方                          │
└──────────────────────────────────────────────────┘

【パターン1: isAdmin() - 条件分岐】

  import { isAdmin } from '@/lib/permissions'

  export async function POST(request: NextRequest) {
    const session = await auth()
    if (!session?.user) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
    }

    // 管理者チェック（adminまたはsite_admin）
    const admin = await isAdmin()
    if (!admin) {
      return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
    }

    // 管理者のみが実行できる処理
    const post = await prisma.post.create({ ... })
    return NextResponse.json(post)
  }


【パターン2: requireAdmin() - エラースロー】

  import { requireAdmin } from '@/lib/permissions'

  export async function POST(request: NextRequest) {
    try {
      // 管理者でない場合はエラーをスロー
      await requireAdmin()

      // 管理者のみが実行できる処理
      const post = await prisma.post.create({ ... })
      return NextResponse.json(post)
    } catch (error) {
      if (error.message === '管理者権限が必要です') {
        return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
      }
      return NextResponse.json({ error: 'Internal Server Error' }, { status: 500 })
    }
  }


【パターン3: isSiteAdmin() - 最高権限のみ】

  import { isSiteAdmin } from '@/lib/permissions'

  // ユーザーの役割を変更（site_adminのみ）
  export async function PATCH(request: NextRequest, { params }: Props) {
    const siteAdmin = await isSiteAdmin()
    if (!siteAdmin) {
      return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
    }

    // ユーザーの役割を変更
    const { role } = await request.json()
    const user = await prisma.user.update({
      where: { id: params.id },
      data: { role },
    })
    return NextResponse.json(user)
  }


【パターン4: UI での使い分け】

  import { useSession } from 'next-auth/react'

  export default function PostsPage() {
    const { data: session } = useSession()
    
    // セッションから役割を取得
    const isAdmin = session?.user?.role === 'admin' || session?.user?.role === 'site_admin'
    const isSiteAdmin = session?.user?.role === 'site_admin'

    return (
      <div>
        {/* 管理者のみ表示 */}
        {isAdmin && (
          <Link href="/posts/new">
            新規投稿
          </Link>
        )}

        {/* サイト管理者のみ表示 */}
        {isSiteAdmin && (
          <Link href="/admin/users">
            ユーザー管理
          </Link>
        )}

        {/* 全員に表示 */}
        <div>投稿一覧</div>
      </div>
    )
  }


┌──────────────────────────────────────────────────┐
│     権限チェックのベストプラクティス              │
└──────────────────────────────────────────────────┘

【1. サーバー側で必ずチェック】
  ✅ API Route で権限チェック
  ❌ UI のみで権限チェック

  理由: ブラウザの開発者ツールで UI を改変できるため

【2. 適切なHTTPステータスコードを返す】
  401 Unauthorized: 認証が必要（ログインしていない）
  403 Forbidden: 権限がない（ログインしているが権限不足）
  404 Not Found: リソースが見つからない（権限がない場合も）

【3. エラーメッセージを統一】
  '管理者権限が必要です' (isAdmin, requireAdmin)
  'サイト管理者権限が必要です' (isSiteAdmin, requireSiteAdmin)
  '認証が必要です' (未ログイン)

【4. ログを記録】
  console.log('[Permission] Admin check:', isAdmin)
  console.log('[Permission] Site admin check:', isSiteAdmin)
  → 不正アクセスの検出に有用

【5. 階層的な権限チェック】
  // ✅ 正しい: site_adminはadminの権限も持つ
  const admin = await isAdmin()  // adminまたはsite_admin

  // ❌ 間違い: site_adminがadmin専用機能を使えない
  const user = await prisma.user.findUnique(...)
  if (user?.role !== 'admin') { ... }

【6. Prismaでの役割取得】
  // セッションの役割は古い可能性があるため、
  // 重要な操作の前にデータベースから最新の役割を取得
  const user = await prisma.user.findUnique({
    where: { id: session.user.id },
    select: { role: true }
  })
```

### 役割の追加・拡張方法

将来的に役割を追加する場合の例：

```typescript
// src/lib/permissions.ts

/**
 * モデレーター権限をチェック（moderator, admin, site_adminのいずれか）
 */
export async function isModerator(): Promise<boolean> {
  const session = await auth()
  if (!session?.user?.id) return false

  const user = await prisma.user.findUnique({
    where: { id: session.user.id },
    select: { role: true }
  })

  return user?.role === 'moderator' ||
         user?.role === 'admin' ||
         user?.role === 'site_admin'
}

/**
 * 特定の役割をチェック
 */
export async function hasRole(role: string): Promise<boolean> {
  const session = await auth()
  if (!session?.user?.id) return false

  const user = await prisma.user.findUnique({
    where: { id: session.user.id },
    select: { role: true }
  })

  return user?.role === role
}

/**
 * 複数の役割のいずれかをチェック
 */
export async function hasAnyRole(roles: string[]): Promise<boolean> {
  const session = await auth()
  if (!session?.user?.id) return false

  const user = await prisma.user.findUnique({
    where: { id: session.user.id },
    select: { role: true }
  })

  return roles.includes(user?.role || '')
}
```

**使用例**:

```typescript
// モデレーター以上の権限が必要
const isMod = await isModerator()
if (!isMod) {
  return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
}

// 特定の役割のみ
const hasSpecificRole = await hasRole('editor')

// 複数の役割のいずれか
const canEdit = await hasAnyRole(['admin', 'site_admin', 'editor'])
```

---

## 33.5 型定義（src/types/）

TypeScript の型定義です。

### next-auth.d.ts

NextAuth.js の型拡張です。

```typescript
import NextAuth, { DefaultSession } from "next-auth";

/**
 * NextAuth の型拡張
 *
 * デフォルトの User と Session 型に
 * カスタムフィールド（id, role）を追加
 */
declare module "next-auth" {
  /**
   * セッション情報の型
   */
  interface Session {
    user: {
      id: string; // ユーザーID
      role: string; // 役割（admin or member）
    } & DefaultSession["user"];
  }

  /**
   * ユーザー情報の型
   */
  interface User {
    id: string;
    email: string;
    role: string;
  }
}

/**
 * JWT トークンの型
 */
declare module "next-auth/jwt" {
  interface JWT {
    id: string;
    role: string;
    avatarUrl?: string | null;
  }
}
```

**型拡張の仕組み:**

```
┌──────────────────────────────────────────────────┐
│     TypeScript の declare module                  │
└──────────────────────────────────────────────────┘

【declare module の役割】
  既存のライブラリの型定義を拡張する

  例: NextAuth のデフォルト型

  interface Session {
    user: {
      name?: string | null
      email?: string | null
      image?: string | null
    }
  }

  → id と role が含まれていない


【拡張後の型】

  interface Session {
    user: {
      id: string        // ✅ 追加
      role: string      // ✅ 追加
      name?: string | null
      email?: string | null
      image?: string | null
    }
  }


【使用例】

  import { useSession } from 'next-auth/react'

  export default function Component() {
    const { data: session } = useSession()

    // ✅ 型安全にアクセスできる
    const userId = session?.user?.id       // string | undefined
    const userRole = session?.user?.role   // string | undefined

    // ❌ 存在しないプロパティはエラー
    const username = session?.user?.username  // TypeScript エラー
  }


┌──────────────────────────────────────────────────┐
│     & DefaultSession["user"] の意味               │
└──────────────────────────────────────────────────┘

【型の合成】
  {
    id: string
    role: string
  } & DefaultSession["user"]

  = {
    id: string
    role: string
    name?: string | null
    email?: string | null
    image?: string | null
  }

  → 既存の型とカスタムフィールドを組み合わせる
```

---

## まとめ

この章では、プロジェクトの基盤となるライブラリとユーティリティを詳しく解説しました。

### 🎓 この章で学んだこと

#### 認証システム（auth.ts）

- ✅ NextAuth.js v5 の設定
- ✅ Google OAuth の実装
- ✅ メールアドレス + パスワード認証
- ✅ JWT セッション管理
- ✅ コールバック関数の活用

#### データベース接続（prisma.ts）

- ✅ Prisma Client の管理
- ✅ ホットリロード対策
- ✅ コネクションプールの設定
- ✅ ログレベルの調整

#### ストレージ連携（supabase.ts）

- ✅ Supabase Storage の使い方
- ✅ 画像のアップロード・削除
- ✅ 公開 URL の取得
- ✅ バケットの設定

#### 権限管理（permissions.ts）

- ✅ isAdmin / requireAdmin の実装
- ✅ 権限チェックのパターン
- ✅ セキュリティのベストプラクティス

#### 型定義（next-auth.d.ts）

- ✅ TypeScript の型拡張
- ✅ NextAuth の型カスタマイズ
- ✅ 型安全なコーディング

### 💡 重要なポイント

#### 共通ライブラリの設計原則

```typescript
// ✅ 良い例: 設定を一元管理
export const prisma = new PrismaClient({ ... })

// ❌ 悪い例: 各ファイルで個別に設定
// ファイルA.ts
const prisma = new PrismaClient()

// ファイルB.ts
const prisma = new PrismaClient()
// → 複数のインスタンスが作られる
```

#### セキュリティのベストプラクティス

```typescript
// ✅ サーバー側で権限チェック
export async function POST(request: NextRequest) {
  const admin = await isAdmin();
  if (!admin) {
    return NextResponse.json({ error: "Forbidden" }, { status: 403 });
  }
  // ...
}

// ❌ UI のみで権限チェック
export default function Page() {
  if (session?.user?.role !== "admin") {
    return <div>権限がありません</div>;
  }
  // → ブラウザで改変できる
}
```

---

## 33.6 メール送信（src/lib/email.ts）

メールアドレスの検証とパスワードリセットのためのメール送信機能です。Resendサービスを使用します。

### 実装の詳細

```typescript
import crypto from 'crypto'
import prisma from './prisma'
import { Resend } from 'resend'

// Resendインスタンスを遅延初期化（ビルド時エラー回避）
let resendInstance: Resend | null = null

function getResend(): Resend {
  if (!resendInstance && process.env.RESEND_API_KEY) {
    resendInstance = new Resend(process.env.RESEND_API_KEY)
  }
  if (!resendInstance) {
    throw new Error('RESEND_API_KEY is not set')
  }
  return resendInstance
}

/**
 * メールアドレス検証トークンを生成して保存
 *
 * @param email - 検証するメールアドレス
 * @returns {Promise<string>} 生成されたトークン
 */
export async function generateVerificationToken(email: string): Promise<string> {
  // ランダムな32バイトのトークンを生成
  const token = crypto.randomBytes(32).toString('hex')
  const expires = new Date(Date.now() + 24 * 60 * 60 * 1000) // 24時間後

  // 既存のトークンを削除（同じメールアドレスで複数回登録された場合）
  await prisma.verificationToken.deleteMany({
    where: { identifier: email }
  })

  // 新しいトークンを作成
  await prisma.verificationToken.create({
    data: {
      identifier: email,
      token,
      expires
    }
  })

  return token
}

/**
 * メールアドレス検証トークンを検証
 *
 * @param token - 検証トークン
 * @returns {Promise<string | null>} 検証成功した場合はメールアドレス、失敗した場合はnull
 */
export async function verifyEmailToken(token: string): Promise<string | null> {
  const verificationToken = await prisma.verificationToken.findUnique({
    where: { token }
  })

  if (!verificationToken) {
    return null // トークンが存在しない
  }

  if (verificationToken.expires < new Date()) {
    // トークンが期限切れの場合は削除
    await prisma.verificationToken.delete({
      where: { token }
    })
    return null
  }

  return verificationToken.identifier // email
}

/**
 * パスワードリセットトークンを生成して保存
 *
 * @param email - パスワードをリセットするメールアドレス
 * @returns {Promise<string>} 生成されたトークン
 */
export async function generatePasswordResetToken(email: string): Promise<string> {
  const token = crypto.randomBytes(32).toString('hex')
  const expires = new Date(Date.now() + 60 * 60 * 1000) // 1時間後

  // 既存のトークンを削除
  await prisma.verificationToken.deleteMany({
    where: { identifier: `reset:${email}` }
  })

  // 新しいトークンを作成
  // identifierに "reset:" プレフィックスをつけてメール検証と区別
  await prisma.verificationToken.create({
    data: {
      identifier: `reset:${email}`,
      token,
      expires
    }
  })

  return token
}

/**
 * パスワードリセットトークンを検証
 *
 * @param token - リセットトークン
 * @returns {Promise<string | null>} 検証成功した場合はメールアドレス、失敗した場合はnull
 */
export async function verifyPasswordResetToken(token: string): Promise<string | null> {
  const verificationToken = await prisma.verificationToken.findUnique({
    where: { token }
  })

  if (!verificationToken) {
    return null
  }

  if (verificationToken.expires < new Date()) {
    await prisma.verificationToken.delete({
      where: { token }
    })
    return null
  }

  // identifier から email を取得（"reset:" プレフィックスを削除）
  const email = verificationToken.identifier.replace('reset:', '')
  return email
}

/**
 * メールアドレス検証メールを送信
 *
 * @param email - 送信先メールアドレス
 * @param token - 検証トークン
 */
export async function sendVerificationEmail(email: string, token: string) {
  const verificationUrl = `${process.env.NEXTAUTH_URL || process.env.AUTH_URL || 'http://localhost:3000'}/api/auth/verify-email?token=${token}`
  
  // 開発環境ではログに出力（Resendを使わない）
  if (process.env.NODE_ENV !== 'production') {
    console.log('\n========================================')
    console.log('📧 メールアドレス検証リンク')
    console.log('========================================')
    console.log(`宛先: ${email}`)
    console.log(`検証URL: ${verificationUrl}`)
    console.log('========================================\n')
    return
  }
  
  // 本番環境ではResendでメール送信
  try {
    const resend = getResend()
    await resend.emails.send({
      from: process.env.RESEND_FROM_EMAIL || 'onboarding@resend.dev',
      to: email,
      subject: 'BOLD 軽音 - メールアドレスを確認してください',
      html: `
        <div style="font-family: sans-serif; max-width: 600px; margin: 0 auto;">
          <h1 style="color: #2563eb;">BOLD 軽音</h1>
          <p>メールアドレスの確認をお願いします。</p>
          <p>以下のボタンをクリックしてメールアドレスを確認してください：</p>
          <a href="${verificationUrl}" 
             style="display: inline-block; background-color: #2563eb; color: white; padding: 12px 24px; text-decoration: none; border-radius: 8px; margin: 16px 0;">
            メールアドレスを確認
          </a>
          <p style="color: #6b7280; font-size: 14px;">
            このリンクは24時間有効です。<br>
            心当たりがない場合は、このメールを無視してください。
          </p>
        </div>
      `
    })
  } catch (error) {
    console.error('Failed to send verification email:', error)
    // エラーが発生してもユーザー登録は継続する
    // 開発環境用のログを出力
    console.log('\n========================================')
    console.log('📧 メールアドレス検証リンク (Resend送信失敗)')
    console.log('========================================')
    console.log(`宛先: ${email}`)
    console.log(`検証URL: ${verificationUrl}`)
    console.log('========================================\n')
  }
}

/**
 * パスワードリセットメールを送信
 *
 * @param email - 送信先メールアドレス
 * @param token - リセットトークン
 */
export async function sendPasswordResetEmail(email: string, token: string) {
  const resetUrl = `${process.env.NEXTAUTH_URL || process.env.AUTH_URL || 'http://localhost:3000'}/auth/reset-password?token=${token}`
  
  // 開発環境ではログに出力
  if (process.env.NODE_ENV !== 'production') {
    console.log('\n========================================')
    console.log('🔑 パスワードリセットリンク')
    console.log('========================================')
    console.log(`宛先: ${email}`)
    console.log(`リセットURL: ${resetUrl}`)
    console.log('========================================\n')
    return
  }
  
  // 本番環境ではResendでメール送信
  try {
    const resend = getResend()
    await resend.emails.send({
      from: process.env.RESEND_FROM_EMAIL || 'onboarding@resend.dev',
      to: email,
      subject: 'BOLD 軽音 - パスワードリセット',
      html: `
        <div style="font-family: sans-serif; max-width: 600px; margin: 0 auto;">
          <h1 style="color: #2563eb;">BOLD 軽音</h1>
          <p>パスワードリセットのリクエストを受け付けました。</p>
          <p>以下のボタンをクリックして新しいパスワードを設定してください：</p>
          <a href="${resetUrl}" 
             style="display: inline-block; background-color: #2563eb; color: white; padding: 12px 24px; text-decoration: none; border-radius: 8px; margin: 16px 0;">
            パスワードをリセット
          </a>
          <p style="color: #6b7280; font-size: 14px;">
            このリンクは1時間有効です。<br>
            心当たりがない場合は、このメールを無視してください。
          </p>
        </div>
      `
    })
  } catch (error) {
    console.error('Failed to send password reset email:', error)
    console.log('\n========================================')
    console.log('🔑 パスワードリセットリンク (Resend送信失敗)')
    console.log('========================================')
    console.log(`宛先: ${email}`)
    console.log(`リセットURL: ${resetUrl}`)
    console.log('========================================\n')
  }
}
```

### メール送信の仕組み

```
┌──────────────────────────────────────────────────┐
│     メール送信フロー                              │
└──────────────────────────────────────────────────┘

【メールアドレス検証フロー】

1. ユーザー登録
   ↓
2. generateVerificationToken(email)
   - ランダムなトークンを生成
   - データベースに保存（24時間有効）
   ↓
3. sendVerificationEmail(email, token)
   - 検証リンクをメールで送信
   - 開発環境: コンソールに出力
   - 本番環境: Resendで送信
   ↓
4. ユーザーがリンクをクリック
   ↓
5. /api/auth/verify-email?token=xxx
   ↓
6. verifyEmailToken(token)
   - トークンを検証
   - 有効期限をチェック
   ↓
7. メールアドレスを確認済みにする
   - emailVerified フィールドを更新
   - トークンを削除


【パスワードリセットフロー】

1. パスワードを忘れた
   ↓
2. generatePasswordResetToken(email)
   - ランダムなトークンを生成
   - データベースに保存（1時間有効）
   ↓
3. sendPasswordResetEmail(email, token)
   - リセットリンクをメールで送信
   ↓
4. ユーザーがリンクをクリック
   ↓
5. /auth/reset-password?token=xxx
   ↓
6. verifyPasswordResetToken(token)
   - トークンを検証
   ↓
7. 新しいパスワードを設定
   - パスワードをハッシュ化
   - データベースを更新
   - トークンを削除


【セキュリティ対策】

✅ ランダムなトークン
  crypto.randomBytes(32).toString('hex')
  → 推測不可能な64文字のトークン

✅ 有効期限
  - メール検証: 24時間
  - パスワードリセット: 1時間

✅ 使い捨て
  - 検証後にトークンを削除
  - 同じトークンを再利用できない

✅ identifier の分離
  - メール検証: identifier = email
  - パスワードリセット: identifier = "reset:email"
  → 同じテーブルを使っても混同しない


【開発環境と本番環境の違い】

開発環境 (NODE_ENV !== 'production'):
  - Resendは使わない
  - コンソールにリンクを出力
  - メールサービス不要で開発可能

本番環境 (NODE_ENV === 'production'):
  - Resendでメール送信
  - RESEND_API_KEY が必要
  - RESEND_FROM_EMAIL が必要
```

### Resendの設定

```
┌──────────────────────────────────────────────────┐
│     Resend セットアップ                           │
└──────────────────────────────────────────────────┘

【1. Resendアカウント作成】
  https://resend.com/
  → サインアップ

【2. APIキーを取得】
  Dashboard → API Keys → Create API Key
  → キーをコピー

【3. 環境変数を設定】
  # .env.local または Vercel環境変数
  RESEND_API_KEY=re_xxxxxxxxxxxxx
  RESEND_FROM_EMAIL=noreply@yourdomain.com

【4. ドメイン認証（オプション）】
  - 独自ドメインから送信する場合
  - Dashboard → Domains → Add Domain
  - DNSレコードを設定

【5. テスト送信】
  - 開発環境: コンソールログで確認
  - 本番環境: 実際にメールを送信してテスト


【料金プラン】
  Free: 月100通まで無料
  Pro: 月$20〜（10,000通まで）

【制限事項】
  - Free プランは 1日100通まで
  - 認証なしドメインは onboarding@resend.dev から送信
  - 独自ドメインは DNS 認証が必要
```

### エラーハンドリング

```typescript
// メール送信失敗時でもユーザー登録は継続
export async function sendVerificationEmail(email: string, token: string) {
  try {
    const resend = getResend()
    await resend.emails.send({ ... })
  } catch (error) {
    // エラーログを出力
    console.error('Failed to send verification email:', error)
    
    // 開発環境用にコンソールにリンクを出力
    console.log('検証URL:', verificationUrl)
    
    // エラーをスローせず、処理を続行
    // → ユーザー登録は成功扱いにする
  }
}
```

**ポイント**:
- 📧 **ユーザー体験を優先**: メール送信に失敗してもユーザー登録は完了
- 🔍 **開発者向けログ**: 失敗時もコンソールにリンクを出力
- 🛡️ **セキュリティ**: トークンの有効期限と使い捨てで安全性を確保

---

## 33.7 共通コンポーネント

このセクションでは、プロジェクト全体で使用される共通のUIコンポーネントを解説します。

### 33.7.1 Providers（src/components/Providers.tsx）

アプリケーション全体にNextAuth.jsのSessionProviderを提供するコンポーネントです。

```tsx
'use client'

import { SessionProvider } from 'next-auth/react'
import { ReactNode } from 'react'

/**
 * Providers コンポーネント
 * 
 * アプリケーション全体に認証セッションを提供
 * layout.tsx で children をラップして使用
 */
export function Providers({ children }: { children: ReactNode }) {
  return <SessionProvider>{children}</SessionProvider>
}
```

**実装のポイント:**

```
┌──────────────────────────────────────────────────┐
│     Providers の役割                              │
└──────────────────────────────────────────────────┘

【SessionProvider の提供】
  <SessionProvider>{children}</SessionProvider>
  
  → アプリケーション全体で useSession() フックを使用可能にする
  → 認証状態を管理
  → セッション情報をコンポーネントツリー全体に配布


【layout.tsx での使用】
  export default function RootLayout({ children }) {
    return (
      <html lang="ja">
        <body>
          <Providers>
            {children}
          </Providers>
        </body>
      </html>
    )
  }
  
  → すべてのページで認証機能が利用可能


【'use client' ディレクティブ】
  SessionProvider は Client Component
  
  → layout.tsx を Server Component のまま維持
  → Providers コンポーネントだけを Client Component にする
  → パフォーマンスの最適化


【拡張性】
  将来的に他のプロバイダーを追加可能:
  
  export function Providers({ children }) {
    return (
      <SessionProvider>
        <ThemeProvider>
          <ToastProvider>
            {children}
          </ToastProvider>
        </ThemeProvider>
      </SessionProvider>
    )
  }
```

---

### 33.7.2 DashboardLayout（src/components/DashboardLayout.tsx）

ダッシュボード全体のレイアウトを提供するコンポーネントです。ナビゲーションバー、サイドバー、フッターを含みます。

```tsx
'use client'

import Link from 'next/link'
import { useSession, signOut } from 'next-auth/react'
import { Home, FileText, Calendar, MessageCircle, LogOut, User, Users, Menu, X, CalendarDays, PartyPopper } from 'lucide-react'
import { usePathname } from 'next/navigation'
import { useState } from 'react'
import Footer from './Footer'

/**
 * DashboardLayout コンポーネント
 * 
 * ダッシュボード全体のレイアウト
 * - ナビゲーションバー
 * - モバイルメニュー
 * - ユーザー情報表示
 * - フッター
 */
export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  // 1. 認証セッションとルーティング
  const { data: session, status } = useSession()
  const pathname = usePathname()
  
  // 2. モバイルメニューの開閉状態
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false)

  /**
   * 現在のパスがアクティブかチェック
   */
  const isActive = (path: string) => pathname.startsWith(path)

  // 3. ローディング中の表示
  if (status === 'loading') {
    return (
      <div className="min-h-screen flex items-center justify-center">
        <div className="text-xl">読み込み中...</div>
      </div>
    )
  }

  // 4. 未認証の場合はログイン画面へ誘導
  if (!session) {
    return (
      <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900">
        <div className="text-center">
          <h2 className="text-2xl font-bold mb-4 text-white">ログインが必要です</h2>
          <Link
            href="/auth/signin"
            className="px-6 py-3 bg-gradient-to-r from-blue-600 to-purple-600 text-white rounded-lg hover:scale-105 transition-all shadow-lg inline-block"
          >
            ログイン画面へ
          </Link>
        </div>
      </div>
    )
  }

  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900">
      {/* ナビゲーションバー */}
      <nav className="bg-white/10 backdrop-blur-md border-b border-white/10 sticky top-0 z-50">
        <div className="container mx-auto px-4">
          <div className="flex items-center justify-between h-16">
            {/* ロゴ */}
            <Link href="/" className="flex items-center gap-2 text-lg sm:text-xl font-bold text-white">
              <Home className="w-5 h-5 sm:w-6 sm:h-6" />
              <span>BOLD軽音</span>
            </Link>

            {/* デスクトップメニュー */}
            <div className="hidden md:flex items-center gap-1">
              {/* 活動一覧リンク */}
              <Link
                href="/posts"
                prefetch={true}
                className={`flex items-center gap-2 px-4 py-2 rounded-lg transition-all relative ${
                  isActive('/posts') 
                    ? 'bg-gradient-to-r from-blue-600 to-purple-600 text-white shadow-lg shadow-blue-500/50 scale-105' 
                    : 'text-white/80 hover:bg-white/10 hover:scale-105'
                }`}
              >
                <FileText className="w-5 h-5" />
                <span>活動一覧</span>
                {/* アクティブインジケーター */}
                {isActive('/posts') && (
                  <div className="absolute -bottom-1 left-1/2 transform -translate-x-1/2 w-1/2 h-1 bg-white rounded-full"></div>
                )}
              </Link>
              
              {/* 活動スケジュールリンク */}
              <Link
                href="/activity-schedules"
                prefetch={true}
                className={`flex items-center gap-2 px-4 py-2 rounded-lg transition-all relative ${
                  isActive('/activity-schedules') 
                    ? 'bg-gradient-to-r from-blue-600 to-purple-600 text-white shadow-lg shadow-blue-500/50 scale-105' 
                    : 'text-white/80 hover:bg-white/10 hover:scale-105'
                }`}
              >
                <CalendarDays className="w-5 h-5" />
                <span>活動スケジュール</span>
                {isActive('/activity-schedules') && (
                  <div className="absolute -bottom-1 left-1/2 transform -translate-x-1/2 w-1/2 h-1 bg-white rounded-full"></div>
                )}
              </Link>
              
              {/* イベントリンク */}
              <Link
                href="/events"
                prefetch={true}
                className={`flex items-center gap-2 px-4 py-2 rounded-lg transition-all relative ${
                  isActive('/events') 
                    ? 'bg-gradient-to-r from-blue-600 to-purple-600 text-white shadow-lg shadow-blue-500/50 scale-105' 
                    : 'text-white/80 hover:bg-white/10 hover:scale-105'
                }`}
              >
                <PartyPopper className="w-5 h-5" />
                <span>イベント</span>
                {isActive('/events') && (
                  <div className="absolute -bottom-1 left-1/2 transform -translate-x-1/2 w-1/2 h-1 bg-white rounded-full"></div>
                )}
              </Link>
              
              {/* メンバーリンク */}
              <Link
                href="/users"
                prefetch={true}
                className={`flex items-center gap-2 px-4 py-2 rounded-lg transition-all relative ${
                  isActive('/users') 
                    ? 'bg-gradient-to-r from-blue-600 to-purple-600 text-white shadow-lg shadow-blue-500/50 scale-105' 
                    : 'text-white/80 hover:bg-white/10 hover:scale-105'
                }`}
              >
                <Users className="w-5 h-5" />
                <span>メンバー</span>
                {isActive('/users') && (
                  <div className="absolute -bottom-1 left-1/2 transform -translate-x-1/2 w-1/2 h-1 bg-white rounded-full"></div>
                )}
              </Link>
            </div>

            {/* ユーザーメニュー（デスクトップ） */}
            <div className="hidden md:flex items-center gap-4">
              <Link
                href="/profile"
                className="flex items-center gap-2 text-white/80 hover:text-white hover:scale-105 transition-all"
              >
                <User className="w-5 h-5" />
                <span className="text-sm">{session.user.name || session.user.email}</span>
              </Link>
              <button
                onClick={() => signOut({ callbackUrl: '/' })}
                className="flex items-center gap-2 px-4 py-2 bg-red-500/20 text-red-300 rounded-lg hover:bg-red-500/30 hover:scale-105 transition-all"
              >
                <LogOut className="w-4 h-4" />
                <span className="text-sm">ログアウト</span>
              </button>
            </div>

            {/* モバイルメニューボタン */}
            <button
              onClick={() => setMobileMenuOpen(!mobileMenuOpen)}
              className="md:hidden text-white p-2 hover:bg-white/10 rounded-lg transition-colors"
            >
              {mobileMenuOpen ? (
                <X className="w-6 h-6" />
              ) : (
                <Menu className="w-6 h-6" />
              )}
            </button>
          </div>

          {/* モバイルメニュー */}
          {mobileMenuOpen && (
            <div className="md:hidden py-4 space-y-2 border-t border-white/10">
              <Link
                href="/posts"
                onClick={() => setMobileMenuOpen(false)}
                className={`flex items-center gap-2 px-4 py-3 rounded-lg transition-all ${
                  isActive('/posts') 
                    ? 'bg-gradient-to-r from-blue-600 to-purple-600 text-white' 
                    : 'text-white/80 hover:bg-white/10'
                }`}
              >
                <FileText className="w-5 h-5" />
                <span>活動一覧</span>
              </Link>
              {/* 他のモバイルメニュー項目... */}
            </div>
          )}
        </div>
      </nav>

      {/* メインコンテンツ */}
      <main className="container mx-auto px-4 py-8">
        {children}
      </main>

      {/* フッター */}
      <Footer />
    </div>
  )
}
```

**実装のポイント:**

```
┌──────────────────────────────────────────────────┐
│     DashboardLayout の設計パターン                │
└──────────────────────────────────────────────────┘

【レスポンシブデザイン】
  - デスクトップ: 横並びナビゲーション
  - モバイル: ハンバーガーメニュー
  
  hidden md:flex → デスクトップのみ表示
  md:hidden → モバイルのみ表示


【アクティブ状態の視覚化】
  const isActive = (path: string) => pathname.startsWith(path)
  
  → 現在のページに対応するナビゲーション項目をハイライト
  → グラデーション背景 + スケールアップ + アンダーライン


【sticky ナビゲーション】
  sticky top-0 z-50
  
  → スクロールしてもナビゲーションバーが固定
  → 常にメニューにアクセス可能


【backdrop-blur 効果】
  bg-white/10 backdrop-blur-md
  
  → 半透明の背景 + ぼかし効果
  → モダンな glassmorphism デザイン


【prefetch の使用】
  <Link href="/posts" prefetch={true}>
  
  → Next.js がリンク先のページを事前読み込み
  → ページ遷移が高速化


【認証ガード】
  if (!session) {
    return <div>ログインが必要です</div>
  }
  
  → 未認証ユーザーをログイン画面へ誘導
  → レイアウトレベルでの保護
```

---

### 33.7.3 RichTextEditor（src/components/RichTextEditor.tsx）

Markdown形式のテキスト入力を提供するシンプルなエディタコンポーネントです。

```tsx
'use client'

/**
 * Props の型定義
 */
interface Props {
  value: string           // 現在の入力値
  onChange: (value: string) => void  // 値変更時のコールバック
  placeholder?: string    // プレースホルダー（オプション）
  minHeight?: string      // 最小の高さ（オプション）
}

/**
 * RichTextEditor コンポーネント
 * 
 * Markdown 形式のテキスト入力エディタ
 * シンプルな textarea ベース
 */
export default function RichTextEditor({ 
  value, 
  onChange, 
  placeholder, 
  minHeight = '300px' 
}: Props) {
  return (
    <div className="border rounded-lg overflow-hidden bg-white">
      <textarea
        value={value}
        onChange={(e) => onChange(e.target.value)}
        className="w-full px-4 py-3 focus:outline-none resize-none"
        style={{ minHeight }}
        placeholder={placeholder || 'ここに入力してください...'}
      />
    </div>
  )
}
```

**実装のポイント:**

```
┌──────────────────────────────────────────────────┐
│     RichTextEditor の設計パターン                 │
└──────────────────────────────────────────────────┘

【シンプルな実装】
  textarea をベースにした軽量エディタ
  
  → 複雑なリッチテキストエディタライブラリを使用しない
  → Markdown で記述
  → サーバー側で HTML に変換


【Controlled Component】
  <textarea 
    value={value} 
    onChange={(e) => onChange(e.target.value)} 
  />
  
  → 親コンポーネントが値を管理
  → React の制御されたコンポーネントパターン


【resize-none】
  className="resize-none"
  
  → ユーザーがテキストエリアのサイズを変更できないようにする
  → レイアウトの一貫性を保つ


【minHeight の柔軟性】
  minHeight = '300px'
  
  → デフォルトは 300px
  → 親コンポーネントでカスタマイズ可能


【将来の拡張性】
  機能追加の候補:
  - Markdown ツールバー（太字、イタリック、リンク挿入など）
  - リアルタイムプレビュー
  - 構文ハイライト
  - オートセーブ
  
  現在は MVP（Minimum Viable Product）として最小限の実装
```

**MarkdownToolbar との連携:**

このプロジェクトでは、RichTextEditor と連携する MarkdownToolbar コンポーネントも実装されています:

```
【MarkdownToolbar の機能】
  - 太字: **text**
  - イタリック: *text*
  - 見出し: # Heading
  - リスト: - Item
  - リンク: [text](url)
  - コードブロック: ```code```
  
  → ボタンをクリックすると Markdown 構文を自動挿入
  → ユーザーは Markdown を覚えなくても使える
```

---

### 33.7.4 AvatarUpload（src/components/AvatarUpload.tsx）

ユーザーのアバター画像をアップロードするためのUIコンポーネントです。

```tsx
'use client'

import { useState } from 'react'
import { Upload, X } from 'lucide-react'

/**
 * Props の型定義
 */
interface AvatarUploadProps {
  currentAvatar?: string | null    // 現在のアバターURL（オプション）
  onUpload: (file: File) => Promise<void>  // アップロード処理のコールバック
  disabled?: boolean               // 無効化フラグ（オプション）
}

/**
 * AvatarUpload コンポーネント
 * 
 * アバター画像のアップロード・削除機能を提供
 * - ファイル選択
 * - プレビュー表示
 * - アップロード処理
 * - 画像削除
 */
export default function AvatarUpload({ currentAvatar, onUpload, disabled }: AvatarUploadProps) {
  // 1. アップロード中の状態管理
  const [uploading, setUploading] = useState(false)
  
  // 2. プレビュー画像の状態管理
  const [preview, setPreview] = useState<string | null>(currentAvatar || null)

  /**
   * ファイル選択時の処理
   * 
   * @param e - ファイル選択イベント
   */
  const handleFileChange = async (e: React.ChangeEvent<HTMLInputElement>) => {
    // 1. 選択されたファイルを取得
    const file = e.target.files?.[0]
    if (!file) return

    // 2. プレビュー表示のためにFileReaderで画像を読み込み
    const reader = new FileReader()
    reader.onloadend = () => {
      // 読み込み完了時にプレビューを設定
      setPreview(reader.result as string)
    }
    // Data URL として読み込み（base64エンコード）
    reader.readAsDataURL(file)

    // 3. アップロード処理を実行
    setUploading(true)
    try {
      // 親コンポーネントから渡されたアップロード関数を実行
      await onUpload(file)
    } catch (error) {
      // 4. エラー時はプレビューを元に戻す
      console.error('Upload failed:', error)
      alert('アップロードに失敗しました')
      setPreview(currentAvatar || null)
    } finally {
      // 5. アップロード状態を解除
      setUploading(false)
    }
  }

  /**
   * アバター削除処理
   * 
   * 空のファイルをアップロードしてアバターを削除
   */
  const handleRemove = async () => {
    // 1. プレビューをクリア
    setPreview(null)
    
    // 2. 空のファイルを作成してアップロード
    // サーバー側でこれを検知してアバターURLをnullに設定
    const emptyFile = new File([], '')
    await onUpload(emptyFile)
  }

  return (
    <div className="flex items-center gap-4">
      {/* アバター表示エリア */}
      <div className="relative">
        {preview ? (
          // プレビュー画像がある場合
          <div className="relative w-24 h-24 rounded-full overflow-hidden bg-gray-200">
            <img
              src={preview}
              alt="Avatar"
              className="w-full h-full object-cover"
            />
          </div>
        ) : (
          // プレビュー画像がない場合はアップロードアイコンを表示
          <div className="w-24 h-24 rounded-full bg-gray-200 flex items-center justify-center">
            <Upload className="text-gray-400" size={32} />
          </div>
        )}
      </div>

      {/* 操作ボタンエリア */}
      <div className="flex flex-col gap-2">
        {/* アップロードボタン（ファイル選択） */}
        <label className={`px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition cursor-pointer inline-block text-center ${
          disabled || uploading ? 'opacity-50 cursor-not-allowed' : ''
        }`}>
          {uploading ? 'アップロード中...' : 'アバターを選択'}
          {/* 実際のfile inputは非表示にしてlabelでラップ */}
          <input
            type="file"
            accept="image/*"       // 画像ファイルのみ受け付ける
            className="hidden"     // inputを非表示
            onChange={handleFileChange}
            disabled={disabled || uploading}
          />
        </label>

        {/* 削除ボタン（画像がある場合のみ表示） */}
        {preview && (
          <button
            type="button"
            onClick={handleRemove}
            disabled={disabled || uploading}
            className="px-4 py-2 border border-red-600 text-red-600 rounded-lg hover:bg-red-50 transition disabled:opacity-50 disabled:cursor-not-allowed"
          >
            <X size={16} className="inline mr-1" />
            削除
          </button>
        )}
      </div>
    </div>
  )
}
```

**実装のポイント:**

```
┌──────────────────────────────────────────────────┐
│     AvatarUpload の設計パターン                   │
└──────────────────────────────────────────────────┘

【FileReader によるプレビュー】
  const reader = new FileReader()
  reader.onloadend = () => {
    setPreview(reader.result as string)
  }
  reader.readAsDataURL(file)
  
  → ファイルを Data URL（base64）に変換
  → アップロード前にプレビュー表示
  → ユーザーに即座にフィードバック


【楽観的UI更新】
  1. ファイル選択 → 即座にプレビュー表示
  2. アップロード開始
  3. エラー時は元に戻す
  
  → ユーザーは待ち時間なく結果を確認
  → エラー時のロールバックも実装


【label を使ったカスタムボタン】
  <label className="...">
    アバターを選択
    <input type="file" className="hidden" />
  </label>
  
  → デフォルトのfile inputは見た目が悪い
  → labelでラップしてカスタムデザインのボタンに
  → クリックするとfile inputが開く


【アバター削除の実装】
  const emptyFile = new File([], '')
  await onUpload(emptyFile)
  
  → 空のファイルをアップロード
  → サーバー側で file.size === 0 を検知
  → avatarUrl を null に設定


【条件付きレンダリング】
  {preview && (
    <button onClick={handleRemove}>削除</button>
  )}
  
  → 画像がある場合のみ削除ボタンを表示
  → UIをシンプルに保つ


【使用例】
  <AvatarUpload
    currentAvatar={user.avatarUrl}
    onUpload={async (file) => {
      const formData = new FormData()
      formData.append('avatar', file)
      
      const res = await fetch('/api/profile/avatar', {
        method: 'POST',
        body: formData
      })
      
      if (!res.ok) throw new Error('Upload failed')
      
      const { avatarUrl } = await res.json()
      setUser({ ...user, avatarUrl })
    }}
  />
```

---

### 33.7.5 TemplateEditor（src/components/TemplateEditor.tsx）

報告書テンプレートを編集するためのモーダルコンポーネントです。

```tsx
'use client'

import { useState, useEffect } from 'react'
import { X, Save } from 'lucide-react'
import RichTextEditor from './RichTextEditor'

/**
 * Props の型定義
 */
interface Props {
  isOpen: boolean          // モーダルの開閉状態
  onClose: () => void      // 閉じる時のコールバック
  onSave: () => void       // 保存成功時のコールバック
}

/**
 * TemplateEditor コンポーネント
 * 
 * 報告書テンプレートを編集するモーダル
 * - テンプレートの取得
 * - RichTextEditor で編集
 * - 保存処理
 */
export default function TemplateEditor({ isOpen, onClose, onSave }: Props) {
  // 1. テンプレート内容の状態管理
  const [content, setContent] = useState('')
  
  // 2. ローディング状態（取得中）
  const [loading, setLoading] = useState(false)
  
  // 3. 保存中の状態
  const [saving, setSaving] = useState(false)

  /**
   * モーダルが開かれた時にテンプレートを取得
   */
  useEffect(() => {
    if (isOpen) {
      fetchTemplate()
    }
  }, [isOpen])

  /**
   * テンプレートを取得
   */
  const fetchTemplate = async () => {
    setLoading(true)
    try {
      // GET /api/templates でテンプレートを取得
      const res = await fetch('/api/templates')
      if (res.ok) {
        const data = await res.json()
        // 取得したテンプレートを状態に設定
        setContent(data.content || '')
      }
    } catch (error) {
      console.error('テンプレート取得エラー:', error)
    } finally {
      setLoading(false)
    }
  }

  /**
   * テンプレートを保存
   */
  const handleSave = async () => {
    setSaving(true)
    try {
      // PUT /api/templates でテンプレートを更新
      const res = await fetch('/api/templates', {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ content })
      })

      if (res.ok) {
        // 保存成功時はコールバックを実行してモーダルを閉じる
        onSave()
        onClose()
      } else {
        alert('テンプレートの保存に失敗しました')
      }
    } catch (error) {
      console.error('テンプレート保存エラー:', error)
      alert('テンプレートの保存に失敗しました')
    } finally {
      setSaving(false)
    }
  }

  // モーダルが閉じている場合は何も表示しない
  if (!isOpen) return null

  return (
    // モーダルオーバーレイ（背景）
    <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center p-4 z-50">
      {/* モーダルコンテンツ */}
      <div className="bg-white rounded-lg shadow-xl max-w-4xl w-full max-h-[90vh] flex flex-col">
        {/* ヘッダー */}
        <div className="flex items-center justify-between p-4 border-b">
          <h2 className="text-xl font-bold">報告書テンプレート編集</h2>
          <button
            onClick={onClose}
            className="text-gray-500 hover:text-gray-700"
          >
            <X className="w-6 h-6" />
          </button>
        </div>

        {/* メインコンテンツ */}
        <div className="flex-1 overflow-y-auto p-4">
          {loading ? (
            // ローディング中の表示
            <div className="flex items-center justify-center h-64">
              <div className="text-gray-500">読み込み中...</div>
            </div>
          ) : (
            <div>
              {/* 説明文 */}
              <p className="text-sm text-gray-600 mb-4">
                このテンプレートは、イベントと活動スケジュールから報告書を作成する際に自動的に入力されます。
              </p>
              
              {/* エディタ */}
              <RichTextEditor
                value={content}
                onChange={setContent}
                placeholder="テンプレートを入力してください..."
                minHeight="400px"
              />
            </div>
          )}
        </div>

        {/* フッター（ボタンエリア） */}
        <div className="flex justify-end gap-3 p-4 border-t">
          {/* キャンセルボタン */}
          <button
            type="button"
            onClick={onClose}
            className="px-4 py-2 text-gray-700 hover:bg-gray-100 rounded-lg"
          >
            キャンセル
          </button>
          
          {/* 保存ボタン */}
          <button
            onClick={handleSave}
            disabled={saving}
            className="flex items-center gap-2 px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 disabled:opacity-50"
          >
            <Save className="w-5 h-5" />
            {saving ? '保存中...' : '保存'}
          </button>
        </div>
      </div>
    </div>
  )
}
```

**実装のポイント:**

```
┌──────────────────────────────────────────────────┐
│     TemplateEditor の設計パターン                 │
└──────────────────────────────────────────────────┘

【モーダルの実装】
  fixed inset-0 bg-black bg-opacity-50
  
  → 全画面を覆うオーバーレイ
  → 背景を暗くしてモーダルを目立たせる
  → z-50 で最前面に表示


【条件付きレンダリング】
  if (!isOpen) return null
  
  → モーダルが閉じている場合はDOMにも存在しない
  → パフォーマンスの最適化
  → display: none ではなく完全に非表示


【useEffect でデータ取得】
  useEffect(() => {
    if (isOpen) {
      fetchTemplate()
    }
  }, [isOpen])
  
  → モーダルが開かれるたびにテンプレートを取得
  → 最新のデータを常に表示
  → isOpen が変更されたときのみ実行


【flex-1 と overflow-y-auto】
  <div className="flex-1 overflow-y-auto p-4">
  
  → flex-1: 残りの高さを全て使用
  → overflow-y-auto: 内容が多い場合はスクロール
  → max-h-[90vh]: モーダルが画面からはみ出さない


【ローディング状態の表示】
  {loading ? (
    <div>読み込み中...</div>
  ) : (
    <RichTextEditor />
  )}
  
  → データ取得中はエディタを表示しない
  → 空のエディタがちらつくのを防ぐ


【保存処理のフロー】
  1. handleSave 実行
  2. saving = true（ボタン無効化）
  3. API リクエスト（PUT /api/templates）
  4. 成功時: onSave() → onClose()
  5. finally: saving = false
  
  → ユーザーが重複クリックできないようにする
  → 成功時のみモーダルを閉じる


【使用例】
  const [isEditorOpen, setIsEditorOpen] = useState(false)
  
  <button onClick={() => setIsEditorOpen(true)}>
    テンプレート編集
  </button>
  
  <TemplateEditor
    isOpen={isEditorOpen}
    onClose={() => setIsEditorOpen(false)}
    onSave={() => {
      alert('テンプレートを保存しました')
      fetchTemplate()  // テンプレートを再取得
    }}
  />
```

---

### 33.7.6 LoadingSpinner（src/components/LoadingSpinner.tsx）

ローディング中に表示するスピナーコンポーネントです。

```tsx
import { Music } from 'lucide-react'

/**
 * Props の型定義
 */
interface LoadingSpinnerProps {
  variant?: 'default' | 'profile'  // 表示スタイルのバリエーション
  size?: 'sm' | 'md' | 'lg'        // サイズ
}

/**
 * LoadingSpinner コンポーネント
 * 
 * ローディング状態を視覚的に表現
 * - default: 音符アイコンが回転
 * - profile: ユーザーアイコンが回転
 * - 3つのサイズから選択可能
 */
export default function LoadingSpinner({ 
  variant = 'default', 
  size = 'md' 
}: LoadingSpinnerProps) {
  // サイズに応じたCSSクラス
  const sizeClasses = {
    sm: 'w-8 h-8',
    md: 'w-12 h-12',
    lg: 'w-16 h-16'
  }

  // アイコンサイズのクラス
  const iconSizes = {
    sm: 'w-4 h-4',
    md: 'w-6 h-6',
    lg: 'w-8 h-8'
  }

  // プロフィールページ用のスピナー
  if (variant === 'profile') {
    return (
      <div className="flex justify-center items-center py-12">
        <div className="relative">
          {/* 外側の回転する円 */}
          <div className={`${sizeClasses[size]} border-4 border-purple-500/30 border-t-purple-500 rounded-full animate-spin`}></div>
          
          {/* 中央のユーザーアイコン */}
          <div className="absolute inset-0 flex items-center justify-center">
            <div className="w-6 h-6 rounded-full bg-gradient-to-br from-blue-400 to-purple-500 flex items-center justify-center animate-pulse">
              {/* ユーザーアイコンのSVG */}
              <svg className="w-3 h-3 text-white" viewBox="0 0 24 24" fill="currentColor">
                <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
              </svg>
            </div>
          </div>
        </div>
      </div>
    )
  }

  // デフォルトのスピナー（音符アイコン）
  return (
    <div className="flex justify-center items-center py-12">
      <div className="relative">
        {/* 外側の回転リング */}
        <div className={`${sizeClasses[size]} border-4 border-blue-500/20 border-t-blue-500 rounded-full animate-spin`}></div>
        
        {/* 中央の音符アイコン */}
        <div className="absolute inset-0 flex items-center justify-center">
          <Music className={`${iconSizes[size]} text-purple-500 animate-pulse`} />
        </div>
      </div>
    </div>
  )
}
```

**実装のポイント:**

```
┌──────────────────────────────────────────────────┐
│     LoadingSpinner の設計パターン                 │
└──────────────────────────────────────────────────┘

【animate-spin と animate-pulse の組み合わせ】
  animate-spin: 回転アニメーション（外側のリング）
  animate-pulse: 拡大縮小アニメーション（中央のアイコン）
  
  → 2つのアニメーションを重ねることで動的な表現


【absolute positioning での中央配置】
  <div className="relative">
    <div className="animate-spin">外側</div>
    <div className="absolute inset-0 flex items-center justify-center">
      <Icon />  中央
    </div>
  </div>
  
  → relative: 基準となる親要素
  → absolute inset-0: 親要素全体に広がる
  → flex で中央配置


【border-t-color での回転表現】
  border-4 border-blue-500/20 border-t-blue-500
  
  → 全体: 薄い青色
  → 上部のみ: 濃い青色
  → 回転すると上部の濃い部分が動いて見える


【バリエーションの実装】
  variant: 'default' | 'profile'
  
  → 用途に応じたアイコンを表示
  → default: 音符（軽音サークルらしい）
  → profile: ユーザーアイコン（プロフィールページ用）


【サイズの柔軟性】
  size: 'sm' | 'md' | 'lg'
  
  → 使用場所に応じてサイズを変更
  → sm: ボタン内などの小さいエリア
  → md: デフォルト
  → lg: ページ全体のローディング


【使用例】
  // ページ全体のローディング
  {loading && <LoadingSpinner size="lg" />}
  
  // プロフィールページ
  {loading && <LoadingSpinner variant="profile" />}
  
  // ボタン内
  {saving && <LoadingSpinner size="sm" />}
```

---

### 33.7.7 Footer（src/components/Footer.tsx）

フッターコンポーネントです。サイト全体で使用されます。

```tsx
import Link from 'next/link'
import { Github, Twitter, Mail } from 'lucide-react'

/**
 * Props の型定義
 */
interface FooterProps {
  isSiteAdmin?: boolean  // サイト管理者かどうか
  isLoggedIn?: boolean   // ログイン状態
}

/**
 * Footer コンポーネント
 * 
 * サイト全体のフッター
 * - ブランド情報
 * - ナビゲーションリンク
 * - ソーシャルリンク
 * - 法的ページへのリンク
 */
export default function Footer({ isSiteAdmin = false, isLoggedIn = false }: FooterProps) {
  return (
    <footer className="border-t border-white/10 bg-gradient-to-b from-black/40 to-black/60 backdrop-blur-lg">
      <div className="container mx-auto px-4 py-12">
        <div className="grid grid-cols-1 md:grid-cols-4 gap-8 lg:gap-12">
          
          {/* ブランドセクション */}
          <div className="md:col-span-1">
            {/* ロゴとサイト名 */}
            <Link href="/" className="flex items-center gap-3 mb-4">
              <div className="text-3xl">🎸</div>
              <span className="text-xl font-bold text-white">BOLD軽音</span>
            </Link>
            
            {/* 説明文 */}
            <p className="text-sm text-white/60 leading-relaxed mb-6">
              BOLD 大阪軽音部のメンバーサイトです。活動報告や活動スケジュール、イベント管理を行っています。
            </p>
            
            {/* ソーシャルリンク */}
            <div className="flex gap-4">
              <a 
                href="https://github.com/shuhei0720/keion-circle-site" 
                target="_blank" 
                rel="noopener noreferrer"  // セキュリティのため必須
                className="text-white/60 hover:text-white transition-colors"
                aria-label="GitHub"         // アクセシビリティのため
              >
                <Github size={20} />
              </a>
              <a 
                href="https://twitter.com" 
                target="_blank" 
                rel="noopener noreferrer"
                className="text-white/60 hover:text-white transition-colors"
                aria-label="Twitter"
              >
                <Twitter size={20} />
              </a>
              <a 
                href="mailto:contact@bold-keion.example.com"
                className="text-white/60 hover:text-white transition-colors"
                aria-label="Email"
              >
                <Mail size={20} />
              </a>
            </div>
          </div>

          {/* メニューセクション */}
          <div>
            <h4 className="text-sm font-semibold text-white uppercase tracking-wider mb-4">
              メニュー
            </h4>
            <ul className="space-y-3">
              <li>
                <Link href="/" className="text-sm text-white/60 hover:text-white transition-colors">
                  ホーム
                </Link>
              </li>
              <li>
                <Link href="/posts" className="text-sm text-white/60 hover:text-white transition-colors">
                  活動報告
                </Link>
              </li>
              <li>
                <Link href="/events" className="text-sm text-white/60 hover:text-white transition-colors">
                  イベント
                </Link>
              </li>
              <li>
                <Link href="/activity-schedules" className="text-sm text-white/60 hover:text-white transition-colors">
                  スケジュール
                </Link>
              </li>
            </ul>
          </div>

          {/* サポートセクション */}
          <div>
            <h4 className="text-sm font-semibold text-white uppercase tracking-wider mb-4">
              サポート
            </h4>
            <ul className="space-y-3">
              <li>
                <Link href="/privacy" className="text-sm text-white/60 hover:text-white transition-colors">
                  プライバシーポリシー
                </Link>
              </li>
              <li>
                <Link href="/terms" className="text-sm text-white/60 hover:text-white transition-colors">
                  利用規約
                </Link>
              </li>
            </ul>
          </div>

          {/* アカウントセクション（ログイン状態に応じて変化） */}
          <div>
            <h4 className="text-sm font-semibold text-white uppercase tracking-wider mb-4">
              アカウント
            </h4>
            <ul className="space-y-3">
              {isLoggedIn ? (
                <>
                  <li>
                    <Link href="/profile" className="text-sm text-white/60 hover:text-white transition-colors">
                      プロフィール
                    </Link>
                  </li>
                  <li>
                    <Link href="/users" className="text-sm text-white/60 hover:text-white transition-colors">
                      メンバー一覧
                    </Link>
                  </li>
                </>
              ) : (
                <>
                  <li>
                    <Link href="/auth/signin" className="text-sm text-white/60 hover:text-white transition-colors">
                      ログイン
                    </Link>
                  </li>
                  <li>
                    <Link href="/auth/signup" className="text-sm text-white/60 hover:text-white transition-colors">
                      新規登録
                    </Link>
                  </li>
                </>
              )}
            </ul>
          </div>
        </div>

        {/* コピーライト */}
        <div className="mt-12 pt-8 border-t border-white/10 text-center">
          <p className="text-sm text-white/40">
            © 2024 BOLD軽音. All rights reserved.
          </p>
        </div>
      </div>
    </footer>
  )
}
```

**実装のポイント:**

```
┌──────────────────────────────────────────────────┐
│     Footer の設計パターン                         │
└──────────────────────────────────────────────────┘

【レスポンシブグリッド】
  grid-cols-1 md:grid-cols-4
  
  → モバイル: 縦並び
  → デスクトップ: 4カラム
  → gap-8 lg:gap-12 でカラム間の余白を調整


【rel="noopener noreferrer"】
  <a target="_blank" rel="noopener noreferrer">
  
  → セキュリティのため必須
  → noopener: 新しいウィンドウが元のページにアクセスできない
  → noreferrer: リファラー情報を送信しない
  → window.opener を使った攻撃を防ぐ


【aria-label の使用】
  <a aria-label="GitHub">
    <Github size={20} />
  </a>
  
  → スクリーンリーダーのためのラベル
  → アイコンだけの場合は必須
  → アクセシビリティの向上


【条件付きレンダリング】
  {isLoggedIn ? (
    <Link href="/profile">プロフィール</Link>
  ) : (
    <Link href="/auth/signin">ログイン</Link>
  )}
  
  → ログイン状態に応じてメニューを変更
  → 動的なUI


【backdrop-blur でガラス効果】
  bg-gradient-to-b from-black/40 to-black/60 backdrop-blur-lg
  
  → 半透明の背景
  → 背景をぼかしてモダンな見た目
  → glassmorphism デザイン


【uppercase tracking-wider】
  className="uppercase tracking-wider"
  
  → 見出しを大文字に変換
  → 文字間を広げる
  → セクション見出しをより目立たせる
```

---

### 33.7.8 ScrollAnimation（src/components/ScrollAnimation.tsx）

スクロール時にアニメーションを表示するコンポーネントです。

```tsx
'use client'

import { useEffect, useRef, useState } from 'react'

/**
 * Props の型定義
 */
interface ScrollAnimationProps {
  children: React.ReactNode          // ラップする子要素
  className?: string                 // 追加のCSSクラス
  animation?: 'fade-up' | 'fade-down' | 'fade-left' | 'fade-right' | 'scale' | 'fade'  // アニメーションの種類
  delay?: number                     // アニメーション開始までの遅延（ミリ秒）
}

/**
 * ScrollAnimation コンポーネント
 * 
 * スクロールして要素が画面に入ったらアニメーション
 * - Intersection Observer API を使用
 * - 6種類のアニメーションから選択可能
 * - 遅延表示にも対応
 */
export default function ScrollAnimation({ 
  children, 
  className = '', 
  animation = 'fade-up',
  delay = 0
}: ScrollAnimationProps) {
  // 1. 要素が表示されたかどうかの状態
  const [isVisible, setIsVisible] = useState(false)
  
  // 2. 監視対象の要素への参照
  const ref = useRef<HTMLDivElement>(null)

  /**
   * Intersection Observer でスクロール監視
   */
  useEffect(() => {
    // Intersection Observer を作成
    const observer = new IntersectionObserver(
      ([entry]) => {
        // 要素が画面に入ったら
        if (entry.isIntersecting) {
          // 指定された遅延後に表示
          setTimeout(() => {
            setIsVisible(true)
          }, delay)
        }
      },
      {
        threshold: 0.1,  // 要素の10%が見えたら発火
        rootMargin: '0px 0px -50px 0px'  // 下から50px手前で発火
      }
    )

    // 要素を監視開始
    if (ref.current) {
      observer.observe(ref.current)
    }

    // クリーンアップ: コンポーネントのアンマウント時に監視を解除
    return () => {
      if (ref.current) {
        observer.unobserve(ref.current)
      }
    }
  }, [delay])

  // アニメーション開始前のスタイル（非表示状態）
  const animationClasses = {
    'fade-up': 'translate-y-10 opacity-0',      // 下から上にフェードイン
    'fade-down': '-translate-y-10 opacity-0',   // 上から下にフェードイン
    'fade-left': 'translate-x-10 opacity-0',    // 右から左にフェードイン
    'fade-right': '-translate-x-10 opacity-0',  // 左から右にフェードイン
    'scale': 'scale-95 opacity-0',              // 拡大しながらフェードイン
    'fade': 'opacity-0'                         // フェードインのみ
  }

  // アニメーション完了後のスタイル（表示状態）
  const visibleClasses = 'translate-y-0 translate-x-0 scale-100 opacity-100'

  return (
    <div
      ref={ref}
      className={`transition-all duration-700 ease-out ${
        isVisible ? visibleClasses : animationClasses[animation]
      } ${className}`}
    >
      {children}
    </div>
  )
}
```

**実装のポイント:**

```
┌──────────────────────────────────────────────────┐
│     ScrollAnimation の設計パターン                │
└──────────────────────────────────────────────────┘

【Intersection Observer API】
  const observer = new IntersectionObserver(
    (entries) => { ... },
    { threshold: 0.1 }
  )
  
  → 要素が画面に入ったかを効率的に監視
  → scroll イベントよりもパフォーマンスが良い
  → ブラウザネイティブのAPI


【threshold と rootMargin】
  threshold: 0.1   → 要素の10%が見えたら発火
  rootMargin: '0px 0px -50px 0px'  → 下から50px手前で発火
  
  → 要素が完全に見えなくてもアニメーション開始
  → ユーザーが気づく前にアニメーション完了
  → スムーズなUX


【遅延アニメーション】
  setTimeout(() => {
    setIsVisible(true)
  }, delay)
  
  → 複数要素を順番にアニメーション
  → 階段状の表示で視覚的に魅力的


【Tailwind の transition クラス】
  transition-all duration-700 ease-out
  
  → all: すべてのプロパティをアニメーション
  → duration-700: 0.7秒かけてアニメーション
  → ease-out: 最初は速く、最後はゆっくり


【条件付きクラス】
  ${isVisible ? visibleClasses : animationClasses[animation]}
  
  → 状態に応じてクラスを切り替え
  → Reactの再レンダリングでアニメーション実行


【クリーンアップ関数】
  return () => {
    observer.unobserve(ref.current)
  }
  
  → コンポーネントがアンマウントされたら監視を解除
  → メモリリークを防ぐ


【使用例】
  <ScrollAnimation animation="fade-up">
    <h2>タイトル</h2>
  </ScrollAnimation>
  
  <ScrollAnimation animation="fade-left" delay={200}>
    <p>説明文</p>
  </ScrollAnimation>
  
  <ScrollAnimation animation="scale" delay={400}>
    <img src="..." />
  </ScrollAnimation>
```

---

### 33.7.9 NavigationLink（src/components/NavigationLink.tsx）

Next.jsのページ遷移を最適化するためのリンクコンポーネントです。

```tsx
'use client'

import { useRouter } from 'next/navigation'
import { useState, useTransition } from 'react'

/**
 * Props の型定義
 */
interface NavigationLinkProps {
  href: string              // 遷移先のURL
  children: React.ReactNode // リンクのテキストや子要素
  className?: string        // 追加のCSSクラス
  onClick?: () => void      // クリック時の追加処理
}

/**
 * NavigationLink コンポーネント
 * 
 * React 18 の useTransition を使った最適化されたリンク
 * - ページ遷移中にUIを更新
 * - ローディング状態を表示
 * - スムーズな遷移
 */
export function NavigationLink({ 
  href, 
  children, 
  className = '', 
  onClick 
}: NavigationLinkProps) {
  // 1. Next.js のルーター
  const router = useRouter()
  
  // 2. React 18 の useTransition
  // isPending: トランジション中かどうか
  // startTransition: トランジションを開始する関数
  const [isPending, startTransition] = useTransition()
  
  /**
   * リンククリック時の処理
   */
  const handleClick = (e: React.MouseEvent) => {
    // 1. デフォルトの遷移を防ぐ
    e.preventDefault()
    
    // 2. 追加の onClick 処理があれば実行
    if (onClick) {
      onClick()
    }
    
    // 3. トランジションを開始してページ遷移
    startTransition(() => {
      router.push(href)
    })
  }
  
  return (
    <a
      href={href}
      onClick={handleClick}
      className={`${className} ${isPending ? 'opacity-70 cursor-wait' : ''} transition-opacity`}
    >
      {children}
      {/* ローディング中のインジケーター */}
      {isPending && (
        <span className="ml-2 inline-block animate-spin">⏳</span>
      )}
    </a>
  )
}
```

**実装のポイント:**

```
┌──────────────────────────────────────────────────┐
│     NavigationLink の設計パターン                 │
└──────────────────────────────────────────────────┘

【useTransition の使用】
  const [isPending, startTransition] = useTransition()
  
  startTransition(() => {
    router.push(href)
  })
  
  → React 18 の新機能
  → 状態更新を「トランジション」として扱う
  → UIがフリーズしない


【isPending 状態の活用】
  className={isPending ? 'opacity-70 cursor-wait' : ''}
  
  → 遷移中はリンクを半透明に
  → cursor-wait でローディング中を示す
  → ユーザーに視覚的フィードバック


【e.preventDefault() の重要性】
  const handleClick = (e: React.MouseEvent) => {
    e.preventDefault()  // デフォルトの遷移を防ぐ
    ...
  }
  
  → <a> タグのデフォルト動作をキャンセル
  → router.push() で制御された遷移を実行


【Next.js の Link との違い】
  Next.js の <Link>:
  - 自動的にプリフェッチ
  - シンプルな使用方法
  
  NavigationLink:
  - トランジション状態を管理
  - カスタムローディングUI
  - より細かい制御
  
  → 基本的には <Link> を使用
  → 特殊なUI要件がある場合に NavigationLink


【使用例】
  <NavigationLink 
    href="/posts" 
    className="text-blue-600 hover:underline"
  >
    活動報告を見る
  </NavigationLink>
  
  <NavigationLink 
    href="/events" 
    onClick={() => {
      console.log('イベントページへ遷移')
      trackEvent('navigation', 'events')
    }}
  >
    イベント一覧
  </NavigationLink>


【パフォーマンス最適化】
  startTransition を使うことで:
  - 重い処理を遅延実行
  - UIの応答性を保つ
  - ユーザー体験の向上
```

---

### 33.7.10 MarkdownToolbar（src/components/MarkdownToolbar.tsx）

Markdown エディタのツールバーコンポーネントです。Markdown 構文を簡単に挿入できます。

```tsx
'use client'

import { Bold, Italic, Heading1, Heading2, Heading3, List, ListOrdered, Quote, Link as LinkIcon } from 'lucide-react'

/**
 * Props の型定義
 */
interface MarkdownToolbarProps {
  onInsert: (before: string, after?: string) => void  // テキスト挿入のコールバック
}

/**
 * MarkdownToolbar コンポーネント
 * 
 * Markdown エディタのツールバー
 * - よく使う Markdown 構文をボタンで挿入
 * - 見出し、太字、リスト、リンクなど
 */
export default function MarkdownToolbar({ onInsert }: MarkdownToolbarProps) {
  // ツールバーのボタン定義
  const buttons = [
    { icon: Heading1, label: '見出し1', before: '# ', after: '' },
    { icon: Heading2, label: '見出し2', before: '## ', after: '' },
    { icon: Heading3, label: '見出し3', before: '### ', after: '' },
    { icon: Bold, label: '太字', before: '**', after: '**' },
    { icon: Italic, label: '斜体', before: '*', after: '*' },
    { icon: List, label: '箇条書き', before: '- ', after: '' },
    { icon: ListOrdered, label: '番号付きリスト', before: '1. ', after: '' },
    { icon: Quote, label: '引用', before: '> ', after: '' },
    { icon: LinkIcon, label: 'リンク', before: '[', after: '](url)' },
  ]

  return (
    <div className="flex flex-wrap gap-1 p-2 bg-gray-50 border border-gray-300 rounded-t-lg">
      {buttons.map((button) => {
        const Icon = button.icon
        return (
          <button
            key={button.label}
            type="button"
            onClick={() => onInsert(button.before, button.after)}
            className="p-2 hover:bg-gray-200 rounded transition-colors"
            title={button.label}     // ツールチップ
            aria-label={button.label} // アクセシビリティ
          >
            <Icon size={18} className="text-gray-700" />
          </button>
        )
      })}
    </div>
  )
}
```

**実装のポイント:**

```
┌──────────────────────────────────────────────────┐
│     MarkdownToolbar の設計パターン                │
└──────────────────────────────────────────────────┘

【ツールバーの構造】
  buttons: Array<{ icon, label, before, after }>
  
  → 各ボタンの定義を配列で管理
  → map() で動的にボタンを生成
  → 新しいボタンの追加が簡単


【before と after の使い分け】
  太字: before='**', after='**'
    → 選択範囲を **text** で囲む
  
  見出し: before='# ', after=''
    → 行頭に # を挿入


【親コンポーネントでのテキスト挿入処理】
  const handleInsert = (before: string, after?: string) => {
    const textarea = textareaRef.current
    if (!textarea) return
    
    const start = textarea.selectionStart
    const end = textarea.selectionEnd
    const text = textarea.value
    const selectedText = text.substring(start, end)
    
    // 選択範囲の前後に before と after を挿入
    const newText = 
      text.substring(0, start) + 
      before + 
      selectedText + 
      (after || '') + 
      text.substring(end)
    
    // テキストを更新
    onChange(newText)
    
    // カーソル位置を調整
    setTimeout(() => {
      textarea.focus()
      textarea.setSelectionRange(
        start + before.length,
        end + before.length
      )
    }, 0)
  }


【アクセシビリティ】
  title={button.label}
    → ホバー時にツールチップ表示
  
  aria-label={button.label}
    → スクリーンリーダー対応


【使用例（エディタとの統合）】
  export default function PostEditor() {
    const textareaRef = useRef<HTMLTextAreaElement>(null)
    const [content, setContent] = useState('')
    
    const handleInsert = (before: string, after?: string) => {
      // テキスト挿入処理...
    }
    
    return (
      <div>
        <MarkdownToolbar onInsert={handleInsert} />
        <textarea 
          ref={textareaRef}
          value={content}
          onChange={(e) => setContent(e.target.value)}
        />
      </div>
    )
  }


【拡張性】
  将来的に追加できる機能:
  - コードブロック: ```code```
  - 画像挿入: ![alt](url)
  - 水平線: ---
  - テーブル: | col1 | col2 |
  - チェックリスト: - [ ] Task
```

---

## 📋 共通コンポーネントのまとめ

この章で解説した共通コンポーネント一覧：

```
┌──────────────────────────────────────────────────┐
│     実装された共通コンポーネント                 │
└──────────────────────────────────────────────────┘

1. Providers
   → NextAuth.js の SessionProvider を提供

2. DashboardLayout
   → ナビゲーションバー、モバイルメニュー、フッター

3. RichTextEditor
   → Markdown テキスト入力エディタ

4. MarkdownToolbar
   → Markdown 構文挿入ツールバー

5. AvatarUpload
   → アバター画像のアップロード・削除

6. TemplateEditor
   → 報告書テンプレート編集モーダル

7. LoadingSpinner
   → ローディング状態の表示

8. Footer
   → サイト全体のフッター

9. ScrollAnimation
   → スクロールアニメーション

10. NavigationLink
    → 最適化されたページ遷移リンク


【共通パターン】

✅ 'use client' ディレクティブ
  → インタラクティブな機能は Client Component

✅ TypeScript の型定義
  → Props の型を明確に定義

✅ アクセシビリティ
  → aria-label, title 属性の使用

✅ レスポンシブデザイン
  → モバイルファーストアプローチ

✅ 再利用性
  → props でカスタマイズ可能

✅ パフォーマンス
  → 最適化されたレンダリング
```

---

### 🚀 次のステップ

次は付録で、用語集、コマンドリファレンス、参考リソースを提供します：

- **付録 A**: 用語集
- **付録 B**: よく使うコマンド
- **付録 C**: 参考リソース
- **付録 D**: コード索引

---

[← 前の章：第32章 ページコンポーネントの詳細解説](32-ページコンポーネントの詳細解説.md) | [目次に戻る](00-目次.md) | [次の章へ：付録A 用語集 →](付録A-用語集.md)

# 第32章：Next.js App Routerページ実装の詳細

> **実際のプロジェクトコードをベースに、各ページの実装パターンを詳細解説**

## 📚 この章の目的

この章は**実装リファレンス**として、各ページコンポーネントの仕組みを深く理解するためのものです。

- ✅ Server Component と Client Component の実装パターン  
- ✅ データ取得とリアルタイム更新の実装
- ✅ ユーザーインタラクションとフォーム処理
- ✅ 楽観的UI更新（Optimistic UI）
- ✅ 認証ガードとアクセス制御

## 💡 プロジェクトのページ構成

```
┌──────────────────────────────────────────────────┐
│     実装されているページ一覧                      │
└──────────────────────────────────────────────────┘

【ルートページ】
1. src/app/page.tsx + HomeClient.tsx
   → トップページ（Server + Client Component パターン）
2. src/app/layout.tsx
   → ルートレイアウト（メタデータ、Providers）

【投稿関連】（統合ページ方式）
3. src/app/posts/page.tsx
   → 投稿一覧・編集・削除・コメント（すべて統合）

【イベント関連】
4. src/app/events/page.tsx
   → イベント一覧・作成・編集・参加登録
5. src/app/events/[id]/report/page.tsx
   → イベントから活動報告を作成

【スケジュール関連】
6. src/app/activity-schedules/page.tsx
   → スケジュール一覧・作成・投票

【ユーザー関連】
7. src/app/users/page.tsx
   → ユーザー一覧（管理者のみ）
8. src/app/users/[id]/page.tsx
   → ユーザー詳細
9. src/app/profile/page.tsx
   → プロフィール編集

【認証関連】
10. src/app/auth/signin/page.tsx
    → ログイン（メール/Google OAuth）
11. src/app/auth/signup/page.tsx
    → 新規登録（メール/Google OAuth）
12. src/app/auth/forgot-password/page.tsx
    → パスワードリセット依頼
13. src/app/auth/reset-password/page.tsx
    → パスワードリセット実行

【投稿編集関連】
14. src/app/posts/[id]/edit/page.tsx
    → 投稿編集ページ（管理者のみ）

【スケジュール報告関連】
15. src/app/activity-schedules/[id]/report/page.tsx
    → スケジュールから活動報告を作成
```

**設計思想：統合ページアプローチ**

このプロジェクトでは、投稿の一覧・作成・編集・削除を **1つのページ** に統合する設計を採用しています。

```
┌──────────────────────────────────────────────────┐
│     従来型 vs 統合型                              │
└──────────────────────────────────────────────────┘

❌ 従来型（ページ分離）
posts/
├─ page.tsx        # 一覧
├─ new/
│  └─ page.tsx     # 新規作成
└─ [id]/
   ├─ page.tsx     # 詳細
   └─ edit/
      └─ page.tsx  # 編集

✅ このプロジェクト（統合型）
posts/
└─ page.tsx        # 一覧 + 編集 + 削除
                  # 詳細はモーダルまたは展開表示
                  # コメント・いいね機能も同ページ

【統合型の利点】
✅ ページ遷移なしで操作完結（UX向上）
✅ ステート管理が簡単（1つのコンポーネント内）
✅ リアルタイムUI更新が高速
✅ 楽観的UI更新の実装が容易

【注意点】
⚠️ コンポーネントが大きくなる
   → セクションごとに分割して管理
⚠️ 権限チェックが複雑
   → カスタムHookで共通化
```

---

## 32.1 トップページ（Server + Client Component パターン）

### アーキテクチャ：分離パターン

```
┌─────────────────────────────────────────────────────────┐
│    ホームページの構成（2ファイル構造）                  │
└─────────────────────────────────────────────────────────┘

src/app/page.tsx (Server Component)
  ↓ サーバー側でデータ取得
  ├─ await auth() でセッション取得
  ├─ Prisma で投稿データ取得（最新3件、人気3件）
  ├─ ISR (revalidate = 60) でキャッシュ制御
  └─ HomeClient に初期データを渡す

  ↓ propsで渡す

HomeClient.tsx (Client Component)
  ↓ ブラウザ側でインタラクション
  ├─ ScrollAnimation でアニメーション
  ├─ タブ切り替え（最新/人気）
  ├─ YouTube動画の表示
  └─ ログイン/登録ボタン表示制御
```

### src/app/page.tsx（Server Component）

```typescript
import Link from 'next/link'
import { Music, Calendar, FileText, Users } from 'lucide-react'
import { auth } from '@/lib/auth'
import { PrismaClient } from '@prisma/client'
import ScrollAnimation from '@/components/ScrollAnimation'
import HomeClient from './HomeClient'
import Footer from '@/components/Footer'

const prisma = new PrismaClient()

// ISR: 60秒ごとにページを再生成
export const revalidate = 60

export default async function Home() {
  const session = await auth()
  
  // サーバーサイドでデータ取得
  const allPosts = await prisma.post.findMany({
    include: {
      user: {
        select: {
          id: true,
          name: true,
          email: true,
          avatarUrl: true,
        },
      },
      likes: {
        select: {
          userId: true,
          createdAt: true,
        },
      },
    },
    orderBy: {
      createdAt: 'desc',
    },
  })

  // JSON化（Date型をstringに変換）
  const posts = allPosts.slice(0, 3).map((post) => ({
    ...post,
    content: post.content || '',
    title: post.title || '',
    createdAt: post.createdAt.toISOString(),
    user: {
      id: post.user.id,
      name: post.user.name || '',
      email: post.user.email || '',
      avatarUrl: post.user.avatarUrl || null,
    },
    likes: post.likes.map((like) => ({
      userId: like.userId,
      createdAt: like.createdAt.toISOString(),
    })),
  }))

  // いいね数でソート
  const sorted = [...allPosts].sort((a, b) => b.likes.length - a.likes.length)
  const popularPosts = sorted.slice(0, 3).filter(p => p.likes.length > 0).map((post) => ({
    ...post,
    content: post.content || '',
    title: post.title || '',
    createdAt: post.createdAt.toISOString(),
    user: {
      id: post.user.id,
      name: post.user.name || '',
      email: post.user.email || '',
      avatarUrl: post.user.avatarUrl || null,
    },
    likes: post.likes.map((like) => ({
      userId: like.userId,
      createdAt: like.createdAt.toISOString(),
    })),
  }))

  return (
    <div className="min-h-screen bg-linear-to-br from-slate-900 via-purple-900 to-slate-900">
      {/* Hero Section */}
      <div className="relative overflow-hidden h-[50vh]">
        {/* 背景アニメーション */}
        <div className="absolute inset-0">
          <div className="absolute w-[200%] h-full bg-cover bg-center opacity-40 animate-slide"></div>
          <div className="absolute inset-0 bg-linear-to-b from-slate-900/80 to-slate-900/90"></div>
        </div>
        
        <div className="container mx-auto px-4 py-20 relative">
          <ScrollAnimation animation="fade-up" delay={0}>
            <div className="text-center space-y-4">
              <Music className="w-16 h-16 text-white mx-auto" />
              <h1 className="text-6xl md:text-7xl font-extrabold text-transparent bg-clip-text bg-linear-to-r from-blue-400 via-purple-400 to-pink-400">
                BOLD 軽音
              </h1>
              <p className="text-3xl text-transparent bg-clip-text bg-linear-to-r from-purple-400 to-blue-400">
                メンバーサイト
              </p>
            </div>
          </ScrollAnimation>
        </div>
      </div>

      {/* メニューセクション */}
      <div className="container mx-auto px-4 py-16">
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
          <Link href="/posts" className="group bg-white/10 backdrop-blur-md rounded-2xl p-8 hover:scale-105">
            <FileText className="w-16 h-16 text-blue-400 mb-4" />
            <h3 className="text-2xl font-bold text-white mb-3">活動報告</h3>
            <p className="text-white/70">練習の成果や演奏動画を共有</p>
          </Link>

          <Link href="/events" className="group bg-white/10 backdrop-blur-md rounded-2xl p-8 hover:scale-105">
            <Music className="w-16 h-16 text-purple-400 mb-4" />
            <h3 className="text-2xl font-bold text-white mb-3">イベント</h3>
            <p className="text-white/70">ライブや発表会の情報管理</p>
          </Link>

          <Link href="/activity-schedules" className="group bg-white/10 backdrop-blur-md rounded-2xl p-8 hover:scale-105">
            <Calendar className="w-16 h-16 text-green-400 mb-4" />
            <h3 className="text-2xl font-bold text-white mb-3">活動スケジュール</h3>
            <p className="text-white/70">練習日程を簡単に調整</p>
          </Link>

          {session?.user?.role === 'site_admin' && (
            <Link href="/users" className="group bg-white/10 backdrop-blur-md rounded-2xl p-8 hover:scale-105">
              <Users className="w-16 h-16 text-orange-400 mb-4" />
              <h3 className="text-2xl font-bold text-white mb-3">ユーザー管理</h3>
              <p className="text-white/70">メンバー情報を管理</p>
            </Link>
          )}
        </div>
      </div>

      {/* Client Componentに投稿データを渡す */}
      <HomeClient posts={posts} popularPosts={popularPosts} />

      <Footer isSiteAdmin={session?.user?.role === 'site_admin'} isLoggedIn={!!session} />
    </div>
  )
}
```

**重要なポイント：**

```
┌──────────────────────────────────────────────────┐
│     Server Component の特徴                       │
└──────────────────────────────────────────────────┘

【1. async/await が使える】
  export default async function Home() {
    const data = await prisma...
  }
  → トップレベルでawaitが使える（Server Componentのみ）

【2. ISR (Incremental Static Regeneration)】
  export const revalidate = 60
  → 60秒ごとにページを再生成
  → 最初のリクエストはキャッシュから返す（高速）

【3. Prismaの直接利用】
  const prisma = new PrismaClient()
  const data = await prisma.post.findMany(...)
  → API Routeを経由せず、直接データベースにアクセス

【4. auth()の直接呼び出し】
  const session = await auth()
  → サーバー側でセッション情報を取得
  → クライアント側の useSession() より高速

【5. Date型のJSON化】
  createdAt: post.createdAt.toISOString()
  → Prisma の Date は JSON にシリアライズできない
  → 文字列に変換してClient Componentに渡す
```

---

## 32.2 投稿ページ（統合型実装）

### 設計: 1つのページにすべての機能を統合

```
┌──────────────────────────────────────────────────┐
│     posts/page.tsx の機能一覧                     │
└──────────────────────────────────────────────────┘

✅ 投稿一覧表示
✅ 投稿編集（管理者のみ）
✅ 投稿削除（管理者のみ）
✅ いいね機能（楽観的UI更新）
✅ コメント投稿・表示
✅ コメント削除（本人・管理者のみ）
✅ YouTube動画埋め込み（複数対応）
✅ 画像表示
✅ 参加者表示
✅ ページネーション
✅ コピー機能（LINE等での共有用）

→ すべて1つのClient Componentで実装
```

### src/app/posts/page.tsx（抜粋）

<details>
<summary>フルコードを表示（846行）</summary>

```typescript
'use client'

import { useState, useEffect, useCallback } from 'react'
import YouTube from 'react-youtube'
import { useSession } from 'next-auth/react'
import { Edit, Heart, MessageCircle, Send, Trash2, Copy, Check } from 'lucide-react'
import Link from 'next/link'
import Image from 'next/image'
import DashboardLayout from '@/components/DashboardLayout'
import LoadingSpinner from '@/components/LoadingSpinner'

interface Post {
  id: string
  title: string
  content: string
  youtubeUrls: string[]
  images?: string[]
  createdAt: string
  userId: string
  user: {
    id: string
    name: string
    email: string
    avatarUrl?: string | null
  }
  participants: {
    id: string
    status: string
    user: {
      id: string
      name: string
      email: string
    }
  }[]
  likes: {
    userId: string
    createdAt: string
  }[]
  comments?: {
    id: string
    content: string
    createdAt: string
    user: {
      id: string
      name: string
      email: string
    }
  }[]
  _count?: {
    comments: number
  }
}

const POSTS_PER_PAGE = 5

export default function PostsPage() {
  const { data: session } = useSession()
  const [posts, setPosts] = useState<Post[]>([])
  const [currentPage, setCurrentPage] = useState(1)
  const [fetchingPosts, setFetchingPosts] = useState(true)
  const [newComment, setNewComment] = useState<{ [postId: string]: string }>({})
  const [submittingComment, setSubmittingComment] = useState<string | null>(null)
  const [copiedPostId, setCopiedPostId] = useState<string | null>(null)

  const isAdmin = session?.user?.role === 'admin' || session?.user?.role === 'site_admin'

  useEffect(() => {
    fetchPosts()
  }, [])

  const fetchPosts = useCallback(async () => {
    setFetchingPosts(true)
    try {
      const res = await fetch('/api/posts')
      const data = await res.json()
      setPosts(data)
    } catch (error) {
      console.error('Failed to fetch posts:', error)
    } finally {
      setFetchingPosts(false)
    }
  }, [])

  // いいね機能（楽観的UI更新）
  const handleLike = async (postId: string) => {
    if (!session?.user?.id) return
    
    // 先に画面を更新
    setPosts(prevPosts => prevPosts.map(post => {
      if (post.id === postId) {
        return {
          ...post,
          likes: [...post.likes, { userId: session.user.id, createdAt: new Date().toISOString() }]
        }
      }
      return post
    }))

    try {
      const res = await fetch(`/api/posts/${postId}/like`, {
        method: 'POST',
      })
      
      if (!res.ok) {
        fetchPosts()  // 失敗したら元に戻す
      }
    } catch (error) {
      fetchPosts()
    }
  }

  const handleUnlike = async (postId: string) => {
    if (!session?.user?.id) return
    
    setPosts(prevPosts => prevPosts.map(post => {
      if (post.id === postId) {
        return {
          ...post,
          likes: post.likes.filter(like => like.userId !== session.user.id)
        }
      }
      return post
    }))

    try {
      const res = await fetch(`/api/posts/${postId}/like`, {
        method: 'DELETE',
      })
      
      if (!res.ok) {
        fetchPosts()
      }
    } catch (error) {
      fetchPosts()
    }
  }

  // コメント投稿
  const handleCommentSubmit = async (postId: string) => {
    const commentContent = newComment[postId]?.trim()
    if (!commentContent || submittingComment) return

    setSubmittingComment(postId)

    try {
      const res = await fetch(`/api/posts/${postId}/comments`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ content: commentContent })
      })

      if (!res.ok) {
        alert('コメントの投稿に失敗しました')
        return
      }

      const newCommentData = await res.json()
      
      // コメントをローカルステートに追加
      setPosts(prevPosts => prevPosts.map(post => {
        if (post.id === postId) {
          return {
            ...post,
            comments: [...(post.comments || []), newCommentData]
          }
        }
        return post
      }))

      setNewComment(prev => ({ ...prev, [postId]: '' }))
    } catch (error) {
      console.error('Failed to post comment:', error)
      alert('コメントの投稿に失敗しました')
    } finally {
      setSubmittingComment(null)
    }
  }

  // コピー機能（LINE等での共有用）
  const handleCopyContent = async (postId: string, content: string, youtubeUrls?: string[]) => {
    try {
      const tempDiv = document.createElement('div')
      tempDiv.innerHTML = content
      const textContent = tempDiv.textContent || tempDiv.innerText || ''
      
      let copyText = textContent
      
      if (youtubeUrls && youtubeUrls.length > 0) {
        copyText += '\n\n【軽音動画】\n'
        youtubeUrls.forEach((url, index) => {
          copyText += `${index + 1}. ${url}\n`
        })
      }
      
      const pageUrl = `${window.location.origin}/posts`
      copyText += `\n【大阪軽音部WebサイトURL】\n${pageUrl}`
      
      await navigator.clipboard.writeText(copyText)
      setCopiedPostId(postId)
      setTimeout(() => setCopiedPostId(null), 2000)
    } catch (error) {
      console.error('コピーに失敗しました:', error)
      alert('コピーに失敗しました')
    }
  }

  const extractYouTubeId = (url: string) => {
    const match = url.match(/(?:youtube\.com\/(?:watch\?v=|live\/|shorts\/|embed\/)|youtu\.be\/)([^&\n?#]+)/)
    return match ? match[1] : null
  }

  // ページネーション
  const totalPages = Math.ceil(posts.length / POSTS_PER_PAGE)
  const startIndex = (currentPage - 1) * POSTS_PER_PAGE
  const endIndex = startIndex + POSTS_PER_PAGE
  const currentPosts = posts.slice(startIndex, endIndex)

  // ログイン済みユーザー向けUI
  if (session) {
    return (
      <DashboardLayout>
        <div className="max-w-4xl mx-auto p-6">
          <h1 className="text-3xl font-bold mb-8 text-white">活動一覧</h1>

          {fetchingPosts ? (
            <LoadingSpinner size="lg" />
          ) : (
            <>
              {/* 投稿一覧 */}
              <div className="space-y-6 mb-8">
                {currentPosts.map((post) => (
                  <div key={post.id} className="bg-white/10 backdrop-blur-md rounded-2xl shadow-xl p-6 border border-white/10">
                    {/* 投稿ヘッダー */}
                    <div className="flex justify-between items-start mb-4">
                      <div className="flex items-center gap-3">
                        <Link href={`/users/${post.userId}`}>
                          {post.user.avatarUrl ? (
                            <Image
                              src={post.user.avatarUrl}
                              alt={post.user.name || 'User'}
                              width={40}
                              height={40}
                              className="w-10 h-10 rounded-full object-cover"
                            />
                          ) : (
                            <div className="w-10 h-10 rounded-full bg-gradient-to-br from-blue-400 to-purple-500"></div>
                          )}
                        </Link>
                        <div>
                          <h2 className="text-2xl font-bold text-white">{post.title}</h2>
                          <p className="text-sm text-white/60">
                            {post.user.name} / {new Date(post.createdAt).toLocaleDateString('ja-JP')}
                          </p>
                        </div>
                      </div>
                      {isAdmin && (
                        <div className="flex gap-2">
                          <button onClick={() => handleEdit(post)} className="text-blue-600 hover:bg-blue-50 p-2 rounded">
                            <Edit className="w-5 h-5" />
                          </button>
                          <button onClick={() => handleDelete(post.id)} className="text-red-600 hover:bg-red-50 p-2 rounded">
                            <Trash2 className="w-5 h-5" />
                          </button>
                        </div>
                      )}
                    </div>

                    {/* 投稿内容 */}
                    {post.content && (
                      <div className="flex justify-between gap-2 mb-4">
                        <div className="prose prose-invert max-w-none" dangerouslySetInnerHTML={{ __html: post.content }} />
                        <button
                          onClick={() => handleCopyContent(post.id, post.content, post.youtubeUrls)}
                          className="p-2 bg-white/10 hover:bg-white/20 text-white rounded-lg"
                        >
                          {copiedPostId === post.id ? <Check className="w-4 h-4 text-green-400" /> : <Copy className="w-4 h-4" />}
                        </button>
                      </div>
                    )}

                    {/* YouTube動画 */}
                    {post.youtubeUrls && post.youtubeUrls.length > 0 && (
                      <div className="space-y-4 mb-4">
                        {post.youtubeUrls.map((url, index) => {
                          const youtubeId = extractYouTubeId(url)
                          return youtubeId ? (
                            <div key={index} className="aspect-video rounded-lg overflow-hidden">
                              <YouTube videoId={youtubeId} opts={{ width: '100%', height: '100%' }} />
                            </div>
                          ) : null
                        })}
                      </div>
                    )}

                    {/* いいねボタン */}
                    <div className="flex items-center gap-4 pt-4 border-t">
                      <button
                        onClick={() => post.likes.some(l => l.userId === session.user.id) ? handleUnlike(post.id) : handleLike(post.id)}
                        className={`flex items-center gap-2 px-4 py-2 rounded-lg ${
                          post.likes.some(l => l.userId === session.user.id)
                            ? 'bg-red-500/20 text-red-400'
                            : 'bg-white/10 text-white'
                        }`}
                      >
                        <Heart className={post.likes.some(l => l.userId === session.user.id) ? 'fill-current' : ''} />
                        <span>{post.likes.length}</span>
                      </button>
                      <div className="flex items-center gap-2 text-white/70">
                        <MessageCircle className="w-5 h-5" />
                        <span>{(post.comments || []).length}</span>
                      </div>
                    </div>

                    {/* コメントセクション */}
                    <div className="mt-4 pt-4 border-t">
                      <h3 className="font-semibold mb-3">コメント</h3>
                      
                      {/* コメント一覧 */}
                      {(post.comments || []).length > 0 && (
                        <div className="space-y-3 mb-4">
                          {(post.comments || []).map((comment) => (
                            <div key={comment.id} className="bg-white/5 rounded-lg p-3">
                              <div className="flex justify-between">
                                <div>
                                  <div className="font-semibold text-sm text-white">{comment.user.name}</div>
                                  <p className="text-white/80 mt-1">{comment.content}</p>
                                  <span className="text-xs text-white/50">
                                    {new Date(comment.createdAt).toLocaleString('ja-JP')}
                                  </span>
                                </div>
                              </div>
                            </div>
                          ))}
                        </div>
                      )}

                      {/* コメント入力 */}
                      <div className="flex gap-2">
                        <input
                          type="text"
                          value={newComment[post.id] || ''}
                          onChange={(e) => setNewComment(prev => ({ ...prev, [post.id]: e.target.value }))}
                          onKeyPress={(e) => {
                            if (e.key === 'Enter' && !e.shiftKey) {
                              e.preventDefault()
                              handleCommentSubmit(post.id)
                            }
                          }}
                          placeholder="コメントを入力..."
                          className="flex-1 px-4 py-2 border border-white/20 rounded-lg bg-white/5 text-white"
                          disabled={submittingComment === post.id}
                        />
                        <button
                          onClick={() => handleCommentSubmit(post.id)}
                          disabled={!newComment[post.id]?.trim() || submittingComment === post.id}
                          className="p-2 bg-gradient-to-r from-blue-600 to-purple-600 text-white rounded-lg hover:scale-105"
                        >
                          <Send className="w-5 h-5" />
                        </button>
                      </div>
                    </div>
                  </div>
                ))}
              </div>

              {/* ページネーション */}
              {totalPages > 1 && (
                <div className="flex justify-center items-center gap-2 mt-8">
                  <button
                    onClick={() => setCurrentPage(prev => Math.max(1, prev - 1))}
                    disabled={currentPage === 1}
                    className="p-2 rounded-lg border border-white/20 bg-white/10 text-white hover:bg-white/20 disabled:opacity-50"
                  >
                    &larr;
                  </button>
                  <span className="px-4 text-white">
                    {currentPage} / {totalPages}
                  </span>
                  <button
                    onClick={() => setCurrentPage(prev => Math.min(totalPages, prev + 1))}
                    disabled={currentPage === totalPages}
                    className="p-2 rounded-lg border border-white/20 bg-white/10 text-white hover:bg-white/20 disabled:opacity-50"
                  >
                    &rarr;
                  </button>
                </div>
              )}
            </>
          )}
        </div>
      </DashboardLayout>
    )
  }

  // 未ログインユーザー向けUI（公開ビュー）
  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900">
      {/* 公開ビューの実装は省略... */}
    </div>
  )
}
```

</details>

**重要な実装パターン：**

```
┌──────────────────────────────────────────────────┐
│     投稿ページの設計ポイント                     │
└──────────────────────────────────────────────────┘

【1. 楽観的UI更新（Optimistic UI）】
  ユーザーアクションに即座に反応

  const handleLike = async (postId: string) => {
    // ① 先に画面を更新（楽観的更新）
    setPosts(/* 更新されたデータ */)
    
    // ② その後APIリクエスト
    const res = await fetch('/api/posts/${postId}/like', { method: 'POST' })
    
    // ③ 失敗したら元に戻す
    if (!res.ok) {
      fetchPosts()
    }
  }

  ✅ メリット:
    - 即座に反応する（UX向上）
    - ネットワーク遅延を感じさせない
  
  ⚠️ 注意点:
    - 失敗時のロールバックが必要
    - 楽観的更新と実データの同期を保つ


【2. オブジェクト形式のステート管理】
  複数の投稿それぞれにステートを持つ

  const [newComment, setNewComment] = useState<{ [postId: string]: string }>({})
  
  // 投稿ごとのコメント入力を管理
  <input value={newComment[post.id] || ''} />
  
  // 更新
  setNewComment({ ...newComment, [postId]: 'new value' })


【3. 条件付きレンダリング】
  {post.youtubeUrls && post.youtubeUrls.length > 0 && (
    <div>YouTube動画</div>
  )}
  
  {isAdmin && (
    <button>編集</button>
  )}


【4. ページネーション】
  const startIndex = (currentPage - 1) * POSTS_PER_PAGE
  const endIndex = startIndex + POSTS_PER_PAGE
  const currentPosts = posts.slice(startIndex, endIndex)
  
  totalPages = Math.ceil(posts.length / POSTS_PER_PAGE)


【5. HTML文字列の表示】
  dangerouslySetInnerHTML={{ __html: post.content }}
  
  ⚠️ XSS対策必須:
    - サーバー側でサニタイズ
    - 信頼できるコンテンツのみ使用


【6. 複数YouTube URL対応】
  {post.youtubeUrls.map((url, index) => {
    const youtubeId = extractYouTubeId(url)
    return youtubeId ? <YouTube videoId={youtubeId} /> : null
  })}
  
  → /watch, /live/, /shorts/, /embed/, youtu.be/ すべて対応
```

---

## 32.3 イベントページ（統合型実装）

### src/app/events/page.tsx（抜粋）

このページもすべての機能を1つに統合しています。

```
┌──────────────────────────────────────────────────┐
│     events/page.tsx の機能一覧                    │
└──────────────────────────────────────────────────┘

✅ イベント一覧表示
✅ イベント作成・編集（管理者のみ）
✅ 課題曲管理（複数対応、YouTube検索、パート割当）
✅ 参加登録・取り消し
✅ コメント投稿・表示
✅ イベントから活動報告を作成
✅ コピー機能（LINEでの共有用）
```

**課題曲管理の実装例：**

```typescript
// 課題曲の型定義
interface Song {
  title: string
  sheetUrl?: string
  youtubeUrl?: string
  parts: Array<{ instrument: string; player: string }>
}

// フォーム状態
const [formData, setFormData] = useState({
  title: '',
  content: '',
  date: '',
  locationName: '',
  locationUrl: '',
  songs: [] as Song[]
})

// 課題曲を追加
const addSong = () => {
  setFormData({
    ...formData,
    songs: [...formData.songs, { title: '', sheetUrl: '', youtubeUrl: '', parts: [] }]
  })
}

// 課題曲の曲名を自動でYouTube検索
const handleSongTitleBlur = async (songIndex: number, title: string) => {
  if (!title || formData.songs[songIndex].youtubeUrl) return
  
  try {
    const res = await fetch(`/api/youtube/search?q=${encodeURIComponent(title)}`)
    if (res.ok) {
      const data = await res.json()
      if (data.url) {
        const newSongs = [...formData.songs]
        newSongs[songIndex].youtubeUrl = data.url
        setFormData({ ...formData, songs: newSongs })
      }
    }
  } catch (error) {
    console.error('YouTube検索エラー:', error)
  }
}

// パート割当
const addPart = (songIndex: number) => {
  const newSongs = [...formData.songs]
  newSongs[songIndex].parts.push({ instrument: 'vocal', player: '' })
  setFormData({ ...formData, songs: newSongs })
}

// パート更新
const updatePart = (
  songIndex: number,
  partIndex: number,
  field: 'instrument' | 'player',
  value: string
) => {
  const newSongs = [...formData.songs]
  newSongs[songIndex].parts[partIndex][field] = value
  setFormData({ ...formData, songs: newSongs })
}

// フォームUI
<form onSubmit={handleSubmit}>
  {/* 課題曲セクション */}
  <div>
    <h3>課題曲</h3>
    {formData.songs.map((song, songIndex) => (
      <div key={songIndex}>
        {/* 曲名入力（自動YouTube検索） */}
        <input
          type="text"
          value={song.title}
          onChange={(e) => updateSong(songIndex, 'title', e.target.value)}
          onBlur={(e) => handleSongTitleBlur(songIndex, e.target.value)}
          placeholder="曲名"
        />
        
        {/* 楽譜URL */}
        <input
          type="url"
          value={song.sheetUrl}
          onChange={(e) => updateSong(songIndex, 'sheetUrl', e.target.value)}
          placeholder="楽譜URL"
        />
        
        {/* YouTube URL */}
        <input
          type="url"
          value={song.youtubeUrl}
          onChange={(e) => updateSong(songIndex, 'youtubeUrl', e.target.value)}
          placeholder="YouTube URL"
        />
        
        {/* パート割当 */}
        <div>
          <h4>パート割当</h4>
          {song.parts.map((part, partIndex) => (
            <div key={partIndex} className="flex gap-2">
              <select
                value={part.instrument}
                onChange={(e) => updatePart(songIndex, partIndex, 'instrument', e.target.value)}
              >
                <option value="vocal">🎤 ボーカル</option>
                <option value="electric_guitar">🎸 エレキギター</option>
                <option value="bass">🎸 ベース</option>
                <option value="drums">🥁 ドラム</option>
                <option value="keyboard">🎹 キーボード</option>
                <option value="other">🎵 その他</option>
              </select>
              <input
                type="text"
                value={part.player}
                onChange={(e) => updatePart(songIndex, partIndex, 'player', e.target.value)}
                placeholder="担当者名"
              />
              <button type="button" onClick={() => removePart(songIndex, partIndex)}>削除</button>
            </div>
          ))}
          <button type="button" onClick={() => addPart(songIndex)}>+ パート追加</button>
        </div>
      </div>
    ))}
    <button type="button" onClick={addSong}>+ 課題曲追加</button>
  </div>
</form>
```

**課題曲データのJSON保存:**

```typescript
// データベーススキーマ (Prisma)
model Event {
  id        String   @id @default(cuid())
  title     String
  content   String
  date      DateTime
  songs     String?  // JSON文字列として保存
  // ...
}

// 保存時
const res = await fetch('/api/events', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    ...formData,
    songs: JSON.stringify(formData.songs)  // JSON文字列化
  })
})

// 取得時
const event = await prisma.event.findUnique({ where: { id } })
const songs = event.songs ? JSON.parse(event.songs) : []
```

---

## 32.4 スケジュール・ユーザー管理ページ

### src/app/activity-schedules/page.tsx

複数の候補日を持つスケジュールを管理します。

```typescript
// 楽観的UI更新の例（参加登録）
const handleParticipate = async (scheduleId: string) => {
  if (!session?.user?.id) return

  const userId = session.user.id
  const isCurrentlyParticipating = schedules.find(s => s.id === scheduleId)
    ?.participants.some(p => p.userId === userId)

  // ① 先に画面を更新
  setSchedules(prevSchedules => prevSchedules.map(s => {
    if (s.id === scheduleId) {
      if (isCurrentlyParticipating) {
        return {
          ...s,
          participants: s.participants.filter(p => p.userId !== userId)
        }
      } else {
        return {
          ...s,
          participants: [...s.participants, {
            id: 'temp-' + Date.now(),  // 一時ID
            userId,
            user: {
              id: userId,
              name: session.user.name || '',
              email: session.user.email || ''
            },
            createdAt: new Date().toISOString()
          }]
        }
      }
    }
    return s
  }))

  // ② APIリクエスト
  try {
    const res = await fetch(`/api/activity-schedules/${scheduleId}/participate`, {
      method: 'POST'
    })

    if (!res.ok) {
      // 失敗したら元に戻す
      fetchSchedules()
    }
  } catch (error) {
    console.error('参加登録エラー:', error)
    fetchSchedules()
  }
}
```

### src/app/users/page.tsx（管理者のみ）

```typescript
'use client'

import { useState, useEffect } from 'react'
import { useSession } from 'next-auth/react'
import { useRouter } from 'next/navigation'
import DashboardLayout from '@/components/DashboardLayout'

export default function UsersPage() {
  const { data: session, status } = useSession()
  const router = useRouter()
  const [users, setUsers] = useState([])
  const [loading, setLoading] = useState(true)

  const isSiteAdmin = session?.user?.role === 'site_admin'

  // 認証チェック
  useEffect(() => {
    if (status === 'loading') return
    
    if (!session || !isSiteAdmin) {
      router.replace('/')  // 権限がない場合はトップページへ
      return
    }
    
    fetchUsers()
  }, [session, isSiteAdmin, router, status])

  const fetchUsers = async () => {
    try {
      const res = await fetch('/api/users')
      if (res.ok) {
        const data = await res.json()
        setUsers(data)
      }
    } catch (error) {
      console.error('Failed to fetch users:', error)
    } finally {
      setLoading(false)
    }
  }

  // 役割変更（楽観的UI更新）
  const confirmRoleChange = async (userId: string, newRole: string) => {
    // 先に画面を更新
    setUsers(prevUsers => prevUsers.map(u => 
      u.id === userId ? { ...u, role: newRole } : u
    ))

    try {
      const res = await fetch(`/api/users/${userId}`, {
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ role: newRole })
      })

      if (!res.ok) {
        fetchUsers()  // 失敗したら元に戻す
        alert('役割の変更に失敗しました')
      }
    } catch (error) {
      fetchUsers()
      alert('役割の変更に失敗しました')
    }
  }

  // ユーザー削除（楽観的UI更新）
  const handleDelete = async (userId: string) => {
    if (!confirm('このユーザーを削除しますか？')) return

    // 先に画面から削除
    setUsers(prevUsers => prevUsers.filter(u => u.id !== userId))

    try {
      const res = await fetch(`/api/users/${userId}`, {
        method: 'DELETE'
      })

      if (!res.ok) {
        fetchUsers()  // 失敗したら元に戻す
        alert('削除に失敗しました')
      }
    } catch (error) {
      fetchUsers()
      alert('削除に失敗しました')
    }
  }

  if (!isSiteAdmin) {
    return null  // 権限がない場合は何も表示しない
  }

  return (
    <DashboardLayout>
      <div className="max-w-6xl mx-auto p-6">
        <h1 className="text-3xl font-bold mb-8">ユーザー管理</h1>
        {/* ユーザー一覧の実装... */}
      </div>
    </DashboardLayout>
  )
}
```

**権限チェックのベストプラクティス:**

```
┌──────────────────────────────────────────────────┐
│     アクセス制御の実装パターン                    │
└──────────────────────────────────────────────────┘

【クライアント側】
  useEffect(() => {
    if (status === 'loading') return  // ① 認証状態確定まで待つ
    
    if (!session || !isSiteAdmin) {
      router.replace('/')  // ② 権限がない場合はリダイレクト
      return
    }
    
    fetchData()  // ③ 権限があればデータ取得
  }, [session, status, router])

  if (!isSiteAdmin) {
    return null  // ④ 権限がない場合は何も表示しない
  }


【サーバー側（API Route）】
  export async function GET(req: Request) {
    const session = await auth()
    
    if (!session || session.user.role !== 'site_admin') {
      return NextResponse.json(
        { error: 'Unauthorized' },
        { status: 403 }
      )
    }
    
    // データを返す
  }

→ クライアント側とサーバー側で二重にチェック
```

---

## 32.5 認証ページ

### src/app/auth/signin/page.tsx

```typescript
'use client'

import { signIn } from 'next-auth/react'
import { useState, useEffect } from 'react'
import { useRouter, useSearchParams } from 'next/navigation'
import Link from 'next/link'

export default function SignIn() {
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [error, setError] = useState('')
  const [message, setMessage] = useState('')
  const router = useRouter()
  const searchParams = useSearchParams()

  // URLパラメータからメッセージを取得
  useEffect(() => {
    const verified = searchParams.get('verified')
    const reset = searchParams.get('reset')

    if (verified === 'true') {
      setMessage('メールアドレスが確認されました。ログインしてください。')
    } else if (reset === 'success') {
      setMessage('パスワードがリセットされました。ログインしてください。')
    }
  }, [searchParams])

  // メール/パスワード認証
  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault()
    setError('')
    setMessage('')
    
    const result = await signIn('credentials', {
      email,
      password,
      redirect: false,  // 手動でリダイレクト制御
      callbackUrl: '/'
    })
    
    if (result?.error) {
      setError('メールアドレスまたはパスワードが正しくありません')
      return
    }
    
    if (result?.ok) {
      window.location.href = '/'  // フルページリロード
    }
  }

  // Google OAuth認証
  const handleGoogleSignIn = async () => {
    await signIn('google', { 
      callbackUrl: '/',
      redirect: true
    })
  }

  return (
    <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-500 to-purple-600 p-4">
      <div className="bg-white p-8 rounded-lg shadow-2xl w-full max-w-md">
        <h1 className="text-3xl font-bold mb-6 text-center">BOLD 軽音</h1>
        
        {/* メッセージ表示 */}
        {message && (
          <div className="mb-4 bg-green-50 border border-green-200 text-green-800 px-4 py-3 rounded-lg">
            {message}
          </div>
        )}
        {error && (
          <div className="mb-4 bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded-lg">
            {error}
          </div>
        )}
        
        {/* メール/パスワードフォーム */}
        <form onSubmit={handleSubmit} className="space-y-4">
          <div>
            <label className="block text-sm font-medium mb-2">メールアドレス</label>
            <input
              type="email"
              value={email}
              onChange={(e) => setEmail(e.target.value)}
              className="w-full px-4 py-2.5 border rounded-lg focus:ring-2 focus:ring-blue-500"
              required
            />
          </div>
          <div>
            <label className="block text-sm font-medium mb-2">パスワード</label>
            <input
              type="password"
              value={password}
              onChange={(e) => setPassword(e.target.value)}
              className="w-full px-4 py-2.5 border rounded-lg focus:ring-2 focus:ring-blue-500"
              required
            />
          </div>
          <button
            type="submit"
            className="w-full bg-blue-600 text-white py-3 rounded-lg hover:bg-blue-700 font-semibold"
          >
            ログイン
          </button>
        </form>

        <div className="mt-4 text-center">
          <Link href="/auth/forgot-password" className="text-sm text-blue-600 hover:text-blue-700">
            パスワードをお忘れですか？
          </Link>
        </div>
        
        {/* 区切り線 */}
        <div className="mt-4 relative">
          <div className="absolute inset-0 flex items-center">
            <div className="w-full border-t border-gray-300"></div>
          </div>
          <div className="relative flex justify-center text-sm">
            <span className="px-2 bg-white text-gray-500">または</span>
          </div>
        </div>

        {/* Google OAuth */}
        <button
          onClick={handleGoogleSignIn}
          className="w-full mt-4 bg-white text-gray-700 py-3 rounded-lg border hover:bg-gray-50 font-semibold flex items-center justify-center gap-2"
        >
          <svg className="w-5 h-5" viewBox="0 0 24 24">
            {/* Google アイコン SVG */}
          </svg>
          Googleでログイン
        </button>
        
        <div className="mt-4 text-center">
          <Link href="/auth/signup" className="text-sm text-blue-600 hover:text-blue-700">
            アカウントをお持ちでない方はこちら
          </Link>
        </div>
      </div>
    </div>
  )
}
```

**認証の実装パターン:**

```
┌──────────────────────────────────────────────────┐
│     NextAuth.js の使い方                          │
└──────────────────────────────────────────────────┘

【1. メール/パスワード認証】
  const result = await signIn('credentials', {
    email,
    password,
    redirect: false  // 手動でリダイレクト制御
  })
  
  if (result?.error) {
    // エラー処理
  } else {
    // 成功時の処理
    window.location.href = '/'
  }


【2. Google OAuth認証】
  const handleGoogleSignIn = () => {
    signIn('google', { callbackUrl: '/' })
  }
  
  → NextAuth.jsが自動でOAuth フローを処理
  → Google認証画面へリダイレクト
  → 認証後、callbackUrlにリダイレクト


【3. セッション情報の取得】
  // Client Component
  const { data: session, status } = useSession()
  
  // Server Component
  const session = await auth()
  
  → Client: useSession() フック
  → Server: auth() 関数


【4. URLパラメータでのメッセージ伝達】
  const searchParams = useSearchParams()
  const verified = searchParams.get('verified')
  
  if (verified === 'true') {
    setMessage('メールアドレスが確認されました')
  }
  
  → リダイレクト時にメッセージを渡す
  → セキュアな方法（トークン検証済み）
```

---

## 📋 ページ実装のベストプラクティス

```
┌──────────────────────────────────────────────────┐
│     ページコンポーネント設計のポイント           │
└──────────────────────────────────────────────────┘

✅ Server Component + Client Component の分離
  - データ取得 → Server Component
  - インタラクション → Client Component
  - トップページのような複雑なページで有効

✅ 統合ページアプローチ
  - 一覧・編集・削除を1つのページに統合
  - ページ遷移なしで操作完結
  - 楽観的UI更新が容易

✅ 楽観的UI更新（Optimistic UI）
  - ユーザーアクション後、即座に画面を更新
  - APIリクエストと並行して実行
  - 失敗時は元に戻す

✅ ローディング状態の管理
  - フォーム送信中はボタンを無効化
  - LoadingSpinner コンポーネントで統一

✅ エラーハンドリング
  - わかりやすいエラーメッセージ
  - セキュリティを考慮した情報公開

✅ アクセス制御
  - クライアント側とサーバー側で二重チェック
  - 権限がない場合はリダイレクト

✅ レスポンシブデザイン
  - モバイルファーストアプローチ
  - Tailwind CSSのブレークポイント活用
```

---

## 32.4 認証関連ページ

### 32.4.1 新規登録ページ（src/app/auth/signup/page.tsx）

**概要:**
新規ユーザー登録のためのページです。メールアドレス＋パスワード認証とGoogle OAuth の両方に対応しています。

**主要機能:**
- ✅ メールアドレス＋パスワード登録
- ✅ Google OAuth 登録
- ✅ パスワード確認（2回入力）
- ✅ バリデーション（パスワード一致確認）
- ✅ メール確認リンク送信

**実装コード:**

```tsx
'use client'

import { signIn } from 'next-auth/react'
import { useState } from 'react'
import { useRouter } from 'next/navigation'
import Link from 'next/link'

export default function SignUp() {
  const [name, setName] = useState('')
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [confirmPassword, setConfirmPassword] = useState('')
  const [message, setMessage] = useState('')
  const router = useRouter()

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault()
    
    if (password !== confirmPassword) {
      setMessage('パスワードが一致しません')
      return
    }

    const res = await fetch('/api/auth/signup', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ name, email, password })
    })

    const data = await res.json()

    if (res.ok) {
      setMessage(data.message || 'メールアドレスに検証リンクを送信しました。確認してください。')
    } else {
      setMessage(data.error || 'アカウント作成に失敗しました')
    }
  }

  const handleGoogleSignUp = async () => {
    await signIn('google', { callbackUrl: '/' })
  }

  return (
    <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-500 to-purple-600">
      <div className="bg-white p-8 rounded-lg shadow-2xl w-96">
        <h1 className="text-3xl font-bold mb-6 text-center text-gray-800">
          新規登録
        </h1>
        
        {message && (
          <div className={`mb-4 px-4 py-3 rounded-lg ${
            message.includes('送信しました') || message.includes('確認してください')
              ? 'bg-green-50 border border-green-200 text-green-800'
              : 'bg-red-50 border border-red-200 text-red-800'
          }`}>
            {message}
          </div>
        )}
        
        <form onSubmit={handleSubmit} className="space-y-4">
          <div>
            <label htmlFor="name" className="block text-sm font-medium text-gray-700 mb-2">
              名前
            </label>
            <input
              id="name"
              name="name"
              type="text"
              value={name}
              onChange={(e) => setName(e.target.value)}
              className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
              required
            />
          </div>
          <div>
            <label htmlFor="email" className="block text-sm font-medium text-gray-700 mb-2">
              メールアドレス
            </label>
            <input
              id="email"
              name="email"
              type="email"
              value={email}
              onChange={(e) => setEmail(e.target.value)}
              className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
              required
            />
          </div>
          <div>
            <label htmlFor="password" className="block text-sm font-medium text-gray-700 mb-2">
              パスワード
            </label>
            <input
              id="password"
              name="password"
              type="password"
              value={password}
              onChange={(e) => setPassword(e.target.value)}
              className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
              required
              minLength={6}
            />
          </div>
          <div>
            <label htmlFor="confirmPassword" className="block text-sm font-medium text-gray-700 mb-2">
              パスワード(確認)
            </label>
            <input
              id="confirmPassword"
              name="confirmPassword"
              type="password"
              value={confirmPassword}
              onChange={(e) => setConfirmPassword(e.target.value)}
              className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
              required
              minLength={6}
            />
          </div>
          <button
            type="submit"
            className="w-full bg-blue-600 text-white py-2 rounded-lg hover:bg-blue-700 transition-colors font-semibold"
          >
            登録
          </button>
        </form>
        
        <div className="mt-4">
          <div className="relative">
            <div className="absolute inset-0 flex items-center">
              <div className="w-full border-t border-gray-300"></div>
            </div>
            <div className="relative flex justify-center text-sm">
              <span className="px-2 bg-white text-gray-500">または</span>
            </div>
          </div>
        </div>

        <button
          onClick={handleGoogleSignUp}
          className="w-full mt-4 bg-white text-gray-700 py-2 rounded-lg border border-gray-300 hover:bg-gray-50 transition-colors font-semibold flex items-center justify-center gap-2"
        >
          <svg className="w-5 h-5" viewBox="0 0 24 24">
            <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
            <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
            <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
            <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
          </svg>
          Googleで登録
        </button>
        
        <div className="mt-4 text-center">
          <Link href="/auth/signin" className="text-sm text-blue-600 hover:text-blue-700">
            既にアカウントをお持ちの方はこちら
          </Link>
        </div>
      </div>
    </div>
  )
}
```

**コードの詳細解説:**

```typescript
// 【パスワード確認のバリデーション】
if (password !== confirmPassword) {
  setMessage('パスワードが一致しません')
  return
}
```

→ パスワードの2回入力を比較してタイポを防止

```typescript
// 【メッセージの色分け】
<div className={`mb-4 px-4 py-3 rounded-lg ${
  message.includes('送信しました') || message.includes('確認してください')
    ? 'bg-green-50 border border-green-200 text-green-800'
    : 'bg-red-50 border border-red-200 text-red-800'
}`}>
```

→ 成功時は緑、エラー時は赤で表示

```typescript
// 【Google OAuth 登録】
const handleGoogleSignUp = async () => {
  await signIn('google', { callbackUrl: '/' })
}
```

→ NextAuth.js の signIn 関数を使用してGoogle認証開始

---

### 32.4.2 パスワードリセット依頼ページ（src/app/auth/forgot-password/page.tsx）

**概要:**
パスワードを忘れたユーザーがリセットリンクを受け取るためのページです。

**主要機能:**
- ✅ メールアドレス入力
- ✅ リセットリンク送信
- ✅ ローディング状態表示
- ✅ 成功/エラーメッセージ表示

**実装コード:**

```tsx
'use client'

import { useState } from 'react'
import Link from 'next/link'

export default function ForgotPassword() {
  const [email, setEmail] = useState('')
  const [message, setMessage] = useState('')
  const [error, setError] = useState('')
  const [loading, setLoading] = useState(false)

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault()
    setError('')
    setMessage('')
    setLoading(true)

    try {
      const res = await fetch('/api/auth/forgot-password', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ email })
      })

      const data = await res.json()

      if (res.ok) {
        setMessage(data.message)
        setEmail('')
      } else {
        setError(data.error || 'パスワードリセットメールの送信に失敗しました')
      }
    } catch (err) {
      setError('エラーが発生しました')
    } finally {
      setLoading(false)
    }
  }

  return (
    <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-500 to-purple-600 p-4">
      <div className="bg-white p-6 sm:p-8 rounded-lg shadow-2xl w-full max-w-md">
        <h1 className="text-2xl sm:text-3xl font-bold mb-2 text-center text-gray-800">
          パスワードリセット
        </h1>
        <p className="text-sm text-gray-600 text-center mb-6">
          登録したメールアドレスを入力してください
        </p>

        {message && (
          <div className="mb-4 bg-green-50 border border-green-200 text-green-800 px-4 py-3 rounded-lg text-sm">
            {message}
          </div>
        )}
        {error && (
          <div className="mb-4 bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded-lg text-sm">
            {error}
          </div>
        )}

        <form onSubmit={handleSubmit} className="space-y-4">
          <div>
            <label htmlFor="email" className="block text-sm font-medium text-gray-700 mb-2">
              メールアドレス
            </label>
            <input
              id="email"
              name="email"
              type="email"
              value={email}
              onChange={(e) => setEmail(e.target.value)}
              className="w-full px-3 sm:px-4 py-2.5 text-sm sm:text-base border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
              required
              disabled={loading}
            />
          </div>

          <button
            type="submit"
            disabled={loading}
            className="w-full bg-blue-600 text-white py-2.5 text-sm sm:text-base rounded-lg hover:bg-blue-700 transition-colors font-semibold disabled:opacity-50 disabled:cursor-not-allowed"
          >
            {loading ? '送信中...' : 'リセットリンクを送信'}
          </button>
        </form>

        <div className="mt-6 text-center space-y-2">
          <Link href="/auth/signin" className="block text-sm text-blue-600 hover:text-blue-700">
            ログインに戻る
          </Link>
        </div>
      </div>
    </div>
  )
}
```

**コードの詳細解説:**

```typescript
// 【ローディング状態の管理】
const [loading, setLoading] = useState(false)

// 送信開始
setLoading(true)

// 送信完了
finally {
  setLoading(false)
}
```

→ ボタンの二重クリック防止と視覚的フィードバック

```typescript
// 【disabled属性の活用】
<input
  disabled={loading}
  className="..."
/>
<button
  disabled={loading}
  className="disabled:opacity-50 disabled:cursor-not-allowed"
>
  {loading ? '送信中...' : 'リセットリンクを送信'}
</button>
```

→ ローディング中は入力とボタンを無効化

---

### 32.4.3 パスワードリセット実行ページ（src/app/auth/reset-password/page.tsx）

**概要:**
メールのリセットリンクからアクセスして新しいパスワードを設定するページです。

**主要機能:**
- ✅ URLパラメータからトークン取得
- ✅ 新しいパスワード入力（2回）
- ✅ パスワードバリデーション
- ✅ 成功後、自動的にログインページへ遷移

**実装コード:**

```tsx
'use client'

import { useState, useEffect } from 'react'
import { useRouter, useSearchParams } from 'next/navigation'
import Link from 'next/link'

export default function ResetPassword() {
  const [password, setPassword] = useState('')
  const [confirmPassword, setConfirmPassword] = useState('')
  const [message, setMessage] = useState('')
  const [error, setError] = useState('')
  const [loading, setLoading] = useState(false)
  const [token, setToken] = useState('')
  const router = useRouter()
  const searchParams = useSearchParams()

  useEffect(() => {
    const tokenParam = searchParams.get('token')
    if (!tokenParam) {
      setError('無効なリンクです')
    } else {
      setToken(tokenParam)
    }
  }, [searchParams])

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault()
    setError('')
    setMessage('')

    if (password !== confirmPassword) {
      setError('パスワードが一致しません')
      return
    }

    if (password.length < 6) {
      setError('パスワードは6文字以上で入力してください')
      return
    }

    setLoading(true)

    try {
      const res = await fetch('/api/auth/reset-password', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ token, password })
      })

      const data = await res.json()

      if (res.ok) {
        setMessage(data.message)
        setTimeout(() => {
          router.push('/auth/signin?reset=success')
        }, 2000)
      } else {
        setError(data.error || 'パスワードのリセットに失敗しました')
      }
    } catch (err) {
      setError('エラーが発生しました')
    } finally {
      setLoading(false)
    }
  }

  if (!token && !error) {
    return (
      <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-500 to-purple-600 p-4">
        <div className="bg-white p-6 sm:p-8 rounded-lg shadow-2xl w-full max-w-md text-center">
          <p className="text-gray-600">読み込み中...</p>
        </div>
      </div>
    )
  }

  return (
    <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-500 to-purple-600 p-4">
      <div className="bg-white p-6 sm:p-8 rounded-lg shadow-2xl w-full max-w-md">
        <h1 className="text-2xl sm:text-3xl font-bold mb-2 text-center text-gray-800">
          新しいパスワード
        </h1>
        <p className="text-sm text-gray-600 text-center mb-6">
          新しいパスワードを入力してください
        </p>

        {message && (
          <div className="mb-4 bg-green-50 border border-green-200 text-green-800 px-4 py-3 rounded-lg text-sm">
            {message}
          </div>
        )}
        {error && (
          <div className="mb-4 bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded-lg text-sm">
            {error}
          </div>
        )}

        {token && !message && (
          <form onSubmit={handleSubmit} className="space-y-4">
            <div>
              <label htmlFor="password" className="block text-sm font-medium text-gray-700 mb-2">
                新しいパスワード
              </label>
              <input
                id="password"
                name="password"
                type="password"
                value={password}
                onChange={(e) => setPassword(e.target.value)}
                className="w-full px-3 sm:px-4 py-2.5 text-sm sm:text-base border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                required
                minLength={6}
                disabled={loading}
              />
            </div>

            <div>
              <label htmlFor="confirmPassword" className="block text-sm font-medium text-gray-700 mb-2">
                パスワード（確認）
              </label>
              <input
                id="confirmPassword"
                name="confirmPassword"
                type="password"
                value={confirmPassword}
                onChange={(e) => setConfirmPassword(e.target.value)}
                className="w-full px-3 sm:px-4 py-2.5 text-sm sm:text-base border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                required
                minLength={6}
                disabled={loading}
              />
            </div>

            <button
              type="submit"
              disabled={loading}
              className="w-full bg-blue-600 text-white py-2.5 text-sm sm:text-base rounded-lg hover:bg-blue-700 transition-colors font-semibold disabled:opacity-50 disabled:cursor-not-allowed"
            >
              {loading ? 'リセット中...' : 'パスワードをリセット'}
            </button>
          </form>
        )}

        <div className="mt-6 text-center space-y-2">
          <Link href="/auth/signin" className="block text-sm text-blue-600 hover:text-blue-700">
            ログインに戻る
          </Link>
        </div>
      </div>
    </div>
  )
}
```

**コードの詳細解説:**

```typescript
// 【URLパラメータからトークン取得】
const searchParams = useSearchParams()

useEffect(() => {
  const tokenParam = searchParams.get('token')
  if (!tokenParam) {
    setError('無効なリンクです')
  } else {
    setToken(tokenParam)
  }
}, [searchParams])
```

→ URLの `?token=xxx` からトークンを取得

```typescript
// 【成功後の自動リダイレクト】
if (res.ok) {
  setMessage(data.message)
  setTimeout(() => {
    router.push('/auth/signin?reset=success')
  }, 2000)
}
```

→ 2秒後にログインページへ自動遷移

```typescript
// 【条件付きレンダリング】
{token && !message && (
  <form onSubmit={handleSubmit}>
    {/* フォーム内容 */}
  </form>
)}
```

→ トークンがあり、まだ成功メッセージが表示されていない場合のみフォームを表示

---

## 32.5 投稿編集ページ

### 32.5.1 投稿編集ページ（src/app/posts/[id]/edit/page.tsx）

**概要:**
管理者が既存の投稿を編集するための専用ページです。

**主要機能:**
- ✅ 既存投稿データの取得と表示
- ✅ タイトル、内容、YouTube URL、画像の編集
- ✅ Supabase Storage への画像アップロード
- ✅ RichTextEditor コンポーネントの活用
- ✅ 管理者権限チェック

**コードの一部（主要機能）:**

```tsx
'use client'

import { useEffect, useState, useRef } from 'react'
import { useSession } from 'next-auth/react'
import { useRouter } from 'next/navigation'
import DashboardLayout from '@/components/DashboardLayout'
import RichTextEditor from '@/components/RichTextEditor'
import { ArrowLeft, Loader2, Upload, X } from 'lucide-react'
import { supabase } from '@/lib/supabase'

export default function EditPostPage({ params }: { params: Promise<{ id: string }> }) {
  const { data: session, status } = useSession()
  const router = useRouter()
  const [loading, setLoading] = useState(false)
  const [uploadingImages, setUploadingImages] = useState(false)
  const [postId, setPostId] = useState<string>('')

  const [formData, setFormData] = useState({
    title: '',
    content: '',
    youtubeUrls: [] as string[],
    images: [] as string[]
  })

  // 投稿IDをparamsから取得
  useEffect(() => {
    params.then(p => setPostId(p.id))
  }, [params])

  // 認証と権限チェック
  useEffect(() => {
    if (status === 'unauthenticated') {
      router.push('/auth/signin')
    } else if (status === 'authenticated') {
      if (session.user.role !== 'admin' && session.user.role !== 'site_admin') {
        router.push('/posts')
        return
      }

      if (postId) {
        fetchPost()
      }
    }
  }, [status, session, router, postId])

  // 既存投稿データの取得
  const fetchPost = async () => {
    try {
      const res = await fetch(`/api/posts/${postId}`)
      if (res.ok) {
        const data = await res.json()
        setFormData({
          title: data.title,
          content: data.content || '',
          youtubeUrls: data.youtubeUrls || [],
          images: data.images || []
        })
      } else {
        alert('投稿の取得に失敗しました')
        router.push('/posts')
      }
    } catch (error) {
      console.error('投稿取得エラー:', error)
      alert('投稿の取得に失敗しました')
      router.push('/posts')
    }
  }

  // 画像アップロード
  const handleImageUpload = async (e: React.ChangeEvent<HTMLInputElement>) => {
    const files = e.target.files
    if (!files || files.length === 0) return

    if (!supabase) {
      alert('画像アップロード機能が利用できません。Supabaseの設定を確認してください。')
      return
    }

    setUploadingImages(true)

    try {
      const uploadedUrls: string[] = []

      for (const file of Array.from(files)) {
        if (file.size > 5 * 1024 * 1024) {
          alert(`${file.name} は5MBを超えています`)
          continue
        }

        const fileExt = file.name.split('.').pop()
        const fileName = `${Date.now()}-${Math.random().toString(36).substring(7)}.${fileExt}`
        const filePath = `event-reports/${fileName}`

        const { data, error } = await supabase.storage
          .from('avatars')
          .upload(filePath, file)

        if (error) {
          console.error('Upload error:', error)
          alert(`${file.name} のアップロードに失敗しました`)
          continue
        }

        const { data: { publicUrl } } = supabase.storage
          .from('avatars')
          .getPublicUrl(filePath)

        uploadedUrls.push(publicUrl)
      }

      setFormData(prev => ({
        ...prev,
        images: [...prev.images, ...uploadedUrls]
      }))

      alert(`${uploadedUrls.length}枚の画像をアップロードしました`)
    } catch (error) {
      console.error('Image upload error:', error)
      alert('画像のアップロードに失敗しました')
    } finally {
      setUploadingImages(false)
    }
  }

  // 投稿更新
  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault()

    if (!formData.title || !formData.content) {
      alert('タイトルと内容は必須です')
      return
    }

    setLoading(true)

    try {
      const res = await fetch(`/api/posts/${postId}`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(formData)
      })

      if (res.ok) {
        router.push('/posts')
      } else {
        const error = await res.json()
        alert(error.error || '投稿の更新に失敗しました')
      }
    } catch (error) {
      console.error('投稿更新エラー:', error)
      alert('投稿の更新に失敗しました')
    } finally {
      setLoading(false)
    }
  }

  // UI部分は省略（フォーム、画像プレビュー、RichTextEditor等）
}
```

**重要なポイント:**

1. **paramsの非同期処理**: Next.js 15+ では params が Promise になったため、`params.then()` で解決
2. **権限チェック**: admin または site_admin のみがアクセス可能
3. **既存データの取得**: ページ読み込み時に投稿データをフェッチしてフォームに反映
4. **画像管理**: Supabase Storage を使った画像アップロードと削除機能
5. **YouTube URL の複数管理**: 配列で複数のYouTube URLを管理

---

## 32.6 活動スケジュール報告ページ

### 32.6.1 スケジュールから活動報告作成（src/app/activity-schedules/[id]/report/page.tsx）

**概要:**
活動スケジュールから活動報告（投稿）を作成するためのページです。テンプレート機能を活用して効率的に報告を作成できます。

**主要機能:**
- ✅ スケジュール情報からテンプレート生成
- ✅ URLパラメータからのテンプレート読み込み
- ✅ デフォルトテンプレートの取得
- ✅ RichTextEditor での編集
- ✅ 投稿として保存

**コードの一部（主要機能）:**

```tsx
'use client'

import { useEffect, useState, useRef } from 'react'
import { useSession } from 'next-auth/react'
import { useRouter, useSearchParams } from 'next/navigation'
import DashboardLayout from '@/components/DashboardLayout'
import RichTextEditor from '@/components/RichTextEditor'

export default function CreateReportPage({ params }: { params: Promise<{ id: string }> }) {
  const { data: session, status } = useSession()
  const router = useRouter()
  const searchParams = useSearchParams()
  const [loading, setLoading] = useState(false)
  const [scheduleId, setScheduleId] = useState<string>('')

  const [formData, setFormData] = useState({
    title: '',
    content: '',
    youtubeUrls: [] as string[],
    images: [] as string[]
  })

  // スケジュールIDの取得
  useEffect(() => {
    params.then(p => setScheduleId(p.id))
  }, [params])

  // 認証と権限チェック
  useEffect(() => {
    if (status === 'unauthenticated') {
      router.push('/auth/signin')
    } else if (status === 'authenticated') {
      if (session.user.role !== 'admin' && session.user.role !== 'site_admin') {
        router.push('/activity-schedules')
        return
      }
      
      if (scheduleId) {
        fetchScheduleAndTemplate()
      }
    }
  }, [status, session, router, scheduleId])

  // スケジュール情報とテンプレートの取得
  const fetchScheduleAndTemplate = async () => {
    try {
      // URLパラメータからテンプレートを取得
      const templateParam = searchParams.get('template')
      
      if (templateParam) {
        // URLパラメータにテンプレートがある場合はそれを使用
        const decodedTemplate = decodeURIComponent(templateParam)
        const lines = decodedTemplate.split('\n')
        const title = lines[0].replace(/^#\s*/, '') // 最初の行からタイトルを抽出
        
        setFormData({
          title: title || '',
          content: decodedTemplate,
          youtubeUrls: [],
          images: []
        })
      } else {
        // テンプレートがない場合は従来通りスケジュール情報を取得
        const scheduleRes = await fetch(`/api/activity-schedules/${scheduleId}/details`)
        let scheduleTitle = ''
        if (scheduleRes.ok) {
          const scheduleData = await scheduleRes.json()
          scheduleTitle = scheduleData.title || ''
        }

        // デフォルトテンプレートを取得
        const templateRes = await fetch('/api/templates')
        if (templateRes.ok) {
          const templateData = await templateRes.json()
          setFormData({
            title: scheduleTitle,
            content: templateData.content || '',
            youtubeUrls: [],
            images: []
          })
        } else {
          setFormData(prev => ({
            ...prev,
            title: scheduleTitle
          }))
        }
      }
    } catch (error) {
      console.error('データ取得エラー:', error)
    }
  }

  // 報告書の作成（投稿として保存）
  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault()

    if (!formData.title || !formData.content) {
      alert('タイトルと内容は必須です')
      return
    }

    setLoading(true)

    try {
      const res = await fetch(`/api/activity-schedules/${scheduleId}/report`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(formData)
      })

      if (res.ok) {
        router.push('/posts')
      } else {
        const error = await res.json()
        alert(error.error || '活動報告の作成に失敗しました')
      }
    } catch (error) {
      console.error('活動報告作成エラー:', error)
      alert('活動報告の作成に失敗しました')
    } finally {
      setLoading(false)
    }
  }

  // UI部分は省略（フォーム、RichTextEditor等）
}
```

**重要なポイント:**

1. **テンプレート優先度**:
   - URLパラメータのテンプレート → 最優先
   - デフォルトテンプレート → 次点
   - スケジュールのタイトルのみ → フォールバック

2. **タイトルの自動抽出**: テンプレートの最初の行（`# タイトル`）からタイトルを抽出

3. **report API への POST**: スケジュール情報を含めた投稿として保存

4. **ワークフロー**:
   ```
   スケジュール一覧
   ↓
   「報告書作成」ボタンクリック
   ↓
   テンプレートエディタでプレビュー
   ↓
   テンプレートを確定
   ↓
   このページ（report/page.tsx）でテンプレート編集
   ↓
   投稿として保存
   ```

---

## 📋 新規追加ページのベストプラクティス

```
┌──────────────────────────────────────────────────┐
│     認証・編集ページ設計のポイント               │
└──────────────────────────────────────────────────┘

✅ 認証ページ
  - パスワード確認の2回入力でタイポ防止
  - ローディング状態で二重送信防止
  - 成功/エラーメッセージの視覚的な区別
  - レスポンシブデザイン（sm:, md: ブレークポイント）

✅ 編集ページ
  - 既存データの取得と表示
  - 管理者権限の厳密なチェック
  - 画像アップロードの進捗表示
  - 保存前のバリデーション

✅ テンプレート活用
  - URLパラメータでテンプレート受け渡し
  - デフォルトテンプレートのフォールバック
  - タイトルの自動抽出

✅ Next.js 15+ 対応
  - params が Promise になったため非同期処理
  - useSearchParams で URLパラメータ取得
  - useRouter で programmatic navigation
```

---

## 🚀 次のステップ

次の章では、API Routesとライブラリ・ユーティリティの詳細を解説します：

- **Chapter 31**: API Routesの詳細解説
- **Chapter 33**: ライブラリとユーティリティの詳細解説

---

[← 前の章：第31章 API Routesの詳細解説](31-API-Routesの詳細解説.md) | [目次に戻る](00-目次.md) | [次の章へ：第33章 ライブラリとユーティリティの詳細解説 →](33-ライブラリとユーティリティの詳細解説.md)

# 第32章：ページコンポーネントの詳細解説

> **この章では、実装したページコンポーネントのコードを詳細に解説します**

## 📚 この章の目的

この章は**リファレンス**として、各ページコンポーネントの仕組みを深く理解するためのものです。

- ✅ Client ComponentとServer Componentの使い分け
- ✅ データ取得とステート管理
- ✅ ユーザーインタラクションの実装
- ✅ Next.jsのApp Routerパターン
- ✅ React Hooksの活用

## 💡 ページコンポーネントの基本構造

```
┌──────────────────────────────────────────────────┐
│     Next.js App Router のページ構成              │
└──────────────────────────────────────────────────┘

src/app/
├─ page.tsx                # / (ホームページ)
├─ posts/
│  ├─ page.tsx             # /posts
│  ├─ [id]/
│  │  ├─ page.tsx          # /posts/:id
│  │  └─ edit/
│  │     └─ page.tsx       # /posts/:id/edit
│  └─ new/
│     └─ page.tsx          # /posts/new
├─ events/
│  ├─ page.tsx             # /events
│  └─ [id]/
│     └─ page.tsx          # /events/:id
└─ activity-schedules/
   ├─ page.tsx             # /activity-schedules
   └─ [id]/
      └─ page.tsx          # /activity-schedules/:id


【ファイル名とルーティング】

  ファイル名              URL
  ─────────────────────────────────────────
  page.tsx                そのディレクトリのメインページ
  [id]/page.tsx           動的ルート（:id部分が変わる）
  layout.tsx              共通レイアウト
  loading.tsx             ローディング状態
  error.tsx               エラー状態


【Client ComponentとServer Component】

  'use client' ディレクティブの有無で区別

  ✅ Client Component（'use client' あり）
    - ブラウザで実行
    - useState, useEffect などのHooksが使える
    - イベントハンドラーが使える
    - リアルタイムなインタラクションが必要な場合

  ✅ Server Component（'use client' なし）
    - サーバーで実行
    - データベースに直接アクセスできる
    - HTMLをサーバーで生成して送信
    - SEOに有利
    - パフォーマンスが良い


【基本的なページコンポーネントの構造】

'use client'  // Client Componentの場合のみ

import { useState, useEffect } from 'react'
import { useSession } from 'next-auth/react'

export default function PageName() {
  // 1. Hooks
  const { data: session } = useSession()
  const [data, setData] = useState([])
  const [loading, setLoading] = useState(true)

  // 2. データ取得
  useEffect(() => {
    fetchData()
  }, [])

  const fetchData = async () => {
    const res = await fetch('/api/...')
    const data = await res.json()
    setData(data)
    setLoading(false)
  }

  // 3. イベントハンドラー
  const handleAction = async () => {
    // ...
  }

  // 4. レンダリング
  if (loading) return <div>Loading...</div>

  return (
    <div>
      {/* UI */}
    </div>
  )
}
```

---

## 32.1 ホームページ（src/app/page.tsx + HomeClient.tsx）

ホームページは、Next.js 15+の推奨パターンに従い、**Server Component**と**Client Component**に分離されています。

### アーキテクチャ: Server Component + Client Component パターン

```
┌─────────────────────────────────────────────────────────┐
│    ホームページの構成（2ファイル構造）                  │
└─────────────────────────────────────────────────────────┘

src/app/page.tsx (Server Component)
  ↓ データ取得（サーバー側）
  ├─ Prismaでデータベースにアクセス
  ├─ ISR (Incremental Static Regeneration) で60秒ごとに再生成
  └─ 初期データをクライアントに渡す

  ↓ propsで渡す

src/app/HomeClient.tsx (Client Component)
  ↓ インタラクティブな処理（ブラウザ側）
  ├─ アニメーション
  ├─ ユーザーインタラクション
  └─ 動的な表示切り替え


【このパターンの利点】

✅ SEO最適化
  - サーバー側でHTMLを生成
  - クローラーが完全なコンテンツを取得

✅ パフォーマンス
  - 初回表示が高速（サーバー側レンダリング）
  - JavaScriptバンドルサイズの削減

✅ データセキュリティ
  - データベースクエリはサーバー側のみ
  - 環境変数がクライアントに露出しない

✅ キャッシュ制御
  - ISRで適度な頻度で再生成
  - サーバー負荷とリアルタイム性のバランス
```

### src/app/page.tsx（Server Component）

サーバー側でデータ取得を行い、Client Componentに渡します。

```typescript
import Link from 'next/link'
import { Music, Calendar, FileText, Users } from 'lucide-react'
import { auth } from '@/lib/auth'
import { PrismaClient } from '@prisma/client'
import HomeClient from './HomeClient'

const prisma = new PrismaClient()

// ISR: 60秒ごとにページを再生成
export const revalidate = 60

export default async function Home() {
  // 1. セッション情報を取得（サーバー側）
  const session = await auth()
  
  // 2. データベースから投稿を取得
  const allPosts = await prisma.post.findMany({
    include: {
      user: {
        select: {
          id: true,
          name: true,
          email: true,
          avatarUrl: true,
        },
      },
      likes: {
        select: {
          userId: true,
          createdAt: true,
        },
      },
    },
    orderBy: {
      createdAt: 'desc',
    },
  })

  // 3. 最新3件を抽出
  const recentPosts = allPosts.slice(0, 3)

  // 4. 人気投稿（いいね数でソート）
  const popularPosts = [...allPosts]
    .sort((a, b) => b.likes.length - a.likes.length)
    .slice(0, 3)
    .filter(p => p.likes.length > 0)

  // 5. Client Componentに初期データを渡す
  return (
    <HomeClient
      session={session}
      initialPosts={recentPosts}
      popularPosts={popularPosts}
    />
  )
}
```

**重要なポイント:**

```
┌──────────────────────────────────────────────────┐
│     Server Component の特徴                       │
└──────────────────────────────────────────────────┘

【1. async/await が使える】
  export default async function Home() {
    const data = await prisma...
  }
  → トップレベルでawaitが使える（Server Componentのみ）


【2. ISR (Incremental Static Regeneration)】
  export const revalidate = 60
  → 60秒ごとにページを再生成
  → 最初のリクエストはキャッシュから返す（高速）
  → 60秒経過後の次のリクエストで再生成

  
【3. Prismaの直接利用】
  const prisma = new PrismaClient()
  const data = await prisma.post.findMany(...)
  → API Routeを経由せず、直接データベースにアクセス
  → ネットワークレイテンシがない（高速）


【4. auth()の直接呼び出し】
  const session = await auth()
  → サーバー側でセッション情報を取得
  → クライアント側の useSession() より高速


【5. propsでデータを渡す】
  <HomeClient
    session={session}
    initialPosts={recentPosts}
  />
  → Client Componentに初期データを渡す
  → サーバー側で取得したデータをブラウザ側で利用
```

### src/app/HomeClient.tsx（Client Component）

インタラクティブな機能を実装します。

```typescript
'use client'  // Client Component の印

import Link from 'next/link'
import { Music, Calendar, MessageCircle, FileText, User } from 'lucide-react'
import { useState } from 'react'
import YouTube from 'react-youtube'
import ScrollAnimation from '@/components/ScrollAnimation'
import Footer from '@/components/Footer'

interface Post {
  id: string
  title: string
  content: string
  youtubeUrls: string[]
  images?: string[]
  userId: string
  createdAt: string
  user: {
    id: string
    name: string | null
    email: string | null
    avatarUrl: string | null
  }
  likes: {
    userId: string
    createdAt: string
  }[]
}

interface Props {
  session: any
  initialPosts: Post[]
  popularPosts: Post[]
}

export default function HomeClient({ session, initialPosts, popularPosts }: Props) {
  // 1. ステート管理
  const [activeTab, setActiveTab] = useState<'latest' | 'popular'>('latest')

  // 2. YouTube URLからIDを抽出
  const extractYouTubeId = (url: string) => {
    const match = url.match(/(?:youtube\.com\/(?:watch\?v=|live\/|shorts\/|embed\/)|youtu\.be\/)([^&\n?#]+)/)
    return match ? match[1] : null
  }

  // 3. コンテンツのレンダリング（画像の処理）
  const renderContent = (content: string) => {
    return content.split('\n').map((line, index) => {
      const imgMatch = line.match(/!\[.*?\]\((.*?)\)/)
      if (imgMatch) {
        return (
          <div key={index} className="my-2">
            <img
              src={imgMatch[1]}
              alt="投稿画像"
              className="max-w-full h-auto rounded-lg"
              style={{ maxHeight: '500px', objectFit: 'contain' }}
            />
          </div>
        )
      }
      return line ? <div key={index}>{line}</div> : <br key={index} />
    })
  }

  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900">
      {/* Hero Section - アニメーション付きヘッダー */}
      <div className="relative overflow-hidden">
        <div className="absolute inset-0 bg-[url('/grid.svg')] opacity-10"></div>
        <div className="container mx-auto px-4 py-20 relative">
          <div className="text-center mb-12 space-y-6">
            {/* アイコン（アニメーション付き） */}
            <ScrollAnimation>
              <div className="inline-flex items-center justify-center mb-6 relative">
                <div className="absolute inset-0 bg-gradient-to-r from-blue-600 to-purple-600 blur-2xl opacity-50 animate-pulse"></div>
                <Music className="w-20 h-20 text-white relative z-10" />
              </div>
            </ScrollAnimation>
            
            {/* タイトル */}
            <ScrollAnimation delay={0.1}>
              <h1 className="text-6xl md:text-7xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-blue-400 via-purple-400 to-pink-400">
                BOLD 軽音
              </h1>
            </ScrollAnimation>

            <ScrollAnimation delay={0.2}>
              <p className="text-2xl md:text-3xl text-white/90 font-light">
                メンバーサイト
              </p>
            </ScrollAnimation>

            <ScrollAnimation delay={0.3}>
              <p className="text-lg text-white/70 max-w-2xl mx-auto">
                音楽を愛する仲間たちが集う、創造と交流の場
              </p>
            </ScrollAnimation>
            
            {/* ログイン/登録ボタン（未ログイン時のみ） */}
            {!session && (
              <ScrollAnimation delay={0.4}>
                <div className="flex flex-col sm:flex-row gap-4 justify-center mt-8">
                  <Link 
                    href="/auth/signin"
                    className="group relative inline-flex items-center justify-center px-8 py-4 text-lg font-semibold text-white bg-gradient-to-r from-blue-600 to-purple-600 rounded-full overflow-hidden shadow-2xl transition-all duration-300 hover:scale-105"
                  >
                    <span className="relative z-10">ログイン</span>
                  </Link>
                  <Link 
                    href="/auth/signup"
                    className="group relative inline-flex items-center justify-center px-8 py-4 text-lg font-semibold text-white border-2 border-white/30 backdrop-blur-sm rounded-full overflow-hidden transition-all duration-300 hover:scale-105"
                  >
                    <span className="relative z-10">新規登録</span>
                  </Link>
                </div>
              </ScrollAnimation>
            )}
          </div>
        </div>
      </div>

      {/* Features Section - 機能紹介カード */}
      <div className="container mx-auto px-4 py-16">
        <ScrollAnimation>
          <h2 className="text-4xl font-bold text-white mb-12 text-center">
            <span className="text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-purple-400">
              機能
            </span>
          </h2>
        </ScrollAnimation>

        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 max-w-7xl mx-auto">
          {/* 各機能カード */}
          <ScrollAnimation delay={0.1}>
            <Link 
              href="/posts"
              className="group relative bg-white/10 backdrop-blur-md rounded-2xl p-8 hover:bg-white/15 transition-all duration-300 hover:scale-105 border border-white/10"
            >
              <div className="absolute inset-0 bg-gradient-to-br from-blue-500/20 to-transparent rounded-2xl opacity-0 group-hover:opacity-100 transition-opacity"></div>
              <div className="relative">
                <div className="w-16 h-16 bg-gradient-to-br from-blue-500 to-blue-600 rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform shadow-lg">
                  <FileText className="w-8 h-8 text-white" />
                </div>
                <h3 className="text-2xl font-bold text-white mb-3">活動報告</h3>
                <p className="text-white/70">練習の成果や演奏動画を共有</p>
              </div>
            </Link>
          </ScrollAnimation>

          {/* 他のカードも同様に実装... */}
        </div>
      </div>

      {/* Latest Posts Section - タブ切り替え */}
      {(initialPosts.length > 0 || popularPosts.length > 0) && (
        <div className="container mx-auto px-4 py-16">
          <ScrollAnimation>
            <div className="flex justify-center mb-8">
              <div className="inline-flex bg-white/10 backdrop-blur-md rounded-full p-1">
                <button
                  onClick={() => setActiveTab('latest')}
                  className={`px-6 py-2 rounded-full transition-all duration-300 ${
                    activeTab === 'latest'
                      ? 'bg-gradient-to-r from-blue-600 to-purple-600 text-white'
                      : 'text-white/70 hover:text-white'
                  }`}
                >
                  最新の投稿
                </button>
                <button
                  onClick={() => setActiveTab('popular')}
                  className={`px-6 py-2 rounded-full transition-all duration-300 ${
                    activeTab === 'popular'
                      ? 'bg-gradient-to-r from-blue-600 to-purple-600 text-white'
                      : 'text-white/70 hover:text-white'
                  }`}
                >
                  人気の投稿
                </button>
              </div>
            </div>
          </ScrollAnimation>

          {/* 投稿カードの表示 */}
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 max-w-7xl mx-auto">
            {(activeTab === 'latest' ? initialPosts : popularPosts).map((post, index) => (
              <ScrollAnimation key={post.id} delay={index * 0.1}>
                <Link 
                  href={`/posts/${post.id}`}
                  className="group bg-white/10 backdrop-blur-md rounded-2xl overflow-hidden hover:bg-white/15 transition-all duration-300 hover:scale-105 border border-white/10"
                >
                  {/* 投稿カードの内容 */}
                  <div className="p-6">
                    <h3 className="text-xl font-bold text-white mb-2">{post.title}</h3>
                    <div className="text-white/70 line-clamp-3">
                      {renderContent(post.content)}
                    </div>
                    {/* いいね数 */}
                    <div className="mt-4 text-white/60 text-sm">
                      ❤️ {post.likes.length}
                    </div>
                  </div>
                </Link>
              </ScrollAnimation>
            ))}
          </div>
        </div>
      )}

      <Footer />
    </div>
  )
}
```

**重要なポイント:**

```
┌──────────────────────────────────────────────────┐
│     Client Component の特徴                       │
└──────────────────────────────────────────────────┘

【1. 'use client' ディレクティブ】
  'use client'
  → ファイルの先頭に記述（必須）
  → このコンポーネント以下はブラウザで実行


【2. useState でステート管理】
  const [activeTab, setActiveTab] = useState<'latest' | 'popular'>('latest')
  → タブの切り替えを管理
  → ブラウザ側でリアルタイムに更新


【3. イベントハンドラー】
  <button onClick={() => setActiveTab('latest')}>
  → ユーザーのクリックを処理
  → Server Component ではイベントハンドラーは使えない


【4. ScrollAnimation コンポーネント】
  <ScrollAnimation delay={0.1}>
    ...
  </ScrollAnimation>
  → スクロールに応じてアニメーション
  → Intersection Observer API を使用


【5. 条件付きレンダリング】
  {!session && (
    <Link href="/auth/signin">ログイン</Link>
  )}
  → ログアウト状態のみ表示

  {activeTab === 'latest' ? initialPosts : popularPosts}
  → タブに応じて表示データを切り替え


【6. propsで初期データを受け取る】
  interface Props {
    session: any
    initialPosts: Post[]
    popularPosts: Post[]
  }
  → Server Component から渡されたデータを受け取る
  → 初期表示が高速（すでにデータがある）
```

### まとめ: Server + Client パターンの利点

```
┌──────────────────────────────────────────────────┐
│     ベストプラクティス                           │
└──────────────────────────────────────────────────┘

✅ Server Component（page.tsx）
  - データ取得
  - 認証チェック
  - 初期レンダリング
  - ISRによるキャッシュ

✅ Client Component（HomeClient.tsx）
  - ユーザーインタラクション
  - アニメーション
  - ステート管理
  - 動的な表示切り替え


【このパターンを使うべき場合】

✅ SEOが重要なページ
✅ 初回表示速度が重要
✅ インタラクティブな機能も必要
✅ サーバー側でデータを取得したい

【避けるべきパターン】

❌ すべてをClient Componentにする
  → JavaScriptバンドルが大きくなる
  → 初回表示が遅い
  → SEOに不利

❌ Client ComponentでPrismaを使う
  → ブラウザでは動かない
  → 環境変数が露出
```

---

## 32.2 投稿ページ（src/app/posts/page.tsx）

投稿一覧ページの実装です。

### コアロジック

```typescript
'use client'

import { useState, useEffect, useRef, useCallback, useMemo } from 'react'
import YouTube from 'react-youtube'
import { useSession } from 'next-auth/react'
import { Heart, MessageCircle, Send, Trash2 } from 'lucide-react'
import Link from 'next/link'
import DashboardLayout from '@/components/DashboardLayout'

// 型定義
interface User {
  id: string
  name: string
  email: string
  avatarUrl?: string | null
}

interface Comment {
  id: string
  content: string
  createdAt: string
  user: User
}

interface PostParticipant {
  id: string
  status: string
  user: User
}

interface Post {
  id: string
  title: string
  content: string
  youtubeUrls: string[]
  images?: string[]
  createdAt: string
  userId: string
  user: User
  participants: PostParticipant[]
  likes: {
    userId: string
    createdAt: string
  }[]
  comments?: Comment[]
  _count?: {
    comments: number
  }
}

const POSTS_PER_PAGE = 5  // 1ページあたりの投稿数

export default function PostsPage() {
  // Hooks
  const { data: session } = useSession()
  const [posts, setPosts] = useState<Post[]>([])
  const [currentPage, setCurrentPage] = useState(1)
  const [fetchingPosts, setFetchingPosts] = useState(true)
  const [newComment, setNewComment] = useState<{ [postId: string]: string }>({})
  const [submittingComment, setSubmittingComment] = useState<string | null>(null)
  const [expandedComments, setExpandedComments] = useState<{ [postId: string]: boolean }>({})

  const isAdmin = session?.user?.role === 'admin'

  // 初回データ取得
  useEffect(() => {
    fetchPosts()
  }, [])

  // 投稿一覧を取得
  const fetchPosts = useCallback(async () => {
    setFetchingPosts(true)
    try {
      const res = await fetch('/api/posts')
      const data = await res.json()
      setPosts(data)
    } catch (error) {
      console.error('Failed to fetch posts:', error)
    } finally {
      setFetchingPosts(false)
    }
  }, [])

  // いいね機能
  const handleLike = async (postId: string) => {
    if (!session) return

    // 楽観的UI更新（即座に画面に反映）
    const isLiked = posts.find(p => p.id === postId)?.likes.some(l => l.userId === session.user.id)
    
    setPosts(posts.map(p => {
      if (p.id === postId) {
        if (isLiked) {
          // いいね解除
          return {
            ...p,
            likes: p.likes.filter(l => l.userId !== session.user.id)
          }
        } else {
          // いいね追加
          return {
            ...p,
            likes: [...p.likes, { userId: session.user.id, createdAt: new Date().toISOString() }]
          }
        }
      }
      return p
    }))

    // APIリクエスト
    try {
      const method = isLiked ? 'DELETE' : 'POST'
      const res = await fetch(`/api/posts/${postId}/like`, { method })
      
      if (!res.ok) {
        // 失敗したら元に戻す
        fetchPosts()
      }
    } catch (error) {
      console.error('いいねエラー:', error)
      fetchPosts()
    }
  }

  // コメント投稿
  const handleCommentSubmit = async (postId: string) => {
    const content = newComment[postId]?.trim()
    if (!content) return

    setSubmittingComment(postId)

    try {
      const res = await fetch(`/api/posts/${postId}/comments`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ content })
      })

      if (res.ok) {
        const comment = await res.json()
        
        // コメントを即座に画面に追加（楽観的UI更新）
        setPosts(posts.map(p => {
          if (p.id === postId) {
            return {
              ...p,
              comments: [comment, ...(p.comments || [])],
              _count: {
                ...p._count,
                comments: (p._count?.comments || 0) + 1
              }
            }
          }
          return p
        }))
        
        // 入力欄をクリア
        setNewComment({ ...newComment, [postId]: '' })
      }
    } catch (error) {
      console.error('コメント投稿エラー:', error)
    } finally {
      setSubmittingComment(null)
    }
  }

  // コメント削除
  const handleDeleteComment = async (postId: string, commentId: string) => {
    if (!confirm('このコメントを削除しますか？')) return

    try {
      const res = await fetch(`/api/posts/${postId}/comments/${commentId}`, {
        method: 'DELETE'
      })

      if (res.ok) {
        // コメントを即座に画面から削除
        setPosts(posts.map(p => {
          if (p.id === postId) {
            return {
              ...p,
              comments: p.comments?.filter(c => c.id !== commentId),
              _count: {
                ...p._count,
                comments: Math.max(0, (p._count?.comments || 0) - 1)
              }
            }
          }
          return p
        }))
      }
    } catch (error) {
      console.error('コメント削除エラー:', error)
    }
  }

  // ページネーション計算（useMemoで最適化）
  const { paginatedPosts, totalPages } = useMemo(() => {
    const startIndex = (currentPage - 1) * POSTS_PER_PAGE
    const endIndex = startIndex + POSTS_PER_PAGE
    return {
      paginatedPosts: posts.slice(startIndex, endIndex),
      totalPages: Math.ceil(posts.length / POSTS_PER_PAGE)
    }
  }, [posts, currentPage])

  // YouTube ID抽出
  const extractYouTubeId = (url: string) => {
    const match = url.match(/(?:youtube\.com\/(?:watch\?v=|live\/|shorts\/|embed\/)|youtu\.be\/)([^&\n?#]+)/)
    return match ? match[1] : null
  }

  // ローディング表示
  if (fetchingPosts) {
    return (
      <DashboardLayout>
        <div className="flex justify-center items-center min-h-screen">
          <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-blue-600"></div>
        </div>
      </DashboardLayout>
    )
  }

  return (
    <DashboardLayout>
      <div className="max-w-4xl mx-auto p-6">
        {/* ヘッダー */}
        <div className="flex justify-between items-center mb-8">
          <h1 className="text-3xl font-bold text-gray-900">活動報告</h1>
          {isAdmin && (
            <Link
              href="/posts/new"
              className="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700"
            >
              新規投稿
            </Link>
          )}
        </div>

        {/* 投稿一覧 */}
        <div className="space-y-6">
          {paginatedPosts.map(post => (
            <div key={post.id} className="bg-white rounded-lg shadow-md p-6">
              {/* 投稿ヘッダー */}
              <div className="flex justify-between items-start mb-4">
                <div>
                  <h2 className="text-2xl font-bold mb-2">{post.title}</h2>
                  <div className="text-sm text-gray-500">
                    投稿者: {post.user.name} | {new Date(post.createdAt).toLocaleDateString('ja-JP')}
                  </div>
                </div>
                {isAdmin && (
                  <div className="flex gap-2">
                    <Link
                      href={`/posts/${post.id}/edit`}
                      className="text-blue-600 hover:underline"
                    >
                      編集
                    </Link>
                    <button
                      onClick={() => handleDelete(post.id)}
                      className="text-red-600 hover:underline"
                    >
                      削除
                    </button>
                  </div>
                )}
              </div>

              {/* 投稿内容 */}
              <div className="prose max-w-none mb-4">
                {renderContent(post.content)}
              </div>

              {/* YouTube動画 */}
              {post.youtubeUrls && post.youtubeUrls.length > 0 && (
                <div className="grid grid-cols-1 md:grid-cols-2 gap-4 mb-4">
                  {post.youtubeUrls.map((url, index) => {
                    const videoId = extractYouTubeId(url)
                    return videoId ? (
                      <YouTube
                        key={index}
                        videoId={videoId}
                        opts={{
                          width: '100%',
                          playerVars: { autoplay: 0 }
                        }}
                      />
                    ) : null
                  })}
                </div>
              )}

              {/* いいねとコメントボタン */}
              <div className="flex gap-4 mb-4 pb-4 border-b">
                <button
                  onClick={() => handleLike(post.id)}
                  className={`flex items-center gap-2 ${
                    post.likes.some(l => l.userId === session?.user?.id)
                      ? 'text-red-600'
                      : 'text-gray-600 hover:text-red-600'
                  }`}
                >
                  <Heart className={`w-5 h-5 ${
                    post.likes.some(l => l.userId === session?.user?.id)
                      ? 'fill-current'
                      : ''
                  }`} />
                  {post.likes.length}
                </button>
                <button
                  onClick={() => toggleComments(post.id)}
                  className="flex items-center gap-2 text-gray-600 hover:text-blue-600"
                >
                  <MessageCircle className="w-5 h-5" />
                  {post._count?.comments || 0}
                </button>
              </div>

              {/* コメントセクション */}
              {expandedComments[post.id] && (
                <div className="mt-4">
                  {/* コメント入力 */}
                  <div className="flex gap-2 mb-4">
                    <input
                      type="text"
                      value={newComment[post.id] || ''}
                      onChange={(e) => setNewComment({ ...newComment, [post.id]: e.target.value })}
                      placeholder="コメントを入力..."
                      className="flex-1 border rounded-lg px-3 py-2"
                      onKeyPress={(e) => {
                        if (e.key === 'Enter') {
                          handleCommentSubmit(post.id)
                        }
                      }}
                    />
                    <button
                      onClick={() => handleCommentSubmit(post.id)}
                      disabled={submittingComment === post.id}
                      className="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 disabled:opacity-50"
                    >
                      <Send className="w-5 h-5" />
                    </button>
                  </div>

                  {/* コメント一覧 */}
                  <div className="space-y-3">
                    {post.comments?.map(comment => (
                      <div key={comment.id} className="bg-gray-50 rounded-lg p-3">
                        <div className="flex justify-between items-start">
                          <div>
                            <div className="font-semibold text-sm">{comment.user.name}</div>
                            <div className="text-gray-700 mt-1">{comment.content}</div>
                            <div className="text-xs text-gray-500 mt-1">
                              {new Date(comment.createdAt).toLocaleDateString('ja-JP')}
                            </div>
                          </div>
                          {(session?.user?.id === comment.user.id || isAdmin) && (
                            <button
                              onClick={() => handleDeleteComment(post.id, comment.id)}
                              className="text-red-600 hover:text-red-800"
                            >
                              <Trash2 className="w-4 h-4" />
                            </button>
                          )}
                        </div>
                      </div>
                    ))}
                  </div>
                </div>
              )}
            </div>
          ))}
        </div>

        {/* ページネーション */}
        {totalPages > 1 && (
          <div className="flex justify-center gap-2 mt-8">
            <button
              onClick={() => setCurrentPage(Math.max(1, currentPage - 1))}
              disabled={currentPage === 1}
              className="px-4 py-2 border rounded-lg disabled:opacity-50"
            >
              前へ
            </button>
            <div className="flex items-center gap-2">
              {Array.from({ length: totalPages }, (_, i) => i + 1).map(page => (
                <button
                  key={page}
                  onClick={() => setCurrentPage(page)}
                  className={`px-4 py-2 rounded-lg ${
                    currentPage === page
                      ? 'bg-blue-600 text-white'
                      : 'border hover:bg-gray-100'
                  }`}
                >
                  {page}
                </button>
              ))}
            </div>
            <button
              onClick={() => setCurrentPage(Math.min(totalPages, currentPage + 1))}
              disabled={currentPage === totalPages}
              className="px-4 py-2 border rounded-lg disabled:opacity-50"
            >
              次へ
            </button>
          </div>
        )}
      </div>
    </DashboardLayout>
  )
}
```

**重要なポイント:**

```
┌──────────────────────────────────────────────────┐
│     投稿ページの設計ポイント                     │
└──────────────────────────────────────────────────┘

【1. 楽観的UI更新（Optimistic UI）】
  ユーザーアクションに即座に反応する

  const handleLike = async (postId: string) => {
    // 先に画面を更新（楽観的更新）
    setPosts(/* 更新されたデータ */)
    
    // その後APIリクエスト
    const res = await fetch('/api/posts/${postId}/like', { method })
    
    // 失敗したら元に戻す
    if (!res.ok) {
      fetchPosts()
    }
  }

  ✅ メリット:
    - ユーザー体験が向上
    - 素早く反応する
  
  ⚠️ 注意点:
    - 失敗時のロールバックが必要


【2. オブジェクト形式のステート管理】
  複数の投稿それぞれにステートを持つ

  const [newComment, setNewComment] = useState<{ [postId: string]: string }>({})
  
  // 使い方
  newComment[post.id]  // 特定の投稿のコメント
  setNewComment({ ...newComment, [postId]: 'new value' })


【3. useMemoによる最適化】
  計算結果をキャッシュして不要な再計算を防ぐ

  const { paginatedPosts, totalPages } = useMemo(() => {
    // ページネーション計算
    return { paginatedPosts, totalPages }
  }, [posts, currentPage])
  // posts または currentPage が変わった時だけ再計算


【4. useCallbackによる最適化】
  関数をメモ化して不要な再生成を防ぐ

  const fetchPosts = useCallback(async () => {
    // データ取得
  }, [])  // 依存配列が空 → 1度だけ生成


【5. 条件付きレンダリング】
  {expandedComments[post.id] && (
    <div>コメントセクション</div>
  )}
  → コメントが展開されている時だけ表示


【6. ページネーション】
  const startIndex = (currentPage - 1) * POSTS_PER_PAGE
  const endIndex = startIndex + POSTS_PER_PAGE
  const paginatedPosts = posts.slice(startIndex, endIndex)
  
  totalPages = Math.ceil(posts.length / POSTS_PER_PAGE)


【7. キーボードイベント】
  onKeyPress={(e) => {
    if (e.key === 'Enter') {
      handleCommentSubmit(post.id)
    }
  }}
  → Enterキーでコメント送信
```

---

## 32.3 イベントページ（src/app/events/page.tsx）

イベント管理ページの実装です。

### コアロジック

```typescript
'use client'

import { useEffect, useState, useCallback } from 'react'
import { useSession } from 'next-auth/react'
import { useRouter } from 'next/navigation'
import DashboardLayout from '@/components/DashboardLayout'
import { Calendar, Users, Plus, MapPin, Music } from 'lucide-react'

interface Event {
  id: string
  title: string
  content: string
  date: string
  locationName: string | null
  locationUrl: string | null
  songs: string | null
  user: User
  participants: Participant[]
  comments?: Comment[]
  _count?: {
    comments: number
  }
}

export default function EventsPage() {
  const { data: session, status } = useSession()
  const router = useRouter()
  const [events, setEvents] = useState<Event[]>([])
  const [loading, setLoading] = useState(true)
  const [showCreateForm, setShowCreateForm] = useState(false)

  // フォーム状態
  const [formData, setFormData] = useState({
    title: '',
    content: '',
    date: '',
    locationName: '',
    locationUrl: '',
    songs: [] as {
      title: string
      sheetUrl: string
      youtubeUrl: string
      parts: { instrument: string; player: string }[]
    }[]
  })

  // 認証チェック
  useEffect(() => {
    if (status === 'unauthenticated') {
      router.push('/auth/signin')
    } else if (status === 'authenticated') {
      fetchEvents()
    }
  }, [status, router])

  // イベント一覧取得
  const fetchEvents = useCallback(async () => {
    try {
      const res = await fetch('/api/events')
      if (res.ok) {
        const data = await res.json()
        setEvents(data)
      }
    } catch (error) {
      console.error('イベント取得エラー:', error)
    } finally {
      setLoading(false)
    }
  }, [])

  // イベント作成
  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault()

    if (!formData.title || !formData.content || !formData.date) {
      alert('タイトル、内容、日付は必須です')
      return
    }

    try {
      const res = await fetch('/api/events', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(formData)
      })

      if (res.ok) {
        setShowCreateForm(false)
        setFormData({
          title: '',
          content: '',
          date: '',
          locationName: '',
          locationUrl: '',
          songs: []
        })
        fetchEvents()
      }
    } catch (error) {
      console.error('イベント作成エラー:', error)
    }
  }

  // 課題曲追加
  const handleAddSong = () => {
    setFormData({
      ...formData,
      songs: [
        ...formData.songs,
        {
          title: '',
          sheetUrl: '',
          youtubeUrl: '',
          parts: []
        }
      ]
    })
  }

  // 課題曲削除
  const handleRemoveSong = (index: number) => {
    setFormData({
      ...formData,
      songs: formData.songs.filter((_, i) => i !== index)
    })
  }

  // パート追加
  const handleAddPart = (songIndex: number) => {
    const newSongs = [...formData.songs]
    newSongs[songIndex].parts.push({ instrument: '', player: '' })
    setFormData({ ...formData, songs: newSongs })
  }

  // パート更新
  const handleUpdatePart = (
    songIndex: number,
    partIndex: number,
    field: 'instrument' | 'player',
    value: string
  ) => {
    const newSongs = [...formData.songs]
    newSongs[songIndex].parts[partIndex][field] = value
    setFormData({ ...formData, songs: newSongs })
  }

  // 参加登録
  const handleParticipate = async (eventId: string) => {
    try {
      const res = await fetch(`/api/events/${eventId}/participate`, {
        method: 'POST'
      })

      if (res.ok) {
        fetchEvents()
      }
    } catch (error) {
      console.error('参加登録エラー:', error)
    }
  }

  // 参加取り消し
  const handleCancelParticipation = async (eventId: string) => {
    try {
      const res = await fetch(`/api/events/${eventId}/participate`, {
        method: 'DELETE'
      })

      if (res.ok) {
        fetchEvents()
      }
    } catch (error) {
      console.error('参加取り消しエラー:', error)
    }
  }

  if (loading) {
    return (
      <DashboardLayout>
        <div className="flex justify-center items-center min-h-screen">
          <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-blue-600"></div>
        </div>
      </DashboardLayout>
    )
  }

  return (
    <DashboardLayout>
      <div className="max-w-6xl mx-auto p-6">
        {/* ヘッダー */}
        <div className="flex justify-between items-center mb-8">
          <h1 className="text-3xl font-bold">イベント</h1>
          {session?.user?.role === 'admin' && (
            <button
              onClick={() => setShowCreateForm(!showCreateForm)}
              className="flex items-center gap-2 bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700"
            >
              <Plus className="w-5 h-5" />
              新規イベント
            </button>
          )}
        </div>

        {/* イベント作成フォーム */}
        {showCreateForm && (
          <div className="bg-white rounded-lg shadow-md p-6 mb-8">
            <h2 className="text-2xl font-bold mb-4">新規イベント作成</h2>
            <form onSubmit={handleSubmit} className="space-y-4">
              {/* タイトル */}
              <div>
                <label className="block text-sm font-medium mb-1">タイトル *</label>
                <input
                  type="text"
                  value={formData.title}
                  onChange={(e) => setFormData({ ...formData, title: e.target.value })}
                  className="w-full border rounded-lg px-3 py-2"
                  required
                />
              </div>

              {/* 日時 */}
              <div>
                <label className="block text-sm font-medium mb-1">日時 *</label>
                <input
                  type="datetime-local"
                  value={formData.date}
                  onChange={(e) => setFormData({ ...formData, date: e.target.value })}
                  className="w-full border rounded-lg px-3 py-2"
                  required
                />
              </div>

              {/* 場所 */}
              <div>
                <label className="block text-sm font-medium mb-1">場所</label>
                <input
                  type="text"
                  value={formData.locationName}
                  onChange={(e) => setFormData({ ...formData, locationName: e.target.value })}
                  className="w-full border rounded-lg px-3 py-2"
                  placeholder="例: 渋谷ライブハウス"
                />
              </div>

              {/* 地図URL */}
              <div>
                <label className="block text-sm font-medium mb-1">地図URL</label>
                <input
                  type="url"
                  value={formData.locationUrl}
                  onChange={(e) => setFormData({ ...formData, locationUrl: e.target.value })}
                  className="w-full border rounded-lg px-3 py-2"
                  placeholder="Google Mapsのリンク等"
                />
              </div>

              {/* 内容 */}
              <div>
                <label className="block text-sm font-medium mb-1">内容 *</label>
                <textarea
                  value={formData.content}
                  onChange={(e) => setFormData({ ...formData, content: e.target.value })}
                  className="w-full border rounded-lg px-3 py-2"
                  rows={5}
                  required
                />
              </div>

              {/* 課題曲セクション */}
              <div>
                <div className="flex justify-between items-center mb-2">
                  <label className="block text-sm font-medium">課題曲</label>
                  <button
                    type="button"
                    onClick={handleAddSong}
                    className="text-blue-600 hover:text-blue-800 text-sm"
                  >
                    + 課題曲を追加
                  </button>
                </div>
                
                {formData.songs.map((song, songIndex) => (
                  <div key={songIndex} className="border rounded-lg p-4 mb-4">
                    <div className="flex justify-between items-center mb-3">
                      <h4 className="font-semibold">課題曲 {songIndex + 1}</h4>
                      <button
                        type="button"
                        onClick={() => handleRemoveSong(songIndex)}
                        className="text-red-600 hover:text-red-800 text-sm"
                      >
                        削除
                      </button>
                    </div>

                    {/* 曲タイトル */}
                    <input
                      type="text"
                      value={song.title}
                      onChange={(e) => {
                        const newSongs = [...formData.songs]
                        newSongs[songIndex].title = e.target.value
                        setFormData({ ...formData, songs: newSongs })
                      }}
                      placeholder="曲名"
                      className="w-full border rounded-lg px-3 py-2 mb-2"
                    />

                    {/* 楽譜URL */}
                    <input
                      type="url"
                      value={song.sheetUrl}
                      onChange={(e) => {
                        const newSongs = [...formData.songs]
                        newSongs[songIndex].sheetUrl = e.target.value
                        setFormData({ ...formData, songs: newSongs })
                      }}
                      placeholder="楽譜URL"
                      className="w-full border rounded-lg px-3 py-2 mb-2"
                    />

                    {/* YouTube URL */}
                    <input
                      type="url"
                      value={song.youtubeUrl}
                      onChange={(e) => {
                        const newSongs = [...formData.songs]
                        newSongs[songIndex].youtubeUrl = e.target.value
                        setFormData({ ...formData, songs: newSongs })
                      }}
                      placeholder="YouTube URL"
                      className="w-full border rounded-lg px-3 py-2 mb-2"
                    />

                    {/* パート割り当て */}
                    <div className="mt-3">
                      <div className="flex justify-between items-center mb-2">
                        <span className="text-sm font-medium">パート割り当て</span>
                        <button
                          type="button"
                          onClick={() => handleAddPart(songIndex)}
                          className="text-blue-600 hover:text-blue-800 text-xs"
                        >
                          + パート追加
                        </button>
                      </div>
                      
                      {song.parts.map((part, partIndex) => (
                        <div key={partIndex} className="flex gap-2 mb-2">
                          <input
                            type="text"
                            value={part.instrument}
                            onChange={(e) => handleUpdatePart(songIndex, partIndex, 'instrument', e.target.value)}
                            placeholder="楽器"
                            className="flex-1 border rounded-lg px-3 py-2 text-sm"
                          />
                          <input
                            type="text"
                            value={part.player}
                            onChange={(e) => handleUpdatePart(songIndex, partIndex, 'player', e.target.value)}
                            placeholder="担当者"
                            className="flex-1 border rounded-lg px-3 py-2 text-sm"
                          />
                        </div>
                      ))}
                    </div>
                  </div>
                ))}
              </div>

              {/* ボタン */}
              <div className="flex gap-3">
                <button
                  type="submit"
                  className="bg-blue-600 text-white px-6 py-2 rounded-lg hover:bg-blue-700"
                >
                  作成
                </button>
                <button
                  type="button"
                  onClick={() => setShowCreateForm(false)}
                  className="border px-6 py-2 rounded-lg hover:bg-gray-100"
                >
                  キャンセル
                </button>
              </div>
            </form>
          </div>
        )}

        {/* イベント一覧 */}
        <div className="space-y-6">
          {events.map(event => {
            const isParticipating = event.participants.some(p => p.userId === session?.user?.id)
            const isPastEvent = new Date(event.date) < new Date()

            return (
              <div key={event.id} className="bg-white rounded-lg shadow-md p-6">
                {/* イベントヘッダー */}
                <div className="flex justify-between items-start mb-4">
                  <div>
                    <h2 className="text-2xl font-bold mb-2">{event.title}</h2>
                    <div className="flex flex-wrap gap-4 text-sm text-gray-600">
                      <div className="flex items-center gap-1">
                        <Calendar className="w-4 h-4" />
                        {new Date(event.date).toLocaleString('ja-JP')}
                      </div>
                      {event.locationName && (
                        <div className="flex items-center gap-1">
                          <MapPin className="w-4 h-4" />
                          {event.locationUrl ? (
                            <a
                              href={event.locationUrl}
                              target="_blank"
                              rel="noopener noreferrer"
                              className="text-blue-600 hover:underline"
                            >
                              {event.locationName}
                            </a>
                          ) : (
                            event.locationName
                          )}
                        </div>
                      )}
                      <div className="flex items-center gap-1">
                        <Users className="w-4 h-4" />
                        {event.participants.length}人参加
                      </div>
                    </div>
                  </div>
                </div>

                {/* イベント内容 */}
                <div className="prose max-w-none mb-4">
                  {event.content}
                </div>

                {/* 参加ボタン */}
                {!isPastEvent && (
                  <div className="mt-4">
                    {isParticipating ? (
                      <button
                        onClick={() => handleCancelParticipation(event.id)}
                        className="bg-red-600 text-white px-6 py-2 rounded-lg hover:bg-red-700"
                      >
                        参加を取り消す
                      </button>
                    ) : (
                      <button
                        onClick={() => handleParticipate(event.id)}
                        className="bg-green-600 text-white px-6 py-2 rounded-lg hover:bg-green-700"
                      >
                        参加する
                      </button>
                    )}
                  </div>
                )}
              </div>
            )
          })}
        </div>
      </div>
    </DashboardLayout>
  )
}
```

**重要なポイント:**

```
┌──────────────────────────────────────────────────┐
│     イベントページの設計ポイント                 │
└──────────────────────────────────────────────────┘

【1. 複雑なフォーム状態管理】
  ネストした配列を含むフォーム

  const [formData, setFormData] = useState({
    title: '',
    songs: [] as {
      title: string
      parts: { instrument: string; player: string }[]
    }[]
  })

  // 配列の更新
  const newSongs = [...formData.songs]  // コピー
  newSongs[songIndex].title = newValue  // 更新
  setFormData({ ...formData, songs: newSongs })  // セット


【2. 日時の扱い】
  input type="datetime-local"
    → "2025-01-01T10:00" 形式

  new Date(event.date)
    → Dateオブジェクトに変換

  new Date().toLocaleString('ja-JP')
    → "2025/1/1 10:00:00" 形式で表示


【3. 条件付きスタイリング】
  {isPastEvent ? (
    <span className="text-gray-500">終了</span>
  ) : (
    <button className="bg-green-600">参加する</button>
  )}

  className={isParticipating
    ? 'bg-red-600'
    : 'bg-green-600'
  }


【4. 配列の操作】
  // 追加
  setFormData({
    ...formData,
    songs: [...formData.songs, newSong]
  })

  // 削除
  setFormData({
    ...formData,
    songs: formData.songs.filter((_, i) => i !== index)
  })

  // 更新
  const newSongs = [...formData.songs]
  newSongs[index] = updatedSong
  setFormData({ ...formData, songs: newSongs })


【5. 参加者チェック】
  const isParticipating = event.participants.some(
    p => p.userId === session?.user?.id
  )

  some(): 配列の中に条件に合う要素があるかチェック
```

---

## まとめ

この章では、主要なページコンポーネントの実装を詳しく解説しました。

### 🎓 この章で学んだこと

#### Client Componentの設計
- ✅ 'use client' ディレクティブの使い方
- ✅ React Hooksの活用（useState, useEffect, useMemo, useCallback）
- ✅ ステート管理のベストプラクティス

#### データ取得とAPI連携
- ✅ fetch APIの使い方
- ✅ 楽観的UI更新
- ✅ エラーハンドリング

#### ユーザーインタラクション
- ✅ フォーム処理
- ✅ いいね機能の実装
- ✅ コメント機能の実装
- ✅ ページネーション

#### パフォーマンス最適化
- ✅ useMemoによる計算結果のキャッシュ
- ✅ useCallbackによる関数のメモ化
- ✅ 条件付きレンダリング

### 💡 重要なパターン

#### データ取得パターン

```typescript
const [data, setData] = useState([])
const [loading, setLoading] = useState(true)

useEffect(() => {
  fetchData()
}, [])

const fetchData = async () => {
  setLoading(true)
  try {
    const res = await fetch('/api/...')
    const data = await res.json()
    setData(data)
  } catch (error) {
    console.error(error)
  } finally {
    setLoading(false)
  }
}
```

#### 楽観的UI更新パターン

```typescript
const handleAction = async (id: string) => {
  // 1. 先に画面を更新
  setData(/* 更新されたデータ */)
  
  // 2. APIリクエスト
  try {
    const res = await fetch('/api/...')
    if (!res.ok) {
      // 3. 失敗したら元に戻す
      fetchData()
    }
  } catch (error) {
    // 3. 失敗したら元に戻す
    fetchData()
  }
}
```

---

## 32.4 その他の主要ページ

プロジェクトには、上記以外にも重要なページが実装されています。ここでは簡潔に各ページの役割と特徴を解説します。

### 32.4.1 ユーザー管理ページ

#### src/app/users/page.tsx - ユーザー一覧

メンバー全員を表示し、プロフィールへのリンクを提供します。

**主な機能**:
- メンバー一覧をグリッドレイアウトで表示
- アバター画像、名前、担当楽器を表示
- 管理者バッジの表示
- ユーザー詳細ページへのリンク

**重要なポイント**:
```typescript
// カードレイアウトでメンバーを見やすく表示
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
  {users.map(user => (
    <Link href={`/users/${user.id}`}>
      {/* アバター、名前、楽器、役割 */}
    </Link>
  ))}
</div>
```

#### src/app/users/[id]/page.tsx - ユーザー詳細

個別のメンバープロフィールを詳しく表示します。

**主な機能**:
- プロフィール情報（アバター、名前、メール、楽器、自己紹介）
- 活動統計（投稿数、いいね数、コメント数）
- 登録日の表示

**重要なポイント**:
```typescript
// Prismaで統計情報も取得
const user = await prisma.user.findUnique({
  where: { id },
  include: {
    _count: {
      select: {
        posts: true,
        likes: true,
        comments: true,
      },
    },
  },
})
```

### 32.4.2 プロフィール編集ページ

#### src/app/profile/page.tsx - プロフィール編集

ログインユーザーが自分のプロフィールを編集できます。

**主な機能**:
- アバター画像のアップロード
- 名前、自己紹介の編集
- 担当楽器の編集（カンマ区切り）
- リアルタイムプレビュー

**重要なポイント**:
```typescript
// FormDataで画像をアップロード
const handleAvatarUpload = async (e: React.ChangeEvent<HTMLInputElement>) => {
  const file = e.target.files?.[0]
  if (!file) return

  const formData = new FormData()
  formData.append('file', file)

  const res = await fetch('/api/upload', {
    method: 'POST',
    body: formData,
  })
  const data = await res.json()
  setFormData(prev => ({ ...prev, avatarUrl: data.url }))
}

// 楽器を配列として管理
instruments: e.target.value.split(',').map(s => s.trim()).filter(Boolean)
```

### 32.4.3 活動スケジュールページ

#### src/app/activity-schedules/page.tsx - スケジュール一覧

複数の候補日を持つスケジュール管理ページです。

**主な機能**:
- スケジュール一覧表示
- 複数の候補日に対する投票
- 最有力候補（投票数が最も多い日）のハイライト
- 楽観的UI更新

**重要なポイント**:
```typescript
// 最有力候補を計算
const topCandidate = schedule.candidateDates.reduce((prev, current) =>
  current.votes.length > prev.votes.length ? current : prev
)

// 楽観的UI更新
const handleVote = async (scheduleId: string, candidateId: string) => {
  // 先に画面を更新
  setSchedules(prev => prev.map(schedule => {
    if (schedule.id === scheduleId) {
      return {
        ...schedule,
        candidateDates: schedule.candidateDates.map(candidate => {
          if (candidate.id === candidateId) {
            const hasVoted = candidate.votes.some(v => v.userId === session?.user?.id)
            return {
              ...candidate,
              votes: hasVoted
                ? candidate.votes.filter(v => v.userId !== session?.user?.id)
                : [...candidate.votes, { userId: session!.user!.id }]
            }
          }
          return candidate
        })
      }
    }
    return schedule
  }))

  // 後でAPIリクエスト
  await fetch(`/api/activity-schedules/${scheduleId}/vote`, { ... })
}
```

#### src/app/activity-schedules/[id]/report/page.tsx - 活動報告作成

スケジュールから活動報告を作成するページです。

**主な機能**:
- スケジュール情報を活動報告テンプレートとして利用
- 参加者情報の自動入力
- YouTube動画、画像の追加

**重要なポイント**:
```typescript
// スケジュール情報からテンプレートを生成
const initialData = {
  title: schedule.title,
  content: schedule.description || '',
  date: topCandidate.date,
  location: schedule.locationName || '',
  participants: topCandidate.votes.map(v => v.userId),
}
```

### 32.4.4 認証ページ

#### src/app/auth/signin/page.tsx - ログイン

メールアドレス + パスワード、またはGoogle OAuth でログインできます。

**主な機能**:
- メールアドレス + パスワード認証
- Google OAuth認証
- パスワードリセットリンク
- 新規登録リンク

**重要なポイント**:
```typescript
// NextAuth.jsのsignIn関数を使用
const handleSubmit = async (e: React.FormEvent) => {
  e.preventDefault()
  
  const result = await signIn('credentials', {
    email: formData.email,
    password: formData.password,
    redirect: false,  // 手動でリダイレクト制御
  })

  if (result?.error) {
    setError('メールアドレスまたはパスワードが間違っています')
  } else {
    router.push('/')
    router.refresh()  // セッション情報を更新
  }
}

// Google OAuth
const handleGoogleSignIn = () => {
  signIn('google', { callbackUrl: '/' })
}
```

#### src/app/auth/signup/page.tsx - 新規登録

新しいアカウントを作成します。

**主な機能**:
- メールアドレス + パスワードで登録
- パスワードの確認入力
- 登録後に認証メールを送信（オプション）

**重要なポイント**:
```typescript
const handleSubmit = async (e: React.FormEvent) => {
  e.preventDefault()

  // パスワードの一致確認
  if (formData.password !== formData.confirmPassword) {
    setError('パスワードが一致しません')
    return
  }

  // API経由で登録
  const res = await fetch('/api/auth/signup', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      email: formData.email,
      password: formData.password,
      name: formData.name,
    }),
  })

  if (res.ok) {
    // 登録成功後、ログインページへ
    router.push('/auth/signin?registered=true')
  }
}
```

#### src/app/auth/forgot-password/page.tsx - パスワードリセット申請

パスワードをリセットするためのリンクをメールで送信します。

**主な機能**:
- メールアドレスを入力してリセットリンクを送信
- セキュリティを考慮した成功メッセージ（メールアドレスの存在を隠す）

**重要なポイント**:
```typescript
const handleSubmit = async (e: React.FormEvent) => {
  e.preventDefault()

  const res = await fetch('/api/auth/forgot-password', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ email: formData.email }),
  })

  // セキュリティ: 常に成功メッセージを表示
  // （メールアドレスの存在を推測されないようにする）
  setMessage('リセットリンクを送信しました。メールをご確認ください。')
}
```

#### src/app/auth/reset-password/page.tsx - パスワードリセット実行

リセットトークンを使って新しいパスワードを設定します。

**主な機能**:
- URLのトークンパラメータを検証
- 新しいパスワードの入力
- パスワードの確認入力

**重要なポイント**:
```typescript
const { searchParams } = useSearchParams()
const token = searchParams.get('token')

const handleSubmit = async (e: React.FormEvent) => {
  e.preventDefault()

  if (!token) {
    setError('無効なリンクです')
    return
  }

  const res = await fetch('/api/auth/reset-password', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      token,
      password: formData.password,
    }),
  })

  if (res.ok) {
    router.push('/auth/signin?reset=true')
  } else {
    setError('トークンが無効または期限切れです')
  }
}
```

---

### 📋 ページ実装のベストプラクティス

```
┌──────────────────────────────────────────────────┐
│     ページコンポーネント設計のポイント           │
└──────────────────────────────────────────────────┘

✅ Server Component + Client Component の分離
  - データ取得 → Server Component
  - インタラクション → Client Component

✅ 楽観的UI更新
  - ユーザーアクション後、即座に画面を更新
  - APIリクエストと並行して実行
  - 失敗時は元に戻す

✅ ローディング状態の管理
  - フォーム送信中はボタンを無効化
  - スケルトンスクリーンで待機時間を短く感じさせる

✅ エラーハンドリング
  - ユーザーにわかりやすいエラーメッセージ
  - セキュリティを考慮した情報公開（メアドの存在など）

✅ アクセシビリティ
  - フォームのlabel要素
  - キーボードナビゲーション
  - ARIA属性（必要に応じて）

✅ レスポンシブデザイン
  - モバイルファーストアプローチ
  - Tailwind CSSのブレークポイント活用
  - touch-friendlyなUI要素
```

---

### 🚀 次のステップ

次の章では、ライブラリとユーティリティの詳細を解説します：

- **Chapter 33**: ライブラリとユーティリティの詳細解説

---

[← 前の章：第31章 API Routesの詳細解説](31-API-Routesの詳細解説.md) | [目次に戻る](00-目次.md) | [次の章へ：第33章 ライブラリとユーティリティの詳細解説 →](33-ライブラリとユーティリティの詳細解説.md)

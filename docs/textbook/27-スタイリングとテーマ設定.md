# 第27章：スタイリングとテーマ設定

この章では、Tailwind CSSの設定、カラーパレット、レスポンシブデザイン、グローバルスタイルについて詳しく解説します。

## 27.1 Tailwind CSS 設定

### tailwind.config.ts（完全版）

```typescript
import type { Config } from 'tailwindcss';

const config: Config = {
  content: [
    './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
    './src/components/**/*.{js,ts,jsx,tsx,mdx}',
    './src/app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // カスタムカラーパレット
        primary: {
          50: '#eff6ff',
          100: '#dbeafe',
          200: '#bfdbfe',
          300: '#93c5fd',
          400: '#60a5fa',
          500: '#3b82f6',
          600: '#2563eb',
          700: '#1d4ed8',
          800: '#1e40af',
          900: '#1e3a8a',
          950: '#172554',
        },
        secondary: {
          50: '#faf5ff',
          100: '#f3e8ff',
          200: '#e9d5ff',
          300: '#d8b4fe',
          400: '#c084fc',
          500: '#a855f7',
          600: '#9333ea',
          700: '#7e22ce',
          800: '#6b21a8',
          900: '#581c87',
          950: '#3b0764',
        },
      },
      fontFamily: {
        sans: [
          'Inter',
          'Noto Sans JP',
          '-apple-system',
          'BlinkMacSystemFont',
          'Segoe UI',
          'Roboto',
          'sans-serif',
        ],
        mono: ['Fira Code', 'Menlo', 'Monaco', 'Courier New', 'monospace'],
      },
      fontSize: {
        'xs': ['0.75rem', { lineHeight: '1rem' }],
        'sm': ['0.875rem', { lineHeight: '1.25rem' }],
        'base': ['1rem', { lineHeight: '1.5rem' }],
        'lg': ['1.125rem', { lineHeight: '1.75rem' }],
        'xl': ['1.25rem', { lineHeight: '1.75rem' }],
        '2xl': ['1.5rem', { lineHeight: '2rem' }],
        '3xl': ['1.875rem', { lineHeight: '2.25rem' }],
        '4xl': ['2.25rem', { lineHeight: '2.5rem' }],
        '5xl': ['3rem', { lineHeight: '1' }],
      },
      spacing: {
        '128': '32rem',
        '144': '36rem',
      },
      borderRadius: {
        '4xl': '2rem',
      },
      boxShadow: {
        'soft': '0 2px 8px rgba(0, 0, 0, 0.05)',
        'medium': '0 4px 16px rgba(0, 0, 0, 0.1)',
        'hard': '0 8px 32px rgba(0, 0, 0, 0.15)',
      },
      animation: {
        'spin-slow': 'spin 3s linear infinite',
        'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite',
        'bounce-slow': 'bounce 2s infinite',
        'fade-in': 'fadeIn 0.3s ease-out',
        'fade-out': 'fadeOut 0.3s ease-out',
        'slide-in-up': 'slideInUp 0.3s ease-out',
        'slide-in-down': 'slideInDown 0.3s ease-out',
        'slide-in-left': 'slideInLeft 0.3s ease-out',
        'slide-in-right': 'slideInRight 0.3s ease-out',
        'scale-in': 'scaleIn 0.2s ease-out',
        'shake': 'shake 0.5s ease-in-out',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0' },
          '100%': { opacity: '1' },
        },
        fadeOut: {
          '0%': { opacity: '1' },
          '100%': { opacity: '0' },
        },
        slideInUp: {
          '0%': { transform: 'translateY(100%)', opacity: '0' },
          '100%': { transform: 'translateY(0)', opacity: '1' },
        },
        slideInDown: {
          '0%': { transform: 'translateY(-100%)', opacity: '0' },
          '100%': { transform: 'translateY(0)', opacity: '1' },
        },
        slideInLeft: {
          '0%': { transform: 'translateX(-100%)', opacity: '0' },
          '100%': { transform: 'translateX(0)', opacity: '1' },
        },
        slideInRight: {
          '0%': { transform: 'translateX(100%)', opacity: '0' },
          '100%': { transform: 'translateX(0)', opacity: '1' },
        },
        scaleIn: {
          '0%': { transform: 'scale(0.9)', opacity: '0' },
          '100%': { transform: 'scale(1)', opacity: '1' },
        },
        shake: {
          '0%, 100%': { transform: 'translateX(0)' },
          '10%, 30%, 50%, 70%, 90%': { transform: 'translateX(-10px)' },
          '20%, 40%, 60%, 80%': { transform: 'translateX(10px)' },
        },
      },
      backgroundImage: {
        'gradient-radial': 'radial-gradient(var(--tw-gradient-stops))',
        'gradient-conic': 'conic-gradient(from 180deg at 50% 50%, var(--tw-gradient-stops))',
      },
    },
  },
  plugins: [],
};

export default config;
```

---

## 27.2 グローバルスタイル

### src/app/globals.css

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

/* カスタムフォント */
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap');
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@300;400;500;700;900&display=swap');

@layer base {
  /* リセットCSS */
  * {
    @apply border-border;
  }
  
  body {
    @apply bg-gray-50 text-gray-900 antialiased;
  }
  
  html {
    @apply scroll-smooth;
  }
  
  /* スクロールバーのカスタマイズ */
  ::-webkit-scrollbar {
    width: 10px;
    height: 10px;
  }
  
  ::-webkit-scrollbar-track {
    @apply bg-gray-100;
  }
  
  ::-webkit-scrollbar-thumb {
    @apply bg-gray-300 rounded-full hover:bg-gray-400;
  }
  
  /* 選択時の色 */
  ::selection {
    @apply bg-blue-200 text-blue-900;
  }
  
  /* フォーカス時のアウトライン */
  *:focus-visible {
    @apply outline-none ring-2 ring-blue-500 ring-offset-2;
  }
}

@layer components {
  /* ボタンスタイル */
  .btn {
    @apply px-4 py-2 rounded-lg font-medium transition-colors focus:outline-none focus:ring-2 focus:ring-offset-2;
  }
  
  .btn-primary {
    @apply bg-blue-600 text-white hover:bg-blue-700 focus:ring-blue-500;
  }
  
  .btn-secondary {
    @apply bg-gray-200 text-gray-900 hover:bg-gray-300 focus:ring-gray-500;
  }
  
  .btn-danger {
    @apply bg-red-600 text-white hover:bg-red-700 focus:ring-red-500;
  }
  
  .btn-success {
    @apply bg-green-600 text-white hover:bg-green-700 focus:ring-green-500;
  }
  
  .btn-outline {
    @apply border-2 border-current bg-transparent hover:bg-current hover:text-white;
  }
  
  .btn-ghost {
    @apply bg-transparent hover:bg-gray-100;
  }
  
  /* インプットスタイル */
  .input {
    @apply w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent;
  }
  
  .input-error {
    @apply border-red-500 focus:ring-red-500;
  }
  
  /* カードスタイル */
  .card {
    @apply bg-white border border-gray-200 rounded-lg p-6 shadow-soft hover:shadow-medium transition-shadow;
  }
  
  .card-flat {
    @apply bg-white border border-gray-200 rounded-lg p-6;
  }
  
  /* リンクスタイル */
  .link {
    @apply text-blue-600 hover:text-blue-700 hover:underline transition-colors;
  }
  
  /* バッジスタイル */
  .badge {
    @apply inline-flex items-center px-3 py-1 rounded-full text-sm font-medium;
  }
  
  .badge-primary {
    @apply bg-blue-100 text-blue-800;
  }
  
  .badge-success {
    @apply bg-green-100 text-green-800;
  }
  
  .badge-warning {
    @apply bg-yellow-100 text-yellow-800;
  }
  
  .badge-danger {
    @apply bg-red-100 text-red-800;
  }
  
  /* アラートスタイル */
  .alert {
    @apply border rounded-lg p-4;
  }
  
  .alert-info {
    @apply bg-blue-50 border-blue-200 text-blue-800;
  }
  
  .alert-success {
    @apply bg-green-50 border-green-200 text-green-800;
  }
  
  .alert-warning {
    @apply bg-yellow-50 border-yellow-200 text-yellow-800;
  }
  
  .alert-error {
    @apply bg-red-50 border-red-200 text-red-800;
  }
  
  /* コンテナスタイル */
  .container-narrow {
    @apply max-w-4xl mx-auto px-4;
  }
  
  .container-wide {
    @apply max-w-7xl mx-auto px-4;
  }
  
  /* グラデーション背景 */
  .gradient-bg-1 {
    @apply bg-gradient-to-r from-blue-500 to-purple-600;
  }
  
  .gradient-bg-2 {
    @apply bg-gradient-to-br from-pink-500 via-red-500 to-yellow-500;
  }
  
  .gradient-bg-3 {
    @apply bg-gradient-to-r from-green-400 to-blue-500;
  }
  
  /* テキストグラデーション */
  .gradient-text {
    @apply bg-clip-text text-transparent bg-gradient-to-r from-blue-600 to-purple-600;
  }
}

@layer utilities {
  /* テキストユーティリティ */
  .text-balance {
    text-wrap: balance;
  }
  
  /* 行数制限 */
  .line-clamp-1 {
    display: -webkit-box;
    -webkit-line-clamp: 1;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  
  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  
  .line-clamp-3 {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  
  /* ガラスモーフィズム */
  .glass {
    @apply backdrop-blur-md bg-white/30 border border-white/20;
  }
  
  /* ネオモーフィズム */
  .neo {
    box-shadow: 8px 8px 16px rgba(0, 0, 0, 0.1), -8px -8px 16px rgba(255, 255, 255, 0.5);
  }
  
  .neo-inset {
    box-shadow: inset 8px 8px 16px rgba(0, 0, 0, 0.1), inset -8px -8px 16px rgba(255, 255, 255, 0.5);
  }
  
  /* アニメーション遅延 */
  .animation-delay-100 {
    animation-delay: 100ms;
  }
  
  .animation-delay-200 {
    animation-delay: 200ms;
  }
  
  .animation-delay-300 {
    animation-delay: 300ms;
  }
  
  /* カスタムスクロール */
  .no-scrollbar::-webkit-scrollbar {
    display: none;
  }
  
  .no-scrollbar {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
}

/* Markdown スタイル */
.prose {
  @apply text-gray-800 max-w-none;
}

.prose h1 {
  @apply text-3xl font-bold mb-4 mt-8;
}

.prose h2 {
  @apply text-2xl font-bold mb-3 mt-6;
}

.prose h3 {
  @apply text-xl font-bold mb-2 mt-4;
}

.prose p {
  @apply mb-4 leading-relaxed;
}

.prose a {
  @apply text-blue-600 hover:text-blue-700 hover:underline;
}

.prose ul,
.prose ol {
  @apply mb-4 ml-6;
}

.prose li {
  @apply mb-2;
}

.prose code {
  @apply bg-gray-100 px-2 py-1 rounded text-sm font-mono;
}

.prose pre {
  @apply bg-gray-900 text-gray-100 p-4 rounded-lg overflow-x-auto mb-4;
}

.prose blockquote {
  @apply border-l-4 border-gray-300 pl-4 italic my-4;
}

.prose table {
  @apply w-full border-collapse mb-4;
}

.prose th,
.prose td {
  @apply border border-gray-300 px-4 py-2;
}

.prose th {
  @apply bg-gray-100 font-bold;
}

/* プリント用スタイル */
@media print {
  body {
    @apply bg-white;
  }
  
  .no-print {
    display: none !important;
  }
  
  a[href]:after {
    content: " (" attr(href) ")";
  }
}
```

---

## 27.3 レスポンシブブレークポイント

### レスポンシブデザインガイド

```typescript
// Tailwind CSSのデフォルトブレークポイント
const breakpoints = {
  sm: '640px',   // スマートフォン（縦）
  md: '768px',   // タブレット（縦）
  lg: '1024px',  // タブレット（横）/ ノートPC
  xl: '1280px',  // デスクトップ
  '2xl': '1536px', // 大画面デスクトップ
};

// 使用例
/*
<div className="
  w-full           // モバイル: 100%幅
  sm:w-1/2         // 640px以上: 50%幅
  md:w-1/3         // 768px以上: 33%幅
  lg:w-1/4         // 1024px以上: 25%幅
">
  コンテンツ
</div>
*/
```

### レスポンシブユーティリティコンポーネント

```typescript
// components/Responsive.tsx
'use client';

import { useEffect, useState } from 'react';

export function useMediaQuery(query: string): boolean {
  const [matches, setMatches] = useState(false);
  
  useEffect(() => {
    const media = window.matchMedia(query);
    setMatches(media.matches);
    
    const listener = (e: MediaQueryListEvent) => setMatches(e.matches);
    media.addEventListener('change', listener);
    
    return () => media.removeEventListener('change', listener);
  }, [query]);
  
  return matches;
}

// カスタムフック
export function useIsMobile() {
  return useMediaQuery('(max-width: 768px)');
}

export function useIsTablet() {
  return useMediaQuery('(min-width: 768px) and (max-width: 1024px)');
}

export function useIsDesktop() {
  return useMediaQuery('(min-width: 1024px)');
}

// 使用例
/*
const isMobile = useIsMobile();

return (
  <div>
    {isMobile ? (
      <MobileNav />
    ) : (
      <DesktopNav />
    )}
  </div>
);
*/
```

---

## 27.4 カラーパレット定義

### lib/constants/colors.ts

```typescript
export const colors = {
  // プライマリカラー
  primary: {
    light: '#60a5fa',
    main: '#3b82f6',
    dark: '#2563eb',
  },
  
  // セカンダリカラー
  secondary: {
    light: '#c084fc',
    main: '#a855f7',
    dark: '#9333ea',
  },
  
  // 状態カラー
  success: {
    light: '#86efac',
    main: '#22c55e',
    dark: '#16a34a',
  },
  
  warning: {
    light: '#fde047',
    main: '#eab308',
    dark: '#ca8a04',
  },
  
  error: {
    light: '#fca5a5',
    main: '#ef4444',
    dark: '#dc2626',
  },
  
  info: {
    light: '#7dd3fc',
    main: '#0ea5e9',
    dark: '#0284c7',
  },
  
  // グレースケール
  gray: {
    50: '#f9fafb',
    100: '#f3f4f6',
    200: '#e5e7eb',
    300: '#d1d5db',
    400: '#9ca3af',
    500: '#6b7280',
    600: '#4b5563',
    700: '#374151',
    800: '#1f2937',
    900: '#111827',
  },
};

// カラーヘルパー関数
export function getColorClass(color: keyof typeof colors, variant: 'light' | 'main' | 'dark' = 'main') {
  return colors[color]?.[variant] || colors.primary.main;
}
```

---

## 27.5 アイコンライブラリ活用

### lib/constants/icons.ts

```typescript
import {
  Home,
  Users,
  Calendar,
  FileText,
  MessageCircle,
  Settings,
  LogIn,
  LogOut,
  User,
  Heart,
  Share2,
  Edit,
  Trash2,
  Search,
  Plus,
  X,
  Check,
  AlertCircle,
  Info,
  type LucideIcon,
} from 'lucide-react';

export const icons = {
  // ナビゲーション
  home: Home,
  users: Users,
  calendar: Calendar,
  posts: FileText,
  chat: MessageCircle,
  settings: Settings,
  
  // 認証
  login: LogIn,
  logout: LogOut,
  user: User,
  
  // アクション
  like: Heart,
  share: Share2,
  edit: Edit,
  delete: Trash2,
  search: Search,
  add: Plus,
  close: X,
  check: Check,
  
  // 状態
  alert: AlertCircle,
  info: Info,
};

// アイコン選択ヘルパー
export function getIcon(name: keyof typeof icons): LucideIcon {
  return icons[name] || Info;
}
```

---

## 27.6 スペーシングシステム

### スペーシングガイド

```typescript
// Tailwind CSSのスペーシングスケール
const spacing = {
  0: '0px',
  1: '0.25rem',  // 4px
  2: '0.5rem',   // 8px
  3: '0.75rem',  // 12px
  4: '1rem',     // 16px
  5: '1.25rem',  // 20px
  6: '1.5rem',   // 24px
  8: '2rem',     // 32px
  10: '2.5rem',  // 40px
  12: '3rem',    // 48px
  16: '4rem',    // 64px
  20: '5rem',    // 80px
  24: '6rem',    // 96px
  32: '8rem',    // 128px
};

// 推奨される使用法
const recommendations = {
  // セクション間の余白
  sectionGap: 'gap-8 md:gap-12 lg:gap-16',
  
  // カード内のパディング
  cardPadding: 'p-4 md:p-6',
  
  // ボタン内のパディング
  buttonPadding: 'px-4 py-2 md:px-6 md:py-3',
  
  // テキスト間の余白
  textGap: 'space-y-4',
  
  // グリッド間の余白
  gridGap: 'gap-4 md:gap-6',
};
```

---

## 27.7 タイポグラフィシステム

### lib/constants/typography.ts

```typescript
export const typography = {
  // 見出し
  h1: 'text-4xl md:text-5xl font-bold leading-tight',
  h2: 'text-3xl md:text-4xl font-bold leading-tight',
  h3: 'text-2xl md:text-3xl font-bold leading-snug',
  h4: 'text-xl md:text-2xl font-bold leading-snug',
  h5: 'text-lg md:text-xl font-bold leading-normal',
  h6: 'text-base md:text-lg font-bold leading-normal',
  
  // 本文
  body1: 'text-base leading-relaxed',
  body2: 'text-sm leading-relaxed',
  
  // キャプション
  caption: 'text-xs text-gray-600',
  
  // ラベル
  label: 'text-sm font-medium',
  
  // コード
  code: 'font-mono text-sm bg-gray-100 px-2 py-1 rounded',
};

// タイポグラフィコンポーネント
export function Heading({ 
  level = 'h1', 
  children, 
  className = '' 
}: { 
  level?: keyof typeof typography; 
  children: React.ReactNode; 
  className?: string;
}) {
  const Component = level.startsWith('h') ? level : 'h1';
  
  return (
    <Component className={`${typography[level]} ${className}`}>
      {children}
    </Component>
  );
}
```

---

## 27.8 シャドウシステム

### シャドウ活用例

```typescript
// components/ShadowExamples.tsx
export function ShadowExamples() {
  return (
    <div className="space-y-8 p-8">
      {/* 小さいシャドウ */}
      <div className="shadow-sm p-6 bg-white rounded-lg">
        shadow-sm: 微妙な影
      </div>
      
      {/* 標準シャドウ */}
      <div className="shadow p-6 bg-white rounded-lg">
        shadow: 標準的な影
      </div>
      
      {/* 中程度のシャドウ */}
      <div className="shadow-md p-6 bg-white rounded-lg">
        shadow-md: やや強めの影
      </div>
      
      {/* 大きいシャドウ */}
      <div className="shadow-lg p-6 bg-white rounded-lg">
        shadow-lg: 大きな影
      </div>
      
      {/* 特大シャドウ */}
      <div className="shadow-xl p-6 bg-white rounded-lg">
        shadow-xl: 特大の影
      </div>
      
      {/* カスタムシャドウ */}
      <div className="shadow-soft p-6 bg-white rounded-lg">
        shadow-soft: カスタム（柔らかい）
      </div>
      
      <div className="shadow-medium p-6 bg-white rounded-lg">
        shadow-medium: カスタム（中程度）
      </div>
      
      <div className="shadow-hard p-6 bg-white rounded-lg">
        shadow-hard: カスタム（強い）
      </div>
      
      {/* ホバーエフェクト */}
      <div className="shadow hover:shadow-lg transition-shadow p-6 bg-white rounded-lg cursor-pointer">
        ホバーで影が変化
      </div>
    </div>
  );
}
```

---

## 27.9 アニメーション活用例

### components/AnimationExamples.tsx

```typescript
'use client';

import { useState } from 'react';

export function AnimationExamples() {
  const [show, setShow] = useState(false);
  
  return (
    <div className="space-y-8 p-8">
      <button
        onClick={() => setShow(!show)}
        className="btn btn-primary"
      >
        アニメーションを{show ? '非表示' : '表示'}
      </button>
      
      {show && (
        <div className="space-y-4">
          {/* フェードイン */}
          <div className="animate-fade-in p-4 bg-blue-100 rounded-lg">
            フェードイン
          </div>
          
          {/* スライドイン（上から） */}
          <div className="animate-slide-in-down p-4 bg-green-100 rounded-lg">
            スライドイン（上から）
          </div>
          
          {/* スライドイン（下から） */}
          <div className="animate-slide-in-up p-4 bg-purple-100 rounded-lg">
            スライドイン（下から）
          </div>
          
          {/* スライドイン（左から） */}
          <div className="animate-slide-in-left p-4 bg-yellow-100 rounded-lg">
            スライドイン（左から）
          </div>
          
          {/* スライドイン（右から） */}
          <div className="animate-slide-in-right p-4 bg-pink-100 rounded-lg">
            スライドイン（右から）
          </div>
          
          {/* スケールイン */}
          <div className="animate-scale-in p-4 bg-red-100 rounded-lg">
            スケールイン
          </div>
          
          {/* 連続アニメーション */}
          <div className="flex gap-4">
            <div className="animate-bounce p-4 bg-blue-500 text-white rounded-full w-12 h-12"></div>
            <div className="animate-pulse p-4 bg-green-500 text-white rounded-full w-12 h-12"></div>
            <div className="animate-spin p-4 bg-purple-500 text-white rounded-full w-12 h-12"></div>
          </div>
          
          {/* 遅延アニメーション */}
          <div className="flex gap-4">
            <div className="animate-fade-in p-4 bg-orange-100 rounded-lg">
              即座
            </div>
            <div className="animate-fade-in animation-delay-100 p-4 bg-orange-100 rounded-lg">
              100ms遅延
            </div>
            <div className="animate-fade-in animation-delay-200 p-4 bg-orange-100 rounded-lg">
              200ms遅延
            </div>
            <div className="animate-fade-in animation-delay-300 p-4 bg-orange-100 rounded-lg">
              300ms遅延
            </div>
          </div>
        </div>
      )}
    </div>
  );
}
```

---

## 27.10 デザイントークン

### lib/constants/design-tokens.ts

```typescript
export const designTokens = {
  // ボーダー半径
  borderRadius: {
    none: '0',
    sm: '0.25rem',
    md: '0.5rem',
    lg: '0.75rem',
    xl: '1rem',
    '2xl': '1.5rem',
    full: '9999px',
  },
  
  // トランジション
  transition: {
    fast: '150ms',
    normal: '300ms',
    slow: '500ms',
  },
  
  // Z-index
  zIndex: {
    dropdown: 1000,
    sticky: 1020,
    fixed: 1030,
    modalBackdrop: 1040,
    modal: 1050,
    popover: 1060,
    tooltip: 1070,
  },
  
  // フォントウェイト
  fontWeight: {
    light: 300,
    normal: 400,
    medium: 500,
    semibold: 600,
    bold: 700,
    extrabold: 800,
    black: 900,
  },
  
  // 行高
  lineHeight: {
    none: 1,
    tight: 1.25,
    snug: 1.375,
    normal: 1.5,
    relaxed: 1.625,
    loose: 2,
  },
};
```

---

## まとめ

この章では、Tailwind CSSを使ったスタイリングシステムについて学びました：

### Tailwind CSS設定
- ✅ **カスタムカラー**: プライマリ・セカンダリカラーの定義
- ✅ **フォント設定**: Inter、Noto Sans JPの設定
- ✅ **アニメーション**: fade、slide、scaleなど多数
- ✅ **カスタムシャドウ**: soft、medium、hardシャドウ

### グローバルスタイル
- ✅ **コンポーネントクラス**: btn、input、card、badgeなど
- ✅ **ユーティリティ**: line-clamp、glass、neoなど
- ✅ **Markdownスタイル**: prose クラス
- ✅ **スクロールバー**: カスタムデザイン

### レスポンシブデザイン
- ✅ **ブレークポイント**: sm、md、lg、xl、2xl
- ✅ **カスタムフック**: useIsMobile、useIsDesktop
- ✅ **モバイルファースト**: 最小から最大へ

### デザインシステム
- ✅ **カラーパレット**: 一貫した色使用
- ✅ **スペーシング**: 統一された余白
- ✅ **タイポグラフィ**: 見出し・本文スタイル
- ✅ **デザイントークン**: 再利用可能な値

### アニメーション
- ✅ **トランジション**: スムーズな状態変化
- ✅ **キーフレーム**: カスタムアニメーション
- ✅ **遅延実行**: 段階的な表示
- ✅ **ホバー効果**: インタラクティブなUI

### ベストプラクティス
- ✅ **一貫性**: デザイントークンで統一
- ✅ **保守性**: カスタムクラスで再利用
- ✅ **パフォーマンス**: 必要な機能のみ使用
- ✅ **アクセシビリティ**: フォーカス状態の考慮

次の章では、**デプロイとCI/CD**について詳しく見ていきます。

---

[← 前の章：第26章 ユーティリティ関数の実装](26-ユーティリティ関数の実装.md) | [目次に戻る](00-目次.md) | [次の章へ：第28章 デプロイとCI/CD →](28-デプロイとCI_CD.md)

# 第 24 章：機能の追加とカスタマイズ

> **この章では、アプリケーションに新しい機能を追加する方法を学びます**

## 📚 この章で学ぶこと

- ✅ 新しいページの追加方法
- ✅ データベースモデルの追加方法
- ✅ API の実装方法
- ✅ UI の実装方法
- ✅ デザインのカスタマイズ方法

## 💡 機能追加の全体像

```
┌──────────────────────────────────────────────────┐
│     新機能追加の標準フロー                        │
└──────────────────────────────────────────────────┘

【ステップ1】企画・設計
   ├─ どんな機能を作るか決める
   ├─ 必要なデータを洗い出す
   └─ 画面設計を考える
        ↓
【ステップ2】データモデル追加
   ├─ Prismaスキーマを更新
   ├─ マイグレーション実行
   └─ Prisma Clientの再生成
        ↓
【ステップ3】API実装
   ├─ API Routeを作成
   ├─ CRUD処理を実装
   └─ 権限チェック・バリデーション
        ↓
【ステップ4】UI実装
   ├─ ページコンポーネント作成
   ├─ フォームコンポーネント作成
   └─ 一覧・詳細画面の実装
        ↓
【ステップ5】動作確認
   ├─ 手動テスト
   ├─ エッジケースの確認
   └─ デプロイ


┌────────────────────────────────────┐
│  実例: 「お知らせ機能」を追加       │
├────────────────────────────────────┤
│                                    │
│  【どんな機能？】                  │
│  運営側が全メンバーにお知らせを配信│
│                                    │
│  【必要なデータ】                  │
│  ├─ タイトル                      │
│  ├─ 内容                          │
│  ├─ 投稿者                        │
│  ├─ 重要度（普通/重要）            │
│  └─ 投稿日時                      │
│                                    │
│  【画面】                          │
│  ├─ お知らせ一覧ページ            │
│  ├─ お知らせ詳細ページ            │
│  └─ お知らせ作成フォーム（管理者） │
│                                    │
└────────────────────────────────────┘
```

---

## 24.1 新しいページの追加

### 実例: 「お知らせ機能」を追加する

この章では、実際に「お知らせ機能」を 1 から作成していきます。

### Step 1: データモデルの追加

**1-1. Prisma スキーマを更新**

`prisma/schema.prisma` に Announcement モデルを追加：

```prisma
// Announcementモデルの追加
model Announcement {
  id          String   @id @default(cuid())
  title       String   // タイトル
  content     String   @db.Text // 内容（Markdown）
  priority    String   @default("normal") // "normal" | "important"
  authorId    String   // 投稿者ID
  author      User     @relation(fields: [authorId], references: [id], onDelete: Cascade)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("announcements")
}

// Userモデルにリレーションを追加
model User {
  // ...既存のフィールド...

  // お知らせ（追加）
  announcements Announcement[]

  // ...他のリレーション...
}
```

**1-2. データベースに反映**

```bash
# データベースに反映
npx prisma db push

# Prisma Clientを再生成
npx prisma generate
```

**出力例:**

```
Environment variables loaded from .env
Prisma schema loaded from prisma/schema.prisma
Datasource "db": PostgreSQL database

🚀  Your database is now in sync with your Prisma schema.

✔ Generated Prisma Client (5.22.0)
```

→ ✅ データモデルが追加されました！

### Step 2: API Route の実装

**2-1. お知らせ一覧・作成 API**

`src/app/api/announcements/route.ts` を作成：

```typescript
import { NextRequest, NextResponse } from "next/server";
import { auth } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
import { z } from "zod";

// 入力検証スキーマ
const createAnnouncementSchema = z.object({
  title: z.string().min(1, "タイトルは必須です").max(200),
  content: z.string().min(1, "内容は必須です").max(10000),
  priority: z.enum(["normal", "important"]).default("normal"),
});

// GET: お知らせ一覧を取得
export async function GET() {
  try {
    // 新しい順に取得
    const announcements = await prisma.announcement.findMany({
      include: {
        author: {
          select: {
            id: true,
            name: true,
            email: true,
            image: true,
          },
        },
      },
      orderBy: {
        createdAt: "desc",
      },
    });

    return NextResponse.json(announcements);
  } catch (error) {
    console.error("お知らせの取得に失敗:", error);
    return NextResponse.json(
      { error: "お知らせの取得に失敗しました" },
      { status: 500 }
    );
  }
}

// POST: お知らせを作成（管理者のみ）
export async function POST(request: NextRequest) {
  try {
    // 1. 認証チェック
    const session = await auth();
    if (!session?.user) {
      return NextResponse.json({ error: "認証が必要です" }, { status: 401 });
    }

    // 2. 権限チェック（管理者のみ）
    if (session.user.role !== "admin") {
      return NextResponse.json(
        { error: "管理者のみお知らせを作成できます" },
        { status: 403 }
      );
    }

    // 3. 入力を検証
    const body = await request.json();
    const validatedData = createAnnouncementSchema.parse(body);

    // 4. お知らせを作成
    const announcement = await prisma.announcement.create({
      data: {
        ...validatedData,
        authorId: session.user.id,
      },
      include: {
        author: {
          select: {
            id: true,
            name: true,
            email: true,
            image: true,
          },
        },
      },
    });

    return NextResponse.json(announcement, { status: 201 });
  } catch (error) {
    if (error instanceof z.ZodError) {
      return NextResponse.json(
        { error: "入力が不正です", details: error.errors },
        { status: 400 }
      );
    }

    console.error("お知らせの作成に失敗:", error);
    return NextResponse.json(
      { error: "お知らせの作成に失敗しました" },
      { status: 500 }
    );
  }
}
```

**2-2. お知らせ詳細・更新・削除 API**

`src/app/api/announcements/[id]/route.ts` を作成：

```typescript
import { NextRequest, NextResponse } from "next/server";
import { auth } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
import { z } from "zod";

// 入力検証スキーマ
const updateAnnouncementSchema = z.object({
  title: z.string().min(1).max(200).optional(),
  content: z.string().min(1).max(10000).optional(),
  priority: z.enum(["normal", "important"]).optional(),
});

// GET: お知らせ詳細を取得
export async function GET(
  request: NextRequest,
  { params }: { params: { id: string } }
) {
  try {
    const announcement = await prisma.announcement.findUnique({
      where: { id: params.id },
      include: {
        author: {
          select: {
            id: true,
            name: true,
            email: true,
            image: true,
          },
        },
      },
    });

    if (!announcement) {
      return NextResponse.json(
        { error: "お知らせが見つかりません" },
        { status: 404 }
      );
    }

    return NextResponse.json(announcement);
  } catch (error) {
    console.error("お知らせの取得に失敗:", error);
    return NextResponse.json(
      { error: "お知らせの取得に失敗しました" },
      { status: 500 }
    );
  }
}

// PATCH: お知らせを更新（管理者のみ）
export async function PATCH(
  request: NextRequest,
  { params }: { params: { id: string } }
) {
  try {
    const session = await auth();
    if (!session?.user || session.user.role !== "admin") {
      return NextResponse.json({ error: "権限がありません" }, { status: 403 });
    }

    const body = await request.json();
    const validatedData = updateAnnouncementSchema.parse(body);

    const announcement = await prisma.announcement.update({
      where: { id: params.id },
      data: validatedData,
      include: {
        author: {
          select: {
            id: true,
            name: true,
            email: true,
            image: true,
          },
        },
      },
    });

    return NextResponse.json(announcement);
  } catch (error) {
    if (error instanceof z.ZodError) {
      return NextResponse.json(
        { error: "入力が不正です", details: error.errors },
        { status: 400 }
      );
    }

    console.error("お知らせの更新に失敗:", error);
    return NextResponse.json(
      { error: "お知らせの更新に失敗しました" },
      { status: 500 }
    );
  }
}

// DELETE: お知らせを削除（管理者のみ）
export async function DELETE(
  request: NextRequest,
  { params }: { params: { id: string } }
) {
  try {
    const session = await auth();
    if (!session?.user || session.user.role !== "admin") {
      return NextResponse.json({ error: "権限がありません" }, { status: 403 });
    }

    await prisma.announcement.delete({
      where: { id: params.id },
    });

    return NextResponse.json({ message: "お知らせを削除しました" });
  } catch (error) {
    console.error("お知らせの削除に失敗:", error);
    return NextResponse.json(
      { error: "お知らせの削除に失敗しました" },
      { status: 500 }
    );
  }
}
```

**API の構成:**

```
src/app/api/announcements/
├─ route.ts             # GET（一覧取得）, POST（作成）
└─ [id]/
   └─ route.ts          # GET（詳細取得）, PATCH（更新）, DELETE（削除）


┌────────────────────────────────────┐
│  API エンドポイント一覧             │
├────────────────────────────────────┤
│                                    │
│  GET    /api/announcements         │
│  → お知らせ一覧を取得               │
│                                    │
│  POST   /api/announcements         │
│  → お知らせを作成（管理者のみ）     │
│                                    │
│  GET    /api/announcements/[id]    │
│  → お知らせ詳細を取得               │
│                                    │
│  PATCH  /api/announcements/[id]    │
│  → お知らせを更新（管理者のみ）     │
│                                    │
│  DELETE /api/announcements/[id]    │
│  → お知らせを削除（管理者のみ）     │
│                                    │
└────────────────────────────────────┘
```

### Step 3: お知らせ一覧ページの実装

**3-1. お知らせ一覧ページ**

`src/app/announcements/page.tsx` を作成：

```typescript
import { Suspense } from "react";
import Link from "next/link";
import { auth } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
import Avatar from "@/components/Avatar";
import { formatDistanceToNow } from "date-fns";
import { ja } from "date-fns/locale";
import { Megaphone, Plus, AlertCircle } from "lucide-react";

// お知らせ一覧を取得
async function getAnnouncements() {
  const announcements = await prisma.announcement.findMany({
    include: {
      author: true,
    },
    orderBy: {
      createdAt: "desc",
    },
  });
  return announcements;
}

export default async function AnnouncementsPage() {
  const session = await auth();
  const announcements = await getAnnouncements();

  return (
    <div className="max-w-4xl mx-auto px-4 py-8">
      {/* ヘッダー */}
      <div className="flex items-center justify-between mb-8">
        <div className="flex items-center gap-3">
          <Megaphone className="w-8 h-8 text-blue-600" />
          <h1 className="text-3xl font-bold">お知らせ</h1>
        </div>

        {/* 管理者のみ作成ボタン表示 */}
        {session?.user?.role === "admin" && (
          <Link
            href="/announcements/new"
            className="flex items-center gap-2 px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition"
          >
            <Plus className="w-5 h-5" />
            お知らせを作成
          </Link>
        )}
      </div>

      {/* お知らせ一覧 */}
      <div className="space-y-4">
        {announcements.length === 0 ? (
          <div className="text-center py-12 text-gray-500">
            お知らせはまだありません
          </div>
        ) : (
          announcements.map((announcement) => (
            <Link
              key={announcement.id}
              href={`/announcements/${announcement.id}`}
              className="block"
            >
              <div className="bg-white border rounded-lg p-6 hover:shadow-md transition">
                {/* タイトルと重要マーク */}
                <div className="flex items-start gap-3 mb-3">
                  {announcement.priority === "important" && (
                    <AlertCircle className="w-6 h-6 text-red-500 flex-shrink-0 mt-1" />
                  )}
                  <h2 className="text-xl font-semibold flex-1">
                    {announcement.title}
                  </h2>
                </div>

                {/* 内容のプレビュー */}
                <p className="text-gray-600 mb-4 line-clamp-2">
                  {announcement.content.slice(0, 150)}
                  {announcement.content.length > 150 && "..."}
                </p>

                {/* メタ情報 */}
                <div className="flex items-center gap-4 text-sm text-gray-500">
                  <div className="flex items-center gap-2">
                    <Avatar
                      src={announcement.author.image}
                      name={announcement.author.name || ""}
                      size="sm"
                    />
                    <span>{announcement.author.name}</span>
                  </div>
                  <span>
                    {formatDistanceToNow(new Date(announcement.createdAt), {
                      addSuffix: true,
                      locale: ja,
                    })}
                  </span>
                </div>
              </div>
            </Link>
          ))
        )}
      </div>
    </div>
  );
}
```

**3-2. お知らせ作成ページ**

`src/app/announcements/new/page.tsx` を作成：

```typescript
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import dynamic from "next/dynamic";
import { AlertCircle, Loader2 } from "lucide-react";

// Markdownエディタを動的インポート
const MarkdownEditor = dynamic(() => import("@/components/MarkdownEditor"), {
  loading: () => <div className="h-96 bg-gray-100 rounded-lg animate-pulse" />,
  ssr: false,
});

export default function CreateAnnouncementPage() {
  const router = useRouter();
  const [title, setTitle] = useState("");
  const [content, setContent] = useState("");
  const [priority, setPriority] = useState<"normal" | "important">("normal");
  const [isLoading, setIsLoading] = useState(false);
  const [error, setError] = useState("");

  // フォーム送信
  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setIsLoading(true);
    setError("");

    try {
      const response = await fetch("/api/announcements", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          title,
          content,
          priority,
        }),
      });

      if (!response.ok) {
        const data = await response.json();
        throw new Error(data.error || "お知らせの作成に失敗しました");
      }

      const announcement = await response.json();
      router.push(`/announcements/${announcement.id}`);
    } catch (err) {
      setError(err instanceof Error ? err.message : "エラーが発生しました");
    } finally {
      setIsLoading(false);
    }
  };

  return (
    <div className="max-w-4xl mx-auto px-4 py-8">
      <h1 className="text-3xl font-bold mb-8">お知らせを作成</h1>

      {/* エラー表示 */}
      {error && (
        <div className="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg mb-6">
          {error}
        </div>
      )}

      <form onSubmit={handleSubmit} className="space-y-6">
        {/* タイトル */}
        <div>
          <label htmlFor="title" className="block text-sm font-medium mb-2">
            タイトル <span className="text-red-500">*</span>
          </label>
          <input
            type="text"
            id="title"
            value={title}
            onChange={(e) => setTitle(e.target.value)}
            required
            maxLength={200}
            className="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
            placeholder="タイトルを入力..."
          />
        </div>

        {/* 重要度 */}
        <div>
          <label className="block text-sm font-medium mb-2">重要度</label>
          <div className="flex gap-4">
            <label className="flex items-center gap-2 cursor-pointer">
              <input
                type="radio"
                name="priority"
                value="normal"
                checked={priority === "normal"}
                onChange={(e) =>
                  setPriority(e.target.value as "normal" | "important")
                }
                className="w-4 h-4 text-blue-600"
              />
              <span>通常</span>
            </label>
            <label className="flex items-center gap-2 cursor-pointer">
              <input
                type="radio"
                name="priority"
                value="important"
                checked={priority === "important"}
                onChange={(e) =>
                  setPriority(e.target.value as "normal" | "important")
                }
                className="w-4 h-4 text-red-600"
              />
              <span className="flex items-center gap-1">
                <AlertCircle className="w-4 h-4 text-red-500" />
                重要
              </span>
            </label>
          </div>
        </div>

        {/* 内容 */}
        <div>
          <label className="block text-sm font-medium mb-2">
            内容 <span className="text-red-500">*</span>
          </label>
          <MarkdownEditor value={content} onChange={setContent} />
        </div>

        {/* ボタン */}
        <div className="flex gap-4">
          <button
            type="submit"
            disabled={isLoading || !title || !content}
            className="px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 disabled:opacity-50 disabled:cursor-not-allowed flex items-center gap-2"
          >
            {isLoading && <Loader2 className="w-4 h-4 animate-spin" />}
            作成する
          </button>
          <button
            type="button"
            onClick={() => router.back()}
            className="px-6 py-2 border rounded-lg hover:bg-gray-50"
          >
            キャンセル
          </button>
        </div>
      </form>
    </div>
  );
}
```

**3-3. お知らせ詳細ページ**

`src/app/announcements/[id]/page.tsx` を作成：

```typescript
import { notFound } from "next/navigation";
import Link from "next/link";
import { auth } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
import Avatar from "@/components/Avatar";
import MarkdownContent from "@/components/MarkdownContent";
import { formatDistanceToNow } from "date-fns";
import { ja } from "date-fns/locale";
import { AlertCircle, Edit, Trash2, ArrowLeft } from "lucide-react";

// お知らせ詳細を取得
async function getAnnouncement(id: string) {
  const announcement = await prisma.announcement.findUnique({
    where: { id },
    include: {
      author: true,
    },
  });

  if (!announcement) {
    notFound();
  }

  return announcement;
}

export default async function AnnouncementDetailPage({
  params,
}: {
  params: { id: string };
}) {
  const session = await auth();
  const announcement = await getAnnouncement(params.id);

  return (
    <div className="max-w-4xl mx-auto px-4 py-8">
      {/* 戻るボタン */}
      <Link
        href="/announcements"
        className="inline-flex items-center gap-2 text-blue-600 hover:text-blue-700 mb-6"
      >
        <ArrowLeft className="w-4 h-4" />
        一覧に戻る
      </Link>

      {/* お知らせカード */}
      <div className="bg-white border rounded-lg p-8">
        {/* ヘッダー */}
        <div className="flex items-start justify-between mb-6">
          <div className="flex-1">
            {/* 重要マーク */}
            {announcement.priority === "important" && (
              <div className="flex items-center gap-2 text-red-600 mb-2">
                <AlertCircle className="w-5 h-5" />
                <span className="font-semibold">重要なお知らせ</span>
              </div>
            )}

            {/* タイトル */}
            <h1 className="text-3xl font-bold mb-4">{announcement.title}</h1>

            {/* メタ情報 */}
            <div className="flex items-center gap-4 text-sm text-gray-600">
              <div className="flex items-center gap-2">
                <Avatar
                  src={announcement.author.image}
                  name={announcement.author.name || ""}
                  size="sm"
                />
                <span>{announcement.author.name}</span>
              </div>
              <span>
                {formatDistanceToNow(new Date(announcement.createdAt), {
                  addSuffix: true,
                  locale: ja,
                })}
              </span>
            </div>
          </div>

          {/* 編集・削除ボタン（管理者のみ） */}
          {session?.user?.role === "admin" && (
            <div className="flex gap-2">
              <Link
                href={`/announcements/${announcement.id}/edit`}
                className="p-2 text-blue-600 hover:bg-blue-50 rounded-lg"
              >
                <Edit className="w-5 h-5" />
              </Link>
              <button className="p-2 text-red-600 hover:bg-red-50 rounded-lg">
                <Trash2 className="w-5 h-5" />
              </button>
            </div>
          )}
        </div>

        {/* 本文 */}
        <div className="prose prose-lg max-w-none">
          <MarkdownContent content={announcement.content} />
        </div>
      </div>
    </div>
  );
}
```

### Step 4: ナビゲーションに追加

`src/components/Header.tsx` にお知らせへのリンクを追加：

```typescript
// ナビゲーションリンクにお知らせを追加
<Link
  href="/announcements"
  className="text-gray-700 hover:text-blue-600 transition"
>
  お知らせ
</Link>
```

### Step 5: 動作確認

**5-1. 開発サーバーを起動**

```bash
npm run dev
```

**5-2. 確認項目**

```
✓ お知らせ一覧ページにアクセスできる
  → http://localhost:3000/announcements

✓ 管理者でログインすると「作成」ボタンが表示される

✓ お知らせを作成できる
  ├─ タイトルを入力
  ├─ 重要度を選択
  └─ 内容をMarkdownで入力

✓ お知らせ詳細ページで内容が表示される

✓ 重要なお知らせには赤いマークが表示される

✓ 一般メンバーでは作成ボタンが表示されない
```

→ ✅ お知らせ機能が完成しました！

---

## 24.2 デザインのカスタマイズ

### 色の変更

Tailwind CSS の色をカスタマイズします。

**tailwind.config.ts を更新:**

```typescript
import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./src/pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/components/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      colors: {
        // カスタムカラーパレット
        primary: {
          50: "#eff6ff",
          100: "#dbeafe",
          200: "#bfdbfe",
          300: "#93c5fd",
          400: "#60a5fa",
          500: "#3b82f6", // デフォルトの青
          600: "#2563eb",
          700: "#1d4ed8",
          800: "#1e40af",
          900: "#1e3a8a",
        },
        // サークルのテーマカラー（例: 音楽サークル）
        brand: {
          light: "#fef3c7", // 明るい黄色
          DEFAULT: "#f59e0b", // オレンジ
          dark: "#b45309", // 濃いオレンジ
        },
      },
    },
  },
  plugins: [],
};

export default config;
```

**使用例:**

```typescript
// 従来の青色
<button className="bg-blue-600 hover:bg-blue-700">
  ボタン
</button>

// カスタムカラー（brand）
<button className="bg-brand hover:bg-brand-dark text-white">
  ボタン
</button>

// primary-500（既存の青と同じ）
<button className="bg-primary-500 hover:bg-primary-600">
  ボタン
</button>
```

### フォントの変更

**next/font で Google Fonts を使用:**

`src/app/layout.tsx` を更新：

```typescript
import { Inter, Noto_Sans_JP } from "next/font/google";

// 英語フォント
const inter = Inter({ subsets: ["latin"] });

// 日本語フォント
const notoSansJP = Noto_Sans_JP({
  weight: ["400", "500", "700"],
  subsets: ["latin"],
  display: "swap",
});

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="ja">
      <body className={notoSansJP.className}>{children}</body>
    </html>
  );
}
```

**フォントサイズのカスタマイズ:**

```typescript
// tailwind.config.ts
export default {
  theme: {
    extend: {
      fontSize: {
        xs: "0.75rem", // 12px
        sm: "0.875rem", // 14px
        base: "1rem", // 16px
        lg: "1.125rem", // 18px
        xl: "1.25rem", // 20px
        "2xl": "1.5rem", // 24px
        "3xl": "1.875rem", // 30px
        "4xl": "2.25rem", // 36px
        "5xl": "3rem", // 48px
      },
    },
  },
};
```

### レイアウトの変更

**例: サイドバーレイアウトの追加**

`src/app/dashboard/layout.tsx` を作成：

```typescript
import Link from "next/link";
import { Home, Users, Calendar, Settings } from "lucide-react";

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div className="flex h-screen">
      {/* サイドバー */}
      <aside className="w-64 bg-gray-800 text-white p-6">
        <h2 className="text-2xl font-bold mb-8">ダッシュボード</h2>

        <nav className="space-y-2">
          <Link
            href="/dashboard"
            className="flex items-center gap-3 px-4 py-3 rounded-lg hover:bg-gray-700 transition"
          >
            <Home className="w-5 h-5" />
            ホーム
          </Link>
          <Link
            href="/dashboard/users"
            className="flex items-center gap-3 px-4 py-3 rounded-lg hover:bg-gray-700 transition"
          >
            <Users className="w-5 h-5" />
            ユーザー
          </Link>
          <Link
            href="/dashboard/events"
            className="flex items-center gap-3 px-4 py-3 rounded-lg hover:bg-gray-700 transition"
          >
            <Calendar className="w-5 h-5" />
            イベント
          </Link>
          <Link
            href="/dashboard/settings"
            className="flex items-center gap-3 px-4 py-3 rounded-lg hover:bg-gray-700 transition"
          >
            <Settings className="w-5 h-5" />
            設定
          </Link>
        </nav>
      </aside>

      {/* メインコンテンツ */}
      <main className="flex-1 overflow-y-auto bg-gray-50">
        <div className="max-w-7xl mx-auto px-8 py-8">{children}</div>
      </main>
    </div>
  );
}
```

---

## 24.3 確認チェックリスト

Chapter 24 の内容を理解できたか確認しましょう。

### 新機能の追加

- [ ] Prisma スキーマにモデルを追加できる
- [ ] データベースにマイグレーションを適用できる
- [ ] API Route を実装できる（CRUD 操作）
- [ ] 権限チェックとバリデーションを実装できる
- [ ] ページコンポーネントを作成できる
- [ ] フォームコンポーネントを作成できる

### デザインのカスタマイズ

- [ ] Tailwind CSS の色をカスタマイズできる
- [ ] Google Fonts を導入できる
- [ ] レイアウトをカスタマイズできる

---

## まとめ

この章では、新しい機能を追加する方法を学びました。

### 🎓 この章で学んだこと

#### 機能追加の流れ

- ✅ データモデルの設計と追加
- ✅ API の実装（CRUD 操作）
- ✅ UI の実装（ページ、フォーム）
- ✅ 権限チェックとバリデーション

#### 実例: お知らせ機能

- ✅ Announcement モデルの作成
- ✅ お知らせ作成・一覧・詳細 API
- ✅ お知らせ一覧・作成・詳細ページ
- ✅ 重要度による視覚的な区別

#### デザインカスタマイズ

- ✅ Tailwind CSS のカラーパレット
- ✅ Google Fonts の導入
- ✅ サイドバーレイアウト

### 💡 重要なポイント

#### 機能追加の標準フロー

```
1. 企画・設計
   → 何を作るか明確にする

2. データモデル
   → Prismaスキーマを更新

3. API実装
   → CRUD操作を実装

4. UI実装
   → ページとコンポーネント

5. 動作確認
   → 手動テスト
```

#### コードの再利用

```typescript
// 既存のコンポーネントを活用
-Avatar -
  MarkdownEditor -
  MarkdownContent -
  Modal -
  // 共通のパターンを使用
  権限チェック -
  バリデーション -
  エラーハンドリング;
```

### 🚀 次のステップ

次の章では、トラブルシューティングについて学びます：

- **Chapter 25**: トラブルシューティング
  - よくあるエラーと対処法
  - デバッグ方法
  - パフォーマンス問題の解決

開発中に遭遇する問題の解決方法を学びましょう！

---

[← 前の章：第 23 章 セキュリティ](23-セキュリティ.md) | [目次に戻る](00-目次.md) | [次の章へ：第 25 章 トラブルシューティング →](25-トラブルシューティング.md)

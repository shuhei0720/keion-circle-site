<div align="center">

# 🎸 BOLD 軽音 メンバーサイト

**Next.js 16 + TypeScript で構築されたモダンなメンバー専用コミュニティプラットフォーム**

[![Next.js](https://img.shields.io/badge/Next.js-16.0-black?style=flat-square&logo=next.js)](https://nextjs.org/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.0-blue?style=flat-square&logo=typescript)](https://www.typescriptlang.org/)
[![Prisma](https://img.shields.io/badge/Prisma-5.0-2D3748?style=flat-square&logo=prisma)](https://www.prisma.io/)
[![Vercel](https://img.shields.io/badge/Deploy-Vercel-black?style=flat-square&logo=vercel)](https://vercel.com/)

[🌐 本番環境](https://keion-circle-site.vercel.app/) | [📖 技術スタック](#技術スタック) | [🚀 機能](#機能)

</div>

---

## 📋 目次

- [プロジェクト概要](#プロジェクト概要)
- [機能](#機能)
- [技術スタック](#技術スタック)
- [アーキテクチャ](#アーキテクチャ)
- [データベース設計](#データベース設計)
- [認証システム](#認証システム)
- [楽観的UI実装](#楽観的ui実装)
- [API仕様](#api仕様)
- [デプロイメント](#デプロイメント)

---

## 🎯 プロジェクト概要

BOLD 軽音メンバーサイトは、音楽サークルメンバーのためのモダンなコミュニティプラットフォームです。
リアルタイムコミュニケーション、スケジュール調整、メディア共有など、サークル活動に必要な機能を統合しています。

### 🎨 特徴

- **🚀 高速**: Next.js 16 App Router による最適化
- **📱 レスポンシブ**: モバイルファーストデザイン
- **⚡ リアルタイム**: 楽観的UI による即座のフィードバック
- **🔒 セキュア**: NextAuth.js v5 による堅牢な認証
- **🎭 役割管理**: 管理者と一般メンバーの権限分離

---

## 🚀 機能

### 👤 認証システム

```typescript
🔐 マルチプロバイダー認証
├─ Google OAuth 2.0
│  └─ 自動ユーザー登録（名前・メール・アバター）
└─ メールアドレス + パスワード
   └─ bcryptjs によるハッシュ化
```

- **JWT セッション管理**: トークンサイズ最小化
- **役割ベース制御**: admin / member
- **セッション永続化**: 30日間有効

### 📝 投稿機能

```typescript
✨ 管理者専用の投稿管理
├─ YouTube 動画埋め込み
│  └─ react-youtube による最適化
├─ 画像アップロード（最大10MB）
│  └─ Markdown 形式で挿入
├─ 参加状況管理
│  └─ 参加/不参加の登録
└─ いいね機能
   └─ 楽観的UI による即座の反映
```

- **公開アクセス**: ログイン不要で閲覧可能
- **リアルタイム更新**: 即座のUI反映
- **ページネーション**: 5件/ページ

### 📅 スケジュール調整

```typescript
📆 複数候補日による投票システム
├─ 候補日の設定（複数可）
├─ 投票オプション
│  ├─ ○ 参加可能
│  ├─ △ 未定
│  └─ × 参加不可
├─ コメント機能
│  └─ 各候補日にコメント可能
└─ 最有力候補の自動表示
   └─ 参加者数に基づく自動選出
```

- **楽観的UI**: 投票が即座に反映
- **参加者一覧**: リアルタイム表示
- **投票状況の可視化**: 色分け表示

### 💬 リアルタイムチャット

```typescript
⚡ 即時メッセージング
├─ テキストメッセージ
│  └─ 楽観的UI による即座の表示
├─ ファイル送信（最大10MB）
│  ├─ 画像
│  ├─ ドキュメント
│  └─ その他ファイル
└─ 自動スクロール
   └─ 新着メッセージに追従
```

- **ポーリング**: 5秒間隔で更新
- **ファイルプレビュー**: 画像の自動表示
- **送信者情報**: アバター・名前表示

### 👥 ユーザー管理（管理者専用）

```typescript
🛠️ 包括的なユーザー管理
├─ ユーザー一覧表示
│  ├─ 名前・メールアドレス
│  ├─ 登録日
│  └─ 活動統計（投稿・メッセージ・投票）
├─ 役割変更
│  └─ 管理者 ⇄ 通常ユーザー
└─ ユーザー削除
   └─ 関連データの自動削除
```

- **モバイル対応**: カードレイアウト
- **楽観的UI**: 即座の操作反映
- **安全装置**: 自己削除の防止

---

## 🛠 技術スタック

### フロントエンド

| 技術 | バージョン | 用途 |
|------|-----------|------|
| **Next.js** | 16.1.0 | React フレームワーク（App Router） |
| **React** | 19.0.0 | UI ライブラリ |
| **TypeScript** | 5.x | 型安全性 |
| **Tailwind CSS** | 4.0 | スタイリング |
| **Lucide React** | - | アイコンライブラリ |
| **react-youtube** | - | YouTube 埋め込み |

### バックエンド

| 技術 | バージョン | 用途 |
|------|-----------|------|
| **Next.js API Routes** | 16.1.0 | サーバーレス API |
| **NextAuth.js** | 5.0.0-beta.30 | 認証システム |
| **Prisma** | 5.22.0 | ORM |
| **PostgreSQL** | - | データベース（本番） |
| **bcryptjs** | - | パスワードハッシュ化 |

### インフラ

| サービス | 用途 |
|---------|------|
| **Vercel** | ホスティング・デプロイ |
| **Supabase** | PostgreSQL データベース |
| **GitHub** | バージョン管理 |

---

## 🏗 アーキテクチャ

### システム構成

```
┌─────────────────────────────────────────────────┐
│                   Client Layer                   │
│  ┌──────────────┐  ┌──────────────┐            │
│  │   Browser    │  │    Mobile    │            │
│  └──────────────┘  └──────────────┘            │
└───────────────────────┬─────────────────────────┘
                        │ HTTPS
┌───────────────────────┴─────────────────────────┐
│              Next.js 16 (App Router)             │
│  ┌──────────────┐  ┌──────────────┐            │
│  │   Pages      │  │  API Routes  │            │
│  │   (RSC)      │  │  (Serverless)│            │
│  └──────────────┘  └──────────────┘            │
└───────────────────────┬─────────────────────────┘
                        │
┌───────────────────────┴─────────────────────────┐
│              Authentication Layer                │
│           NextAuth.js v5 (JWT Strategy)          │
│  ┌──────────────┐  ┌──────────────┐            │
│  │ Google OAuth │  │  Credentials │            │
│  └──────────────┘  └──────────────┘            │
└───────────────────────┬─────────────────────────┘
                        │
┌───────────────────────┴─────────────────────────┐
│                 Data Layer                       │
│              Prisma ORM + PostgreSQL             │
│  ┌──────────────────────────────────┐           │
│  │  Supabase (Mumbai Region)        │           │
│  └──────────────────────────────────┘           │
└──────────────────────────────────────────────────┘
```

### ディレクトリ構造

```
src/
├── app/                    # Next.js App Router
│   ├── api/               # API Routes
│   │   ├── auth/         # 認証エンドポイント
│   │   ├── posts/        # 投稿 CRUD
│   │   ├── schedules/    # スケジュール管理
│   │   ├── messages/     # チャット
│   │   └── users/        # ユーザー管理
│   ├── auth/             # 認証ページ
│   ├── posts/            # 投稿ページ
│   ├── schedules/        # スケジュールページ
│   ├── chat/             # チャットページ
│   ├── users/            # ユーザー管理
│   ├── privacy/          # プライバシーポリシー
│   └── terms/            # 利用規約
├── components/            # React コンポーネント
│   └── DashboardLayout.tsx
├── lib/                   # ユーティリティ
│   ├── auth.ts           # NextAuth 設定
│   └── prisma.ts         # Prisma クライアント
└── types/                # TypeScript 型定義
    └── next-auth.d.ts    # NextAuth 型拡張
```

---

## 🗄 データベース設計

### ER図

```
┌──────────────┐       ┌──────────────┐
│     User     │───────│   Account    │
│              │ 1   N │              │
└──────┬───────┘       └──────────────┘
       │
       │ 1
       │
       │ N
   ┌───┴────────────────────────┬─────────────────┐
   │                            │                 │
┌──┴──────────┐    ┌───────────┴──┐    ┌────────┴────────┐
│    Post     │    │   Message    │    │ScheduleResponse │
│             │    │              │    │                 │
└──┬──────────┘    └──────────────┘    └─────────────────┘
   │
   ├── PostLike
   └── PostParticipant
```

### 主要モデル

#### User（ユーザー）

```prisma
model User {
  id            String    @id @default(cuid())
  name          String?
  email         String?   @unique
  password      String?
  avatarUrl     String?
  role          String    @default("member") // admin, member
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
}
```

#### Post（投稿）

```prisma
model Post {
  id          String   @id @default(cuid())
  title       String
  content     String?
  youtubeUrl  String?
  userId      String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
```

#### Schedule（スケジュール）

```prisma
model Schedule {
  id          String   @id @default(cuid())
  title       String
  description String
  createdAt   DateTime @default(now())
  dates       ScheduleDate[]
}

model ScheduleDate {
  id         String   @id @default(cuid())
  date       String
  scheduleId String
  responses  ScheduleResponse[]
}
```

---

## 🔐 認証システム

### 認証フロー

```typescript
// 1. Google OAuth フロー
User clicks "Google Login"
  → Redirect to Google
  → User authorizes
  → Callback with tokens
  → Create/Update user in DB
  → Create JWT session
  → Redirect to home

// 2. Credentials フロー
User submits email + password
  → Validate credentials
  → Compare hashed password
  → Create JWT session
  → Redirect to home
```

### JWT トークン構造

```typescript
{
  sub: "user_id",           // ユーザーID
  role: "admin|member",     // 役割
  iat: 1640000000,          // 発行時刻
  exp: 1642592000           // 有効期限（30日）
}
```

### セキュリティ対策

- ✅ パスワードハッシュ化（bcrypt、10 rounds）
- ✅ CSRF 保護（NextAuth.js 組み込み）
- ✅ HTTPOnly Cookie（XSS 対策）
- ✅ Secure Cookie（HTTPS のみ）
- ✅ トークンサイズ最小化（ヘッダーサイズ制限対応）

---

## ⚡ 楽観的UI実装

### 実装パターン

```typescript
const handleAction = async (id: string, data: any) => {
  // 1. 即座にUIを更新（楽観的）
  setData(prevData => updateOptimistically(prevData, id, data))
  
  try {
    // 2. バックグラウンドでAPIリクエスト
    const res = await fetch(`/api/endpoint/${id}`, {
      method: 'POST',
      body: JSON.stringify(data)
    })
    
    if (!res.ok) {
      // 3. エラー時のみ元に戻す（悲観的）
      fetchData() // サーバーから正しいデータを取得
      alert('操作に失敗しました')
    }
  } catch (error) {
    // 4. ネットワークエラー時も元に戻す
    fetchData()
    alert('操作に失敗しました')
  }
}
```

### 実装済み機能

| 機能 | 説明 |
|------|------|
| **投稿のいいね** | ハートアイコンが即座に変化 |
| **投稿の参加** | 参加状態が即座に反映 |
| **スケジュール投票** | 投票結果が即座に表示 |
| **チャット送信** | メッセージが即座に表示 |
| **ユーザー管理** | 削除・役割変更が即座に反映 |

---

## 📡 API仕様

### 認証

```typescript
POST /api/auth/signup
Body: { name, email, password }
Response: { success: true }

POST /api/auth/signin
Body: { email, password }
Response: { session: {...} }
```

### 投稿

```typescript
GET    /api/posts              // 全投稿取得
POST   /api/posts              // 投稿作成（管理者のみ）
PUT    /api/posts/:id          // 投稿更新（管理者のみ）
DELETE /api/posts/:id          // 投稿削除（管理者のみ）
POST   /api/posts/:id/like     // いいね
DELETE /api/posts/:id/like     // いいね解除
POST   /api/posts/:id/participate  // 参加登録
```

### スケジュール

```typescript
GET  /api/schedules                         // 全スケジュール取得
POST /api/schedules                         // スケジュール作成
POST /api/schedules/:dateId/response        // 投票
```

### チャット

```typescript
GET  /api/messages              // メッセージ取得
POST /api/messages              // メッセージ送信
```

### ユーザー管理

```typescript
GET    /api/users               // ユーザー一覧（管理者のみ）
PATCH  /api/users/:id           // 役割変更（管理者のみ）
DELETE /api/users/:id           // ユーザー削除（管理者のみ）
```

---

## 🚀 デプロイメント

### 本番環境

- **URL**: https://keion-circle-site.vercel.app/
- **ホスティング**: Vercel
- **データベース**: Supabase PostgreSQL (Mumbai)
- **自動デプロイ**: main ブランチへの push

### 環境変数

```bash
# 認証
AUTH_SECRET=<ランダムな秘密鍵>
AUTH_URL=https://your-domain.vercel.app
AUTH_TRUST_HOST=true

# NextAuth v5
NEXTAUTH_SECRET=<AUTH_SECRETと同じ>
NEXTAUTH_URL=https://your-domain.vercel.app

# データベース
DATABASE_URL=postgresql://user:password@host:port/database

# Google OAuth
GOOGLE_CLIENT_ID=<Google Cloud Console>
GOOGLE_CLIENT_SECRET=<Google Cloud Console>
```

### デプロイ手順

1. GitHub に push
2. Vercel が自動的にビルド開始
3. Prisma migration 自動実行
4. デプロイ完了

---

## 📄 ライセンス

© 2025 BOLD 軽音. All rights reserved.

---

<div align="center">

**Built with ❤️ using Next.js 16**

[⬆ トップに戻る](#-bold-軽音-メンバーサイト)

</div>
